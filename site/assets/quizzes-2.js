/* ==========================================================================
   quizzes-2.js — the question bank, chapters 13 to 25.
   Format documented at the top of quizzes-1.js.
   ========================================================================== */

window.QUIZZES = Object.assign(window.QUIZZES || {}, {

  /* ------------------------------------------------------------------ 13 */
  "13-rest-principles": [
    { q: "Which HTTP verbs are idempotent?",
      a: ["GET, PUT, DELETE (and HEAD)", "POST only", "All of them", "None of them"],
      c: 0,
      why: "Idempotent means calling it again produces the same end state, not the same response. DELETE twice leaves the resource deleted — the second call returning 404 does not break idempotency. POST is the odd one out, which is why a retried POST can double-charge somebody." },

    { q: "A client sends a request that fails validation. What status code?",
      a: ["200 with an error message in the body", "400 Bad Request", "500 Internal Server Error", "404 Not Found"],
      c: 1,
      why: "The 4xx/5xx split is the important one: 4xx says the client can fix it, 5xx says only you can. Returning 200 with `{ success: false }` breaks every client, cache and monitor that reads the status line." },

    { q: "Which URL is the better REST design for “the orders belonging to customer 42”?",
      a: ["/getOrdersByCustomer?id=42", "/api/customers/42/orders", "/api/OrderService/Fetch", "/api/orders/get/42"],
      c: 1,
      why: "Nouns for resources, plural, hierarchy for containment, and the verb carried by the HTTP method. `getX` in a path means the design is RPC wearing REST's clothes." },

    { q: "What does “stateless” actually forbid in a REST API?",
      a: ["Using a database",
          "Storing per-client conversational state on the server between requests — every request must carry everything needed to serve it",
          "Caching",
          "Authentication"],
      c: 1,
      why: "Persisted application state in a database is fine. Server-side session affinity is what breaks: it stops you adding a second instance behind a load balancer." },

    { q: "PUT versus PATCH?",
      a: ["Identical",
          "PUT replaces the whole resource with the body you send; PATCH applies a partial change",
          "PATCH is for creation",
          "PUT cannot create a resource"],
      c: 1,
      why: "The practical trap: implementing PUT and then only updating the fields that were present in the JSON. That is PATCH behaviour under PUT's name, and it silently keeps values the client intended to clear." },

    { q: "A POST creates a resource successfully. What should come back?",
      a: ["200 with an empty body",
          "201 Created, a Location header pointing at the new resource, and usually the created representation",
          "204 No Content",
          "302 Found"],
      c: 1,
      why: "In ASP.NET Core that is `Results.Created(uri, dto)` or `CreatedAtRoute`. The Location header is the part everybody forgets and every API guideline requires." },

    { q: "Select every level of the Richardson maturity model, in order.",
      a: ["0: one URI, one verb (RPC over HTTP)", "1: resources get their own URIs", "2: HTTP verbs and status codes used correctly", "3: hypermedia controls (HATEOAS)"],
      c: [0, 1, 2, 3],
      why: "Almost every production API stops at level 2, and that is a defensible, honest answer to give an interviewer. Claiming level 3 without links in your payloads is not." },

    { q: "What is the difference between 401 and 403?",
      a: ["They are interchangeable",
          "401 means not authenticated (who are you?); 403 means authenticated but not permitted (I know who you are, and no)",
          "401 is for expired tokens only",
          "403 means the server is down"],
      c: 1,
      why: "The naming is historically confusing — 401 is literally labelled “Unauthorized” but means unauthenticated. Getting this right in an answer is a cheap, visible signal." },
  ],

  /* ------------------------------------------------------------------ 14 */
  "14-web-api": [
    { q: "In the ASP.NET Core middleware pipeline, what breaks if UseAuthorization() runs before UseAuthentication()?",
      a: ["Nothing",
          "Nobody is ever authenticated by the time authorization runs, so every protected endpoint returns 401 regardless of the token",
          "The application will not start",
          "Requests are served twice"],
      c: 1,
      why: "Order is behaviour in that file. The same class of bug: registering exception handling late means exceptions from earlier middleware escape it entirely." },

    { q: "What are the three DI lifetimes in ASP.NET Core?",
      a: ["Static, Instance, Global",
          "Singleton (one for the app), Scoped (one per request), Transient (a new one every time it is injected)",
          "Short, Medium, Long",
          "Public, Private, Internal"],
      c: 1,
      why: "The interview follow-up is the captive dependency: injecting a Scoped DbContext into a Singleton keeps the first request's context alive for the life of the process." },

    { q: "Why should an API endpoint never return an EF Core entity directly?",
      a: ["It is slower",
          "It leaks the database shape into the contract, can serialise navigation properties into cycles or accidental over-fetching, and exposes fields nobody meant to publish",
          "Entities cannot be serialised",
          "It breaks OpenAPI"],
      c: 1,
      why: "The accidental-disclosure version of this bug is the one that ends up in a security review: a PasswordHash or an internal cost price serialised because somebody added a property." },

    { q: "Where should a production connection string come from?",
      a: ["appsettings.json, committed to git",
          "The environment, user-secrets locally, or a key vault — anywhere that is not source control",
          "A hard-coded constant",
          "The query string"],
      c: 1,
      why: "Configuration providers are layered and later ones win: appsettings → appsettings.{Environment} → user-secrets → environment variables → command line. Knowing that precedence order is the practical half of the answer." },

    { q: "What is model binding?",
      a: ["Mapping an entity to a database table",
          "Populating an action's parameters from the route, query string, headers and body of the request",
          "Compiling Razor views",
          "Registering services"],
      c: 1,
      why: "And [ApiController] adds automatic 400 responses when validation fails, plus inference of [FromBody] — which is why an action without it silently behaves differently." },

    { q: "Which is the modern, framework-supported way to return an error from an ASP.NET Core API?",
      a: ["Throwing a raw exception and letting it 500",
          "A Problem Details document (RFC 7807) with type, title, status and traceId — the same shape for every error",
          "A 200 with a message string",
          "A plain-text body"],
      c: 1,
      why: "One error shape means one client-side handler. AddProblemDetails plus an IExceptionHandler makes framework 404s and your own thrown errors come out identical." },

    { q: "Select every statement that is true of minimal APIs compared with controllers.",
      a: ["They have lower per-request overhead",
          "They can do everything controllers can, including filters and model validation",
          "They keep the route and its handler in one visible place",
          "They cannot use dependency injection"],
      c: [0, 2],
      why: "Minimal APIs do support DI (parameters are resolved from the container). Filters exist but the ecosystem around controllers — model binding conventions, action filters, [ApiController] behaviours — is still richer." },

    { q: "What does the `[ApiController]` attribute change?",
      a: ["Nothing visible",
          "Automatic 400 on invalid ModelState, inference of binding sources, and ProblemDetails-shaped error responses",
          "It enables HTTPS",
          "It registers the controller with DI"],
      c: 1,
      why: "The automatic 400 is the one that surprises people: a manual `if (!ModelState.IsValid)` check inside the action becomes unreachable." },
  ],

  /* ----------------------------------------------------------------- 14b */
  "14b-integrations": [
    { q: "Why is creating a new HttpClient for each request a problem, even though HttpClient implements IDisposable?",
      a: ["It allocates too much memory",
          "Each disposal leaves a socket in TIME_WAIT for minutes, so under load the ephemeral ports are exhausted and connections start failing",
          "HttpClient is not thread-safe",
          "It resets the cookie container"],
      c: 1,
      why: "The error surfaces as “address in use”, which reads like a server fault rather than a client bug. It is the most common .NET networking mistake." },

    { q: "A colleague fixes that by making one static HttpClient for the lifetime of the process. What have they broken?",
      a: ["Nothing — that is the recommended fix",
          "The connection never picks up DNS changes, so a failover on the other side stays invisible until a restart",
          "Thread safety — a static HttpClient cannot be shared",
          "Timeouts stop working"],
      c: 1,
      why: "This is why IHttpClientFactory exists: it pools the expensive handlers and rotates them periodically, solving both the sockets and the stale DNS." },

    { q: "What is the default HttpClient timeout, and why does it matter?",
      a: ["30 seconds — usually fine",
          "100 seconds — long enough that under load it behaves like a hang, so an explicit timeout belongs on every outbound client",
          "There is none by default",
          "5 seconds — usually too aggressive"],
      c: 1,
      why: "Set a per-attempt timeout AND a total timeout, or three retries of a slow call quietly add up while your own caller waits." },

    { q: "A POST that creates an order times out. What is the correct reading of that timeout?",
      a: ["It failed — retry immediately",
          "It is unknown: the work may well have succeeded, so retrying blindly can create a duplicate",
          "It succeeded — the response was just lost",
          "It failed permanently — do not retry"],
      c: 1,
      why: "Across a boundary there are three outcomes, not two. Either make the operation idempotent with a key they recognise, or query before retrying." },

    { q: "Why must a retry policy add jitter to its backoff?",
      a: ["To make the logs easier to read",
          "Without it, every instance that failed during the same outage retries at the same instant, turning their blip into their outage",
          "Jitter reduces the number of retries",
          "It is required by the HTTP specification"],
      c: 1,
      why: "Randomising the delay is what makes retry safe at scale. Synchronised retries are a self-inflicted denial of service." },

    { q: "What does a circuit breaker add that retry alone does not?",
      a: ["It retries more times",
          "After enough failures it fails fast for a cooling-off period, so you stop adding traffic to a struggling dependency and stop parking your own threads against it",
          "It caches the last successful response",
          "It routes to a backup endpoint"],
      c: 1,
      why: "It protects both sides. Retry alone adds load exactly when the other system cannot cope, while every one of your threads waits." },

    { q: "Their API responds 429 with a Retry-After header. What should your client do?",
      a: ["Ignore it and use the configured exponential backoff",
          "Honour Retry-After — it is their statement of when they will be ready, and it beats your guess",
          "Fail the request permanently",
          "Retry immediately, since 429 is not a server error"],
      c: 1,
      why: "Your backoff is a guess; theirs is information. Fall back to your own schedule only when the header is absent." },

    { q: "Why map a third-party DTO into your own model at the boundary instead of using their type directly?",
      a: ["Because their type will not deserialise correctly",
          "Because otherwise their field names, date formats and enum spellings become load-bearing throughout your codebase, and their next version breaks you everywhere",
          "For performance",
          "Because DTOs cannot be used outside the Infrastructure project"],
      c: 1,
      why: "It is chapter 23's dependency rule applied to a third party — and it gives the unknown enum value exactly one place to be handled." },

    { q: "Their API starts returning a status value your switch has never seen. What should happen?",
      a: ["Throw, so the problem is visible",
          "Map it to an explicit Unknown, because a new value on their side is a normal event that must not take your service down",
          "Silently map it to the most likely existing value",
          "Log it and return null"],
      c: 1,
      why: "Throwing turns their routine release into your outage; guessing corrupts data quietly. An explicit Unknown arm is the answer, and it only exists if you translate at the boundary." },

    { q: "You verify a webhook's HMAC signature and it never matches, though the secret is right. What is the most likely cause?",
      a: ["The wrong hash algorithm",
          "The body was deserialised and re-serialised before hashing, which changes the bytes — the signature must be computed over the raw body",
          "The header name is wrong",
          "The secret needs base64 decoding"],
      c: 1,
      why: "Whitespace, key order and number formatting all change on a round trip. EnableBuffering and read the raw stream. This is the bug that costs a whole day." },

    { q: "Which of these belong on a webhook receiver? (Select every one that applies.)",
      a: ["Verify the signature in constant time",
          "Reject requests with a stale timestamp",
          "Queue the work and return immediately",
          "Process the payload inline so the sender knows it succeeded"],
      c: [0, 1, 2],
      why: "Processing inline is the trap: the sender times out and redelivers, so one event becomes four. Acknowledge fast, work asynchronously, and deduplicate on their event id." },

    { q: "An Italian partner sends a CSV containing the value 1.234,56. Your importer parses it with InvariantCulture. What is the worst outcome?",
      a: ["An exception, which you will notice",
          "It parses to a different number without any error, and the wrong value is committed",
          "The value becomes null",
          "The file fails to open"],
      c: 1,
      why: "A crash is a good day. Silent misparsing of decimals and dates is why an explicit culture belongs on every parse at an integration boundary — chapter 23, arriving as a production incident." },

    { q: "Why does the file-drop convention write orders.tmp and then rename it to orders.csv?",
      a: ["To keep a backup of the original",
          "Because rename is atomic, so the reader can never pick up a half-uploaded file",
          "To trigger the file-system watcher",
          "Because .csv files cannot be created directly over SFTP"],
      c: 1,
      why: "Without it you will eventually read a file mid-upload and import half a day's orders. If the sender will not do it, wait for the size to stop changing." },

    { q: "What is the most practical way to test that your client retries correctly and opens its circuit breaker?",
      a: ["Call the real API and hope it fails",
          "A stub HttpMessageHandler for mapping and error handling, and WireMock.Net when the behaviour under test is HTTP-level like retries and delays",
          "Unit-test the policy configuration object",
          "Integration tests against a staging environment"],
      c: 1,
      why: "A stub handler needs no network and no package. WireMock gives you a real server you can program with failures and delays — the only practical way to exercise a breaker." },
  ],

  /* ------------------------------------------------------------------ 15 */
  "15-api-security": [
    { q: "Authentication versus authorization?",
      a: ["Two words for the same check",
          "Authentication establishes who the caller is; authorization decides what that caller may do",
          "Authorization happens first",
          "Authentication is only for admins"],
      c: 1,
      why: "Which is why the middleware order is authentication then authorization, and why 401 and 403 are different codes." },

    { q: "What are the three parts of a JWT, and which one is encrypted?",
      a: ["Header, payload, signature — and the payload is encrypted",
          "Header, payload, signature — and NONE of them is encrypted; it is base64url-encoded and merely signed",
          "Key, value, hash — the hash is encrypted",
          "Issuer, audience, expiry — all encrypted"],
      c: 1,
      why: "Anyone holding the token can read every claim in it. That single fact rules out putting anything confidential in a JWT payload, and it is the most common misunderstanding in junior interviews." },

    { q: "Which token validation parameter, if turned off, lets a forged token through?",
      a: ["ValidateAudience", "ValidateIssuerSigningKey", "ValidateLifetime", "ValidateIssuer"],
      c: 1,
      why: "Without signature validation anyone can mint a token claiming any role. The others matter too — skipping ValidateLifetime means expired tokens work forever, and skipping ValidateAudience accepts a token minted for a different service." },

    { q: "How should a password be stored?",
      a: ["Encrypted with AES so it can be decrypted for password reminders",
          "Hashed with a slow, salted, purpose-built algorithm — bcrypt, scrypt, Argon2 or PBKDF2 with a high iteration count",
          "SHA-256, which is cryptographically strong",
          "In plain text, behind a firewall"],
      c: 1,
      why: "SHA-256 is the trap: it is strong AND fast, and fast is the wrong property here — a GPU tries billions per second. Slowness is the feature. Also: if you can email a user their password, you have already failed." },

    { q: "What is the practical problem with long-lived JWT access tokens?",
      a: ["They are large",
          "There is no way to revoke one before it expires — a stolen token is valid until its exp",
          "They cannot carry roles",
          "They only work over HTTPS"],
      c: 1,
      why: "Hence short access tokens (minutes) plus a longer refresh token that IS stored server-side and can be revoked. That is the standard shape and the expected answer." },

    { q: "A JWT is stored in browser localStorage. What is the specific risk?",
      a: ["It cannot be read by JavaScript",
          "Any XSS on the page can read it and exfiltrate it; an httpOnly cookie cannot be read by script",
          "It expires too quickly",
          "It is sent on every request automatically"],
      c: 1,
      why: "The trade is real: cookies are XSS-safe but CSRF-exposed, which is what SameSite and antiforgery tokens address. Being able to state the trade-off is the answer, not picking a side." },

    { q: "Select every mistake that actually ships in real .NET APIs.",
      a: ["The signing key committed in appsettings.json",
          "CORS configured with AllowAnyOrigin together with AllowCredentials",
          "Authorization checked on the UI but not on the endpoint",
          "Using HTTPS in production"],
      c: [0, 1, 2],
      why: "The CORS pair is actually rejected by browsers, which usually gets “fixed” by reflecting the request's Origin back — which is the same hole with extra steps." },

    { q: "What does a FallbackPolicy requiring an authenticated user achieve?",
      a: ["It blocks all traffic",
          "Endpoints are protected unless they explicitly opt out — so forgetting an attribute leaves an endpoint closed, not open",
          "It disables anonymous health checks permanently",
          "It replaces authentication"],
      c: 1,
      why: "Secure by default. The inverse default — open unless annotated — is how an endpoint reaches production unprotected because somebody forgot one line." },
  ],

  /* ------------------------------------------------------------------ 16 */
  "16-aspnet-mvc": [
    { q: "In MVC, what belongs in the controller?",
      a: ["The business rules",
          "Only coordination: take the request, call something that does the work, pick a view or a result",
          "The SQL queries",
          "The HTML"],
      c: 1,
      why: "A fat controller is the standard smell. If the action has an `if` about business policy rather than about the request, that `if` is in the wrong project." },

    { q: "What is the difference between ViewBag, ViewData and a view model?",
      a: ["None",
          "ViewBag/ViewData are dynamic and untyped — typos fail at runtime; a view model is a class, so the compiler and IntelliSense help you",
          "ViewBag is faster",
          "View models only work in Razor Pages"],
      c: 1,
      why: "The same argument as DTOs in an API: give the view exactly the shape it needs, typed, and never hand it an EF Core entity." },

    { q: "What attack does the antiforgery token prevent, and how?",
      a: ["SQL injection, by escaping input",
          "CSRF — a hidden token that only your own page can know, so a form posted from another site fails validation",
          "XSS, by encoding output",
          "Brute force, by rate limiting"],
      c: 1,
      why: "The attack depends on the browser sending the auth cookie automatically. The token is proof the request came from a page your server rendered — which a cross-site form cannot produce." },

    { q: "Razor renders `@Model.Description` and the description contains `<script>`. What happens?",
      a: ["The script runs",
          "Razor HTML-encodes it by default, so it renders as visible text — you have to opt in with Html.Raw to break that",
          "The page fails to render",
          "It is stripped silently"],
      c: 1,
      why: "Encode-by-default is why XSS in Razor is almost always caused by an explicit Html.Raw. Search for Html.Raw in any codebase you inherit." },

    { q: "What is a tag helper?",
      a: ["A JavaScript library",
          "Server-side code attached to HTML-looking markup — asp-for, asp-action — so the view stays readable HTML and gets validation, names and URLs generated for it",
          "A CSS framework",
          "A Razor comment"],
      c: 1,
      why: "asp-for is doing three jobs at once: name, id and the data-val-* attributes that drive client-side validation from the same DataAnnotations the server uses." },

    { q: "MVC, Razor Pages or Blazor — which fits “a form-heavy internal admin tool, small team, existing MVC codebase”?",
      a: ["Blazor WebAssembly, because it is newest",
          "Stay with MVC, or add Razor Pages for the new page-centric screens — both are the same runtime and can coexist file by file",
          "Rewrite in React",
          "Razor Pages only, after a full migration"],
      c: 1,
      why: "The coexistence point is the useful one: MVC, Razor Pages and Blazor components can live in one project, so migration is incremental rather than a rewrite." },

    { q: "Select every statement that is true about the POST-Redirect-GET pattern.",
      a: ["It stops a refresh re-submitting the form",
          "It gives the user a bookmarkable URL after the action",
          "It requires JavaScript",
          "RedirectToAction is the MVC way to do it"],
      c: [0, 1, 3],
      why: "It is pure HTTP — a 302 after the POST. The browser's “confirm form resubmission” dialog is the symptom of not doing it." },

    { q: "Where does model validation actually run in an MVC form post?",
      a: ["Only in the browser",
          "Both: client-side from data-val attributes for a fast message, and again on the server, because the client can always be bypassed",
          "Only on the server, always",
          "In the database"],
      c: 1,
      why: "“The client is a convenience, the server is the rule” is the sentence to have ready. Anyone can post to your endpoint with curl." },
  ],

  /* ----------------------------------------------------------------- 16b */
  "16b-aspnet-framework": [
    { q: "What is the relationship between ASP.NET (Framework) and ASP.NET Core?",
      a: ["ASP.NET Core is a newer version of the same codebase",
          "They share a name and almost no code — different pipeline, hosting, configuration and async behaviour",
          "ASP.NET Core is ASP.NET compiled for Linux",
          "ASP.NET Core replaced ASP.NET, which no longer runs anywhere"],
      c: 1,
      why: "Framework is Windows/IIS-only on System.Web with an event-based pipeline and web.config. Core is cross-platform, self-hosted, middleware-based, DI-first. .NET Framework 4.8 is still fully supported — it is finished, not deprecated." },

    { q: "In ASP.NET Framework, when does the code in Application_Start run?",
      a: ["Once, when the server boots",
          "Once per request",
          "Every time the application pool starts the app — including after every recycle",
          "Only when the application is deployed"],
      c: 2,
      why: "Pools recycle on a memory threshold, a web.config edit, a schedule or an idle timeout. So static state lasts until the next recycle, not until reboot — and that recycle is why the first request after a quiet period is slow." },

    { q: "This action is on ASP.NET Framework. What happens when it is called?",
      code: "public ActionResult Index()\n{\n    var data = _service.GetAsync().Result;   // GetAsync awaits an HTTP call\n    return View(data);\n}",
      a: ["It works, but wastes a thread",
          "It deadlocks — the request never completes",
          "It throws an AggregateException",
          "It works normally; .Result is fine on the request thread"],
      c: 1,
      why: "Framework installs a synchronization context that allows one thread on the request at a time. .Result holds that thread while the continuation waits to re-enter the same context. Mutual wait, forever." },

    { q: "The same code is moved to ASP.NET Core. What happens now?",
      code: "var data = _service.GetAsync().Result;",
      a: ["It still deadlocks — the bug is in the code, not the framework",
          "It completes, but blocks a thread pool thread for the whole call",
          "The compiler rejects it",
          "It runs faster than the async version"],
      c: 1,
      why: "Core has no synchronization context, so the continuation resumes on any pool thread and .Result returns. The bug does not disappear — it changes disguise from a hang into thread-pool starvation under load. Note the direction: migrating can hide it." },

    { q: "What was ConfigureAwait(false) actually for, and where does it still matter?",
      a: ["It makes the await faster; it matters everywhere",
          "It says the continuation need not resume on the captured context — essential in library code on Framework, a no-op in ASP.NET Core application code",
          "It cancels the operation if it takes too long",
          "It is obsolete and should be removed from all code"],
      c: 1,
      why: "Both pieces of advice you have read are correct, for different runtimes. Being able to say that sentence is what separates a memorised answer from an understood one." },

    { q: "A WebForms page has a Refresh button. Clicking it appears to do nothing. What is the most likely cause?",
      a: ["The button is missing runat=\"server\"",
          "Page_Load re-binds the grid without an if (!IsPostBack) guard, so the state the click was about to act on is thrown away first",
          "ViewState is disabled",
          "The event handler is registered twice"],
      c: 1,
      why: "The lifecycle runs Page_Load BEFORE control events. An unguarded re-bind on every postback is the single most common WebForms bug." },

    { q: "A WebForms page returns two megabytes of HTML and takes eight seconds. Where do you look first?",
      a: ["The database query",
          "ViewState on a data-bound GridView, serialised into a hidden field and round-tripped on every postback",
          "The IIS compression settings",
          "The session provider"],
      c: 1,
      why: "Set EnableViewState=\"false\" on anything the user does not edit. It is the standard cause, and the standard fix." },

    { q: "A logging ActionFilterAttribute is applied to a Web API 2 ApiController. It compiles and deploys but never runs. Why?",
      a: ["Filters must be registered globally to work",
          "It is the System.Web.Mvc filter type; Web API 2 needs the System.Web.Http.Filters one — same class name, different namespace",
          "ApiController does not support filters",
          "The filter needs an [Attribute] suffix"],
      c: 1,
      why: "MVC 5 and Web API 2 are two parallel stacks with two route tables and two filter pipelines. The wrong using gives you silence, not an error — check it first." },

    { q: "Why can HttpContext.Current not exist in ASP.NET Core?",
      a: ["Because Core does not have an HTTP context",
          "It was an ambient static tied to the request thread — a hidden dependency that is null off that thread and untestable; Core declares it instead via IHttpContextAccessor",
          "Because statics are forbidden in .NET Core",
          "It exists in Core, just under a different name"],
      c: 1,
      why: "Task.Run(() => HttpContext.Current.User) throws a NullReferenceException on Framework too. If a ported class cannot be constructed in a test, this is usually why — and the fix is usually to pass the two values it actually wanted." },

    { q: "Users of a Framework app on two load-balanced IIS servers are logged out at random. What is the cause?",
      a: ["The auth cookie has expired",
          "In-process session state: it lives in one pool's memory, is lost on recycle, and is invisible to the other server",
          "The load balancer is misconfigured for HTTPS",
          "The database connection pool is exhausted"],
      c: 1,
      why: "Move session out of process — SQL Server or Redis — and share the key material protecting the auth cookie across both machines. Sticky sessions are a stopgap, not the fix." },

    { q: "Which of these genuinely cannot be ported to modern .NET? (Select every one that applies.)",
      a: ["WebForms",
          "Server-side WCF hosting as-is",
          "AppDomains",
          "A class library containing business logic"],
      c: [0, 1, 2],
      why: "Business logic almost always ports — retarget it to netstandard2.0 and both applications can share it, which is usually the cheapest real progress. WebForms has no equivalent, WCF hosting needs CoreWCF or a REST rewrite, and AppDomains need processes or AssemblyLoadContext." },

    { q: "You are asked how you would migrate a large .NET Framework web app to .NET 8. Which answer is strongest?",
      a: ["Rewrite it in .NET 8, then cut over in one release",
          "Analyse first, do the safe mechanical steps on Framework, retarget shared libraries to netstandard2.0, then move route by route behind a reverse proxy with both apps live",
          "Freeze the old app and build the new features in a separate service",
          "Upgrade the target framework in the csproj and fix the compiler errors"],
      c: 1,
      why: "The rewrite means no delivery, no rollback, and re-discovering ten years of undocumented rules. The incremental route is independently releasable and revertable at every step — and YARP plus Microsoft.AspNetCore.SystemWebAdapters is what makes it practical." },
  ],

  /* ------------------------------------------------------------------ 17 */
  "17-frontend-basics": [
    { q: "What is the DOM?",
      a: ["The HTML file on disk",
          "The browser's live in-memory tree built from the HTML, which JavaScript reads and changes",
          "A JavaScript framework",
          "The CSS rules"],
      c: 1,
      why: "The distinction matters: editing the DOM does not change the file, and viewing source shows the original HTML while the inspector shows the current tree." },

    { q: "Your fetch() to the API is blocked with a CORS error. Where is the fix?",
      a: ["In the browser settings",
          "On the server — it must send Access-Control-Allow-Origin; the browser is enforcing a rule the server has to relax",
          "In the JavaScript, by adding a header",
          "In the DNS"],
      c: 1,
      why: "The single most useful CORS fact: it is enforced by the browser and configured by the server. Nothing you write in the client can grant it, which is why the fix is app.UseCors on the .NET side." },

    { q: "What does `await fetch(url)` resolve to?",
      a: ["The parsed JSON body",
          "A Response object — you still have to await response.json(), and check response.ok yourself",
          "A string",
          "The HTTP status code"],
      c: 1,
      why: "And fetch does NOT reject on a 404 or 500 — only on a network failure. Forgetting `if (!res.ok)` is why a failed call silently produces `undefined` three functions later." },

    { q: "What does TypeScript actually add to JavaScript?",
      a: ["A faster runtime",
          "A compile-time type system that is erased before it runs — the browser executes plain JavaScript",
          "New syntax the browser understands natively",
          "Automatic memory management"],
      c: 1,
      why: "Type erasure is the key idea: no runtime checks. A value typed `User` that arrived from an API response is only a User because you said so — which is why validating at the boundary still matters." },

    { q: "Which HTML choice matters most for accessibility and for the browser's built-in behaviour?",
      a: ["Using <div> with click handlers for everything",
          "Using semantic elements — <button>, <nav>, <label>, <form> — which come with keyboard, focus and screen-reader behaviour for free",
          "Using tables for layout",
          "Adding more CSS classes"],
      c: 1,
      why: "A <div onclick> is not focusable, not reachable by keyboard, and not announced as a control. `<button>` gives you all three without a line of script." },

    { q: "What is the difference between `==` and `===` in JavaScript?",
      a: ["None",
          "== coerces types before comparing ('1' == 1 is true); === compares type and value, so '1' === 1 is false",
          "=== is slower",
          "== only works on numbers"],
      c: 1,
      why: "Use === by default. The coercion table has genuine surprises in it — `[] == false` is true — and no reader should have to remember them." },

    { q: "Select everything that is true about `const` in JavaScript.",
      a: ["The binding cannot be reassigned",
          "The object it points at can still be mutated",
          "It makes the value deeply immutable",
          "It is block-scoped, like let"],
      c: [0, 1, 3],
      why: "`const arr = []; arr.push(1);` is perfectly legal. Object.freeze is the (shallow) tool for actual immutability." },

    { q: "Why does an inline `onclick=\"...\"` attribute age badly compared with addEventListener?",
      a: ["It is slower",
          "Only one handler, logic mixed into markup, and it is exactly the pattern a Content-Security-Policy blocks",
          "It does not work in modern browsers",
          "It cannot access the element"],
      c: 1,
      why: "CSP is the one that bites in an enterprise: a strict policy forbids inline script, and every inline handler becomes a silent no-op." },
  ],

  /* ----------------------------------------------------------------- 17b */
  "17b-javascript-language": [
    { q: "What does this print?",
      code: "console.log('1');\nsetTimeout(() => console.log('2'), 0);\nPromise.resolve().then(() => console.log('3'));\nconsole.log('4');",
      a: ["1 2 3 4", "1 4 2 3", "1 4 3 2", "1 3 4 2"],
      c: 2,
      why: "Synchronous code first (1, 4). Then the engine drains the ENTIRE microtask queue (3). Only then does it take one macrotask (2). setTimeout(fn, 0) means “after everything currently queued”, not “now”." },

    { q: "Which of these go on the microtask queue rather than the macrotask queue? (Select every one that applies.)",
      a: ["A .then() callback",
          "The continuation after an await",
          "A setTimeout callback",
          "A click event handler"],
      c: [0, 1],
      why: "Promise callbacks and await continuations are microtasks and are drained completely each turn. Timers, I/O completions and UI events are macrotasks — exactly one runs per turn." },

    { q: "Why does a long synchronous for-loop freeze the whole page, including animations?",
      a: ["The browser throttles busy scripts",
          "JavaScript has one thread and one call stack, and the browser can only render between turns of the event loop",
          "The garbage collector pauses rendering",
          "It does not — rendering happens on a separate thread"],
      c: 1,
      why: "This is the JavaScript version of chapter 07's rule: never block. In C# blocking starves the thread pool; here there is no pool to starve, so everything stops." },

    { q: "Why is `this` undefined here?",
      code: "const order = { number: 'ORD-1', show() { console.log(this.number); } };\nsetTimeout(order.show, 0);",
      a: ["setTimeout always clears this",
          "`this` is determined by the call site, and passing order.show detaches the function from the object",
          "The method needs to be declared with function()",
          "Objects cannot have methods in JavaScript"],
      c: 1,
      why: "Fix with an arrow wrapper that preserves the call — setTimeout(() => order.show(), 0) — or with order.show.bind(order)." },

    { q: "When is an arrow function the WRONG choice?",
      a: ["As a callback passed to setTimeout",
          "As a method on an object literal, because it has no `this` of its own and captures the enclosing scope instead",
          "Inside a .map() call",
          "As an event handler registered with addEventListener"],
      c: 1,
      why: "An arrow captures `this` lexically and cannot be rebound. That is exactly what you want in a callback and exactly what you do not want in an object method." },

    { q: "What does this print, and what single change fixes it?",
      code: "for (var i = 0; i < 3; i++) {\n  setTimeout(() => console.log(i), 0);\n}",
      a: ["0 1 2 — nothing needs fixing",
          "3 3 3 — change var to let, because a for loop with let creates a new binding each iteration",
          "0 0 0 — wrap the body in an IIFE",
          "undefined three times — hoist the declaration"],
      c: 1,
      why: "A closure captures the variable, not the value — the same law as C#. var is function-scoped so there is only ever one i, and the callbacks run after the loop has finished." },

    { q: "A C# developer chains arr.filter(...).map(...). What differs from the LINQ equivalent?",
      a: ["Nothing — they are identical",
          "The JavaScript methods are eager: filter builds a complete intermediate array before map builds a second one, whereas LINQ streams in one pass",
          "JavaScript evaluates them lazily and LINQ does not",
          "map runs before filter"],
      c: 1,
      why: "Irrelevant on ten items, a habit worth having on ten thousand. Deferred execution is a LINQ property, not a general one." },

    { q: "An array holding 10, 9 and 1 is sorted with .sort() and no comparator. What order comes back?",
      code: "const nums = [10, 9, 1];\nnums.sort();",
      a: ["1, 9, 10 — ascending numeric order",
          "1, 10, 9 — because the values are compared as strings",
          "10, 9, 1 — unchanged",
          "It throws, because sort requires a comparator"],
      c: 1,
      why: "sort() converts to strings and compares them, so \"10\" sorts before \"9\". Pass a comparator: sort((a, b) => a - b). It also mutates the array in place, unlike OrderBy." },

    { q: "Why does this finish instantly and save nothing in order?",
      code: "orders.forEach(async (o) => { await save(o); });",
      a: ["save is not awaited by forEach, so the callbacks all start and the loop returns immediately",
          "forEach cannot take a function argument",
          "async functions cannot be used as callbacks",
          "The array is empty"],
      c: 0,
      why: "forEach ignores the returned promise. Use for…of when you need sequence, or await Promise.all(orders.map(save)) when you want them concurrent." },

    { q: "How does Promise.all differ from Task.WhenAll on first failure?",
      a: ["They behave identically",
          "Promise.all rejects immediately on the first rejection while the others keep running unobserved; Task.WhenAll waits for every task before throwing",
          "Promise.all cancels the remaining promises",
          "Promise.all never rejects"],
      c: 1,
      why: "Use Promise.allSettled when a partial answer is still useful — a dashboard with one broken tile. Promises cannot be cancelled; that is what AbortController is for." },

    { q: "What happens to the error when an async function is called without await and without .catch()?",
      a: ["It is thrown synchronously at the call site",
          "It becomes an unhandled rejection — silent in the browser apart from an unhandledrejection event, and fatal to the process in Node",
          "It is swallowed and logged automatically by the runtime",
          "The function will not compile"],
      c: 1,
      why: "The twin of an unawaited Task in C# — except nothing warns you. It is the usual cause of “the save button does nothing and there is no error”." },

    { q: "A table has 500 rows, each with a delete button, and rows are added dynamically. What is the right way to wire the clicks?",
      a: ["querySelectorAll over the buttons and addEventListener on each",
          "One listener on the container, using bubbling, with e.target.closest('.delete-btn') to identify the button",
          "An inline onclick attribute on each button",
          "A MutationObserver that re-attaches listeners when rows change"],
      c: 1,
      why: "Delegation: one listener instead of 500, and it keeps working for rows created after page load. closest() matters because the click may land on an icon inside the button." },

    { q: "What is the difference between preventDefault() and stopPropagation()?",
      a: ["They are aliases",
          "preventDefault suppresses the browser's default action but the event still bubbles; stopPropagation stops ancestors seeing it but the default action still happens",
          "preventDefault stops bubbling; stopPropagation stops the default action",
          "Both stop the event entirely"],
      c: 1,
      why: "Reaching for stopPropagation to fix a bug usually breaks somebody else's delegated handler months later." },

    { q: "Which is true of `??` compared with `||`?",
      a: ["They are equivalent",
          "?? only falls back on null or undefined, so a valid 0 or empty string survives; || falls back on any falsy value",
          "?? falls back on any falsy value and || only on null",
          "?? cannot be used with numbers"],
      c: 1,
      why: "count || 10 turns a legitimate 0 into 10. Exactly the C# operators from chapter 02, and exactly the same trap." },

    { q: "You inherit a working jQuery page in a legacy app. What is the professional response?",
      a: ["Rewrite it in vanilla JavaScript immediately",
          "Read it and work in it; write new pages without jQuery and touch the old one only where the ticket takes you",
          "Rewrite it as a React SPA",
          "Refuse to work on it until it is modernised"],
      c: 1,
      why: "Removing jQuery from a page that works is a rewrite with regression risk and no business value — the same argument as migrating a legacy app incrementally rather than in one go." },
  ],

  /* ------------------------------------------------------------------ 18 */
  "18-spa-and-typescript": [
    { q: "What makes a single-page application “single-page”?",
      a: ["It only has one screen",
          "The browser loads one HTML document, and navigation afterwards swaps rendered components in place rather than requesting new documents",
          "It has no routing",
          "It is served from one file"],
      c: 1,
      why: "Which is why the server needs a fallback route: refresh on /orders/42 asks the server for a document that does not exist unless it is configured to return index.html." },

    { q: "What is the one idea Angular and React genuinely share?",
      a: ["The same template syntax",
          "The component: a reusable unit of markup, state and behaviour, composed into a tree",
          "Two-way binding",
          "The same build tool"],
      c: 1,
      why: "Getting to that sentence means you can talk about either without having used both — which is the honest position most junior candidates are in and can defend." },

    { q: "Why does a SPA need a build step at all?",
      a: ["To minify only",
          "Browsers do not run TypeScript or JSX, and modules must be bundled and transformed — the dev server does this on the fly with hot reload",
          "To generate the database",
          "It does not; the build step is optional"],
      c: 1,
      why: "The dev server also proxies API calls, which is how a front end on :5173 talks to an API on :5001 without tripping CORS during development." },

    { q: "Your API changes a field from `total` to `totalAmount`. In a TypeScript SPA, when do you find out?",
      a: ["At compile time, automatically",
          "At runtime — unless the types are generated from the OpenAPI document, TypeScript believes whatever interface you hand-wrote",
          "Never",
          "During npm install"],
      c: 1,
      why: "Generating the client from the OpenAPI document is the trick worth knowing: it turns a runtime `undefined` into a compile error, and it is the reason an accurate OpenAPI document has value beyond documentation." },

    { q: "Angular versus React, in a sentence an interviewer will accept?",
      a: ["React is better",
          "Angular is an opinionated full framework (DI, router, forms, HttpClient, TypeScript-first); React is a view library you assemble a stack around",
          "They are identical",
          "Angular is only for enterprise"],
      c: 1,
      why: "Then the useful follow-up you can offer: Angular's structure suits large teams and long-lived line-of-business apps, which is why Italian gestionale shops use it more than the job-board noise suggests." },

    { q: "What does `strict: true` in tsconfig turn on that matters most?",
      a: ["Faster builds",
          "strictNullChecks — null and undefined stop being assignable to every type, which is the same shift as nullable reference types in C#",
          "Minification",
          "JSX support"],
      c: 1,
      why: "Without it, TypeScript's guarantees are much weaker than they appear. Turning it on in an existing codebase is a real, gradual piece of work." },

    { q: "Select every reason a .NET developer should still learn some front-end.",
      a: ["Adverts ask for HTML/JS/TS as a completamento",
          "You will be asked to debug why a request from the browser fails",
          "Blazor and Razor still render into a DOM you must be able to reason about",
          "Because .NET is being replaced by JavaScript"],
      c: [0, 1, 2],
      why: "The point is competence at the boundary, not a career change. Most .NET jobs in this region want you able to read a component, not to own the design system." },

    { q: "A SPA calls your API and the request works in Postman but fails in the browser with no response. First thing to check?",
      a: ["The database",
          "The browser console and network tab for a CORS preflight (OPTIONS) failure — Postman does not enforce CORS",
          "The server's disk space",
          "The JSON serialiser"],
      c: 1,
      why: "“Works in Postman, fails in browser” is a fingerprint. The preflight is a separate OPTIONS request that never reaches your endpoint if CORS is unconfigured." },
  ],

  /* ----------------------------------------------------------------- 18b */
  "18b-angular": [
    { q: "What are the two halves of an Angular component?",
      a: ["A controller and a view model",
          "A TypeScript class holding state and methods, and a template that binds to them",
          "An interface and an implementation",
          "A module and a route"],
      c: 1,
      why: "Add the composition point and you have the whole answer: components nest into a tree, and a screen is the root of one." },

    { q: "`@Injectable({ providedIn: 'root' })` is the Angular equivalent of what in ASP.NET Core?",
      a: ["A controller attribute",
          "Registering the class as an application-wide singleton in the DI container",
          "A middleware registration",
          "An EF Core entity configuration"],
      c: 1,
      why: "The difference worth naming out loud is where it is written: on the class itself, rather than in a startup file." },

    { q: "In an Angular template, what is the difference between `[value]=\"x\"` and `(click)=\"f()\"`?",
      a: ["Nothing; the brackets are stylistic",
          "Square brackets bind data into the template; round brackets send events out of it into the class",
          "Square brackets are for strings, round for numbers",
          "Round brackets are the older syntax"],
      c: 1,
      why: "Which is why two-way binding is written `[(ngModel)]` — both brackets, data in and events out. The shape is sometimes called the banana in a box." },

    { q: "Your service calls `this.http.get<Order[]>(url)` and returns it, but no request is ever sent. Why?",
      a: ["The URL is wrong",
          "Nothing subscribed — an Observable is cold, so the request is not made until somebody subscribes",
          "HttpClient was not injected",
          "CORS blocked it silently"],
      c: 1,
      why: "This is the commonest “my POST never fires” bug, and it has no error message because nothing failed — nothing started." },

    { q: "Why is `Observable<T>` a poor match for `Task<T>`?",
      a: ["They are equivalent",
          "A Task is eager, emits one value and cannot be cancelled; an Observable is lazy, may emit many, and unsubscribing cancels the work",
          "Observables are synchronous",
          "Tasks cannot be awaited in TypeScript"],
      c: 1,
      why: "`IAsyncEnumerable<T>` plus a `CancellationToken` is the honest comparison, and reaching for it is what separates someone who has used RxJS from someone who has read one blog post about it." },

    { q: "What does the type argument in `http.get<Shipment[]>(url)` actually guarantee at runtime?",
      a: ["That the JSON matches Shipment",
          "Nothing — it is a compile-time assertion, and TypeScript believes whatever you declared",
          "That missing fields become null",
          "That the response is validated against the OpenAPI document"],
      c: 1,
      why: "This is the sentence the whole contract section rests on. A TypeScript interface is a promise you make to the compiler, not a check the compiler makes for you." },

    { q: "A field renders as `undefined` and the network tab shows the value is present in the response. What is the most likely cause?",
      a: ["A CORS problem",
          "A casing mismatch — ASP.NET Core serialises camelCase by default, and the hand-written interface used the C# PascalCase name",
          "The API returned 204",
          "The component forgot to subscribe"],
      c: 1,
      why: "It compiles, it ships, and nothing warns you. Generating the client from the OpenAPI document turns this exact class of bug into a build failure." },

    { q: "You rename a DTO property in C#. What breaks in a TypeScript SPA with hand-written interfaces?",
      a: ["The front-end build fails",
          "Nothing fails anywhere — a column just goes blank on a screen nobody opened",
          "The API returns 500",
          "The router stops resolving"],
      c: 1,
      why: "Silence is the danger. Generating the client in CI is what converts this from a production discovery into a red pipeline." },

    { q: "Your interface declares `dispatchedAt: Date`, but `typeof s.dispatchedAt` is `\"string\"`. Why?",
      a: ["Angular converted it",
          "JSON has no date type — a DateTime arrives as a string, and nothing deserialises it into a Date for you",
          "The API is misconfigured",
          "TypeScript coerced it"],
      c: 1,
      why: "The date pipe copes, which is why this hides for weeks; `.getFullYear()` is where it finally throws. Serving DateTimeOffset, or UTC with a trailing Z, also removes the local-timezone ambiguity." },

    { q: "Why can a C# `decimal` round-trip through a browser and come back slightly wrong?",
      a: ["The API truncates it",
          "JavaScript numbers are doubles, so a decimal becomes a binary floating-point value",
          "JSON caps precision at two places",
          "HttpClient rounds on serialisation"],
      c: 1,
      why: "Where exactness matters, send it as a string — or do not accept it back at all and have the API recompute it." },

    { q: "A table flickers and loses input focus every time the list reloads. What is missing?",
      a: ["A loading spinner",
          "A stable identity for each row — `track s.id` on the `@for`, or `trackBy` on `*ngFor`",
          "Change detection",
          "An interceptor"],
      c: 1,
      why: "Without it Angular destroys and rebuilds rows instead of moving them. It is the same failure as a missing `@key` in Blazor, or a list index used as a React key." },

    { q: "Why should a screen distinguish “loading”, “empty” and “error” rather than just binding to the array?",
      a: ["It is a style preference",
          "An array starts empty, so a plain binding tells the user “no results” while the request is still running — and forever if it fails",
          "Angular requires it",
          "It improves change-detection performance"],
      c: 1,
      why: "Same rule as chapter 19: null means “not loaded”, an empty list means “none”, and rendering them identically is lying to the user." },

    { q: "Where should a subscription made in a component be cleaned up, and why?",
      a: ["Nowhere; Angular handles it",
          "On destruction — via the async pipe or `takeUntilDestroyed()` — because an unsubscribed stream keeps running after the component is gone",
          "In ngOnInit",
          "In the service that created it"],
      c: 1,
      why: "This is the commonest memory leak in Angular, and the async pipe avoids it by subscribing and unsubscribing on your behalf." },

    { q: "Why should a service return the Observable rather than subscribing inside itself?",
      a: ["It is shorter",
          "The caller owns cancellation and error display; a service that subscribes internally can do neither",
          "Subscribing in a service is a syntax error",
          "It avoids CORS"],
      c: 1,
      why: "The same instinct as not catching an exception you cannot handle: pass it to the layer that knows what to do about it." },

    { q: "In development, an Angular app on :4200 calling an API on :5001 is blocked by CORS. What is the right fix?",
      a: ["Add AllowAnyOrigin() to Program.cs",
          "Proxy /api through the Angular dev server so the browser sees one origin",
          "Disable the browser's security flags",
          "Move the API into the Angular project"],
      c: 1,
      why: "In production you then allow the exact origin the app is served from, and nothing wider. AllowAnyOrigin is the answer that makes the message go away and the reviewer wince." },

    { q: "What is CORS actually protecting?",
      a: ["Your server, from unauthorised callers",
          "Nothing on your server — it is a browser rule about who may read a response",
          "The database connection",
          "The JWT in localStorage"],
      c: 1,
      why: "Your API is still reachable by anything that is not a browser, which is exactly why authorisation lives in the API and never in the front end." },

    { q: "Refreshing the browser on `/orders/42` returns a 404, although clicking through to it works. Why?",
      a: ["The route is misspelled",
          "The web server was asked for a document that does not exist — the host must fall back to index.html and let the client router resolve the path",
          "The API is down",
          "A guard rejected the route"],
      c: 1,
      why: "“It works until you press F5” is always this. It is a hosting configuration, not an Angular bug." },

    { q: "What is an Angular route guard the rough equivalent of?",
      a: ["A middleware",
          "The Authorize attribute — it decides whether a route may be entered",
          "A model binder",
          "An exception filter"],
      c: 1,
      why: "And like everything client-side, it is cosmetic: it decides what to show, while the API decides what is allowed." },

    { q: "What does an HTTP interceptor do, in terms a .NET developer already has?",
      a: ["It caches responses",
          "It is middleware facing the other way — every outgoing request passes through it, which is where the bearer token and correlation id are attached",
          "It replaces the router",
          "It validates forms"],
      c: 1,
      why: "It is also where a 401 triggers a token refresh, in one place instead of in every service." },

    { q: "Why do enterprise Angular projects prefer reactive forms over template-driven ones?",
      a: ["They are faster",
          "The form is declared in the class as an object, so it can be composed, validated and unit-tested without the DOM",
          "Template-driven forms are deprecated",
          "They do not require validators"],
      c: 1,
      why: "Neither kind removes the API's duty to validate again: the endpoint is reachable without the form, which is the point an interviewer is usually listening for." },

    { q: "You open an Angular codebase and every component is listed inside an `@NgModule`. What does that tell you?",
      a: ["It is broken",
          "It predates standalone components — the older organising unit, which you will meet in any project started before Angular 15",
          "It is a React project",
          "The build is misconfigured"],
      c: 1,
      why: "Recognising both words is the useful part. Standalone is the modern default; NgModule is what most existing line-of-business code still looks like." },

    { q: "What is the honest claim to make about Angular in a .NET interview?",
      a: ["That you are a full-stack front-end developer",
          "That you have built and consumed your own API from an Angular front end and know where the contract breaks — but are not a front-end specialist",
          "That you prefer React",
          "That the front end is not your job"],
      c: 1,
      why: "An advert listing Angular as a plus wants somebody who will not be blocked by the other half of the repository, not a second specialist. Both the overclaim and the shrug are worse answers." },
  ],

  /* ----------------------------------------------------------------- 18c */
  "18c-react": [
    { q: "What is a React component?",
      a: ["A class that inherits from Component and holds a template file",
          "A function that takes props and returns the elements to draw",
          "A decorated class with a selector",
          "An HTML file with data attributes"],
      c: 1,
      why: "Classes existed and still work, but every new codebase writes functions. The follow-up sentence worth having ready is that the function must be pure, which is why effects exist at all." },

    { q: "Why must a render be pure — same output for the same props and state, with no side effects?",
      a: ["Because JavaScript has no classes",
          "Because React may call it more times than there were user actions, so anything it does to the outside world happens an unpredictable number of times",
          "Because it runs on the server",
          "Because TypeScript requires it"],
      c: 1,
      why: "This one sentence explains StrictMode's double render, why fetching inside the render body is wrong, and why effects are a separate concept." },

    { q: "What does `useState` actually return?",
      a: ["A reference you can assign to",
          "The current value and a setter that schedules another render",
          "An observable",
          "A getter and a setter function pair that both mutate immediately"],
      c: 1,
      why: "Calling the setter does not change the variable you are holding. The next render receives the new value — which is why reading it on the line after setting it shows the old one." },

    { q: "What is `useEffect`'s dependency array for?",
      a: ["Performance tuning only",
          "Declaring which values the effect reads, so React can re-run it when one of them changes",
          "Listing the imports the effect needs",
          "Telling React which DOM nodes to update"],
      c: 1,
      why: "It is a specification, not an optimisation. Treating it as a knob to make re-renders stop is how the stale closure gets bought on purpose." },

    { q: "You leave a value out of the dependency array. What happens?",
      a: ["React throws at runtime",
          "The effect keeps reading the copy it captured on the render that created it — a stale closure, with no error anywhere",
          "The effect never runs",
          "The component stops rendering"],
      c: 1,
      why: "The symptom is a timer, socket handler or subscription still using a filter or token that changed ten minutes ago. Silence is what makes it expensive." },

    { q: "What does the function returned from `useEffect` do?",
      a: ["Nothing; it is a convention",
          "It is the cleanup: React runs it before re-running the effect and when the component is removed",
          "It sets the component's return value",
          "It runs before the effect, as a guard"],
      c: 1,
      why: "Cancelling a request, clearing a timer and closing a connection all live there. Without it, a component that unmounts leaves its work running." },

    { q: "Why is the array index a bad `key` for a list that can change?",
      a: ["It is slower to compare",
          "A key is an identity, not a position: delete or reorder a row and every index below it names a different item, so React reuses the wrong DOM nodes",
          "React forbids numbers as keys",
          "It breaks TypeScript inference"],
      c: 1,
      why: "The visible damage is focus jumping, ticked boxes moving line, and typed input attaching to a neighbouring row. Same family as a missing `track` in Angular." },

    { q: "A component starts with `useState([])` for data it is about to fetch. What does the user see?",
      a: ["A loading spinner",
          "The empty-list message — “no shipments yet” — for a customer who has forty, and forever if the request fails",
          "Nothing at all",
          "The previous customer's rows"],
      c: 1,
      why: "Loading, error and empty are three states and must look different. Starting from `null` makes “not loaded” distinguishable from “none”." },

    { q: "You switch customers quickly and the table shows the wrong one. What happened?",
      a: ["The server cached the wrong response",
          "Two requests were in flight and the slower one landed last, overwriting the newer answer",
          "React reordered the rows",
          "The key was wrong"],
      c: 1,
      why: "The fix is cancellation in the effect's cleanup, or a query cache keyed by customer id so the two answers can never write to the same place." },

    { q: "What is `AbortController` used for in a React data-fetching effect?",
      a: ["Retrying the request",
          "Cancelling the in-flight request from the effect's cleanup, so a stale response cannot overwrite a newer one",
          "Debouncing the input",
          "Aborting the render"],
      c: 1,
      why: "It is passed as `signal` to fetch. A query library creates and cancels one for you, and forgetting to pass it through silently gives the cancellation away." },

    { q: "Why does a POST fire twice in development and once in production?",
      a: ["A bug in React's scheduler",
          "StrictMode mounts components and runs effects twice on purpose, to surface effects that are not safe to re-run",
          "The dev server retries failed requests",
          "Hot reload replayed it"],
      c: 1,
      why: "It is a diagnosis, not a defect. A POST usually belongs in an event handler or a mutation rather than an effect; the wrong fix is deleting StrictMode." },

    { q: "Two sibling components must agree on the selected row. Where does that state go?",
      a: ["In a global store, always",
          "In their nearest common ancestor, passed down as props — lifting state up",
          "Duplicated in both, kept in step by effects",
          "In the URL"],
      c: 1,
      why: "The duplicated version is the bug this rule exists to prevent: one value in three places, two of them wrong." },

    { q: "You need a filtered view of a list already in state. What is the right move?",
      a: ["A second useState kept in step by an effect",
          "Derive it during the render, and reach for useMemo only if a profiler says the filtering is expensive",
          "Store it in Context",
          "Refetch from the server with the filter applied"],
      c: 1,
      why: "Never store what you can compute. Two sources of truth with one update path guarantees a render where they disagree." },

    { q: "Why is server data usually not held in `useState`?",
      a: ["useState cannot hold arrays",
          "It is a cache of something the API owns, so it needs staleness, refetching, deduplication and cancellation — which is what a query library provides",
          "It is too large",
          "React forbids it"],
      c: 1,
      why: "Saying “server state and client state are different problems” is the sentence that shows you have maintained a React app rather than built one." },

    { q: "In TanStack Query, what does the `queryKey` do?",
      a: ["Names the component",
          "Identifies the cache entry, so a different key is different data with its own loading state — it is the dependency array and the cache key at once",
          "Sets the retry count",
          "Chooses the HTTP verb"],
      c: 1,
      why: "It is why the race in the hand-written version cannot happen: two customers are two entries, and neither can overwrite the other." },

    { q: "What is `staleTime`?",
      a: ["How long before the cache entry is deleted",
          "How long a cached answer is served without going back to the network",
          "The request timeout",
          "How long a mutation may take"],
      c: 1,
      why: "It is a TTL one tier out from Redis, and everything chapter 20 says about invalidation applies unchanged." },

    { q: "You create a shipment and the table does not update. What is missing?",
      a: ["A page reload",
          "Invalidating the list query after the mutation succeeds, so it refetches",
          "A key on the table rows",
          "A second useEffect"],
      c: 1,
      why: "If the team's answer to this has always been “press F5”, the codebase never answered it. `invalidateQueries` is the answer." },

    { q: "`fetch` receives a 500 from your API. What does it do?",
      a: ["Throws, like HttpClient with EnsureSuccessStatusCode",
          "Resolves normally — it rejects only when the network itself failed, so you must check `response.ok` yourself",
          "Retries automatically",
          "Returns null"],
      c: 1,
      why: "A wrapper that skips that check will happily parse an error page as your DTO. Every codebase has such a wrapper; find it before you trust it." },

    { q: "When is React's Context the wrong tool?",
      a: ["For the signed-in user",
          "For a fast-changing value, because every consumer re-renders when the value changes",
          "For the theme",
          "For a feature flag"],
      c: 1,
      why: "Context solves prop drilling, not performance. Putting a value that changes many times a second in a high-up provider re-renders half the application." },

    { q: "An interviewer asks when you reach for `useMemo` and `React.memo`. What is the honest answer?",
      a: ["On every component, as a habit",
          "When a profiler says the work is expensive — and the follow-up is which profiler, so do not claim it if you have not done it",
          "Never; they are deprecated",
          "Only on lists"],
      c: 1,
      why: "Both are cheap to name and easy to be caught on. Premature memoisation also has a cost: more allocations and dependency arrays that are now load-bearing." },

    { q: "What should you say about Next.js if you have used React but not Next?",
      a: ["Claim it; it is just React",
          "Say you have not used it, and that you know it does server rendering and brings its own routing",
          "Avoid the topic",
          "Say React and Next are unrelated"],
      c: 1,
      why: "Server components and the rendering modes are a separate subject with their own failure modes. The short honest answer costs nothing and survives the follow-up." },
  ],

  /* ------------------------------------------------------------------ 19 */
  "19-blazor": [
    { q: "In Blazor Server (InteractiveServer), where does the component code run?",
      a: ["In the browser as WebAssembly",
          "On the server; the browser holds a SignalR circuit and receives DOM diffs over it",
          "In a service worker",
          "Half in each"],
      c: 1,
      why: "That is why latency to the server is felt on every keystroke-driven update, why the server holds per-user state in memory, and why a dropped connection needs a reconnect strategy." },

    { q: "Blazor WebAssembly's main practical costs compared with Blazor Server?",
      a: ["None",
          "A larger initial download and the .NET runtime running in the browser — but it works offline and needs no persistent connection",
          "It cannot use C#",
          "It requires Windows"],
      c: 1,
      why: "The other side: WASM has no server-side secrets, so anything the client calls must be an authenticated API. Server can talk to the database directly; WASM must not." },

    { q: "What does `@bind` do on an input?",
      a: ["One-way display only",
          "Two-way binding — sets the value attribute AND wires an event handler that writes back to the field",
          "Nothing at runtime",
          "It calls the API"],
      c: 1,
      why: "The afternoon-costing detail: the default event is onchange, not oninput. `@bind:event=\"oninput\"` is what you want for live filtering." },

    { q: "Why does a Blazor list of components sometimes reuse state on the wrong item after reordering?",
      a: ["A garbage-collection bug",
          "The diffing algorithm needs @key to identify each item; without it, it matches by position",
          "The component is not async",
          "The list is not sorted"],
      c: 1,
      why: "Exactly the same problem and the same fix as React's `key`. It shows up as a checked checkbox following the wrong row." },

    { q: "A component's data does not refresh after an event raised outside Blazor's own event handling. What is missing?",
      a: ["A page reload",
          "StateHasChanged() — and on a non-UI thread, InvokeAsync(StateHasChanged)",
          "A new @page directive",
          "Dependency injection"],
      c: 1,
      why: "Blazor re-renders automatically after its own handlers. A timer callback, a SignalR message or a background service completing is outside that, and must say so." },

    { q: "For a WinForms shop moving to the web, why is Blazor often the honest recommendation?",
      a: ["It is the fastest framework",
          "The team keeps C# and can reuse validation and domain code, and BlazorWebView lets new screens be hosted inside the existing desktop shell during migration",
          "It requires no HTML",
          "It is the only Microsoft option"],
      c: 1,
      why: "That last point is the strangler pattern in practice, and it is the answer that matters most in Emilia-Romagna's manufacturing IT market." },

    { q: "Select every place a Blazor component's lifecycle can bite you.",
      a: ["OnInitializedAsync runs twice with prerendering enabled",
          "Dispose is needed to unsubscribe from events or timers",
          "Parameters can be set again at any time, re-running OnParametersSet",
          "Components are singletons per application"],
      c: [0, 1, 2],
      why: "Components are per-instance, not singletons. The double-execution under prerendering catches almost everyone once — usually as a duplicated API call." },

    { q: "What is a `RenderFragment`?",
      a: ["A partial class",
          "A delegate that writes markup — the mechanism behind ChildContent and templated components",
          "A JavaScript interop handle",
          "A CSS isolation file"],
      c: 1,
      why: "It is the Blazor answer to “how do I pass markup into a component?”, and knowing it is a delegate connects it straight back to the delegates chapter." },
  ],

  /* ----------------------------------------------------------------- 19b */
  "19b-realtime": [
    { q: "Why can a plain HTTP API not push to a browser?",
      a: ["Because JSON is not streamable",
          "Because HTTP is request/response: the connection is opened by the client, one answer comes back, and nothing in that shape lets the server speak first",
          "Because browsers block it",
          "Because of CORS"],
      c: 1,
      why: "Everything in this chapter is a way around that one sentence — asking repeatedly, keeping the response open, or upgrading the connection." },

    { q: "What are the two real costs of polling?",
      a: ["Bandwidth and battery only",
          "Latency averaging half the interval, and a request count of users times frequency whether or not anything changed",
          "Server memory and CPU",
          "It cannot work through a proxy"],
      c: 1,
      why: "Say the arithmetic, then say when it is still right: low frequency and few users, where it costs nothing to operate and survives proxies and offline gaps for free." },

    { q: "How does a WebSocket connection start?",
      a: ["On its own port with its own protocol",
          "As an ordinary HTTP GET carrying an Upgrade header, answered with 101 Switching Protocols, after which the same TCP connection is no longer HTTP",
          "With a TLS handshake only",
          "With a POST carrying a session token"],
      c: 1,
      why: "The upgrade is why proxies and load balancers have to be configured for it — they see the start of an HTTP request and then traffic they do not understand." },

    { q: "What is Server-Sent Events good for?",
      a: ["Two-way chat",
          "One-directional streaming to the browser over a single HTTP response, with reconnection built in",
          "Binary transfer",
          "Replacing a message broker"],
      c: 1,
      why: "When the answer to “does the client need to send too?” is no, SSE is simpler than a WebSocket and goes through more proxies. It deserves the shortlist more often than it gets it." },

    { q: "What is SignalR?",
      a: ["A fourth transport protocol",
          "A library over WebSocket, SSE and long polling that adds negotiation and fallback, a method-call protocol both ways, groups, reconnect and a backplane",
          "A message broker",
          "A replacement for HTTP"],
      c: 1,
      why: "On a healthy network it is a WebSocket underneath. Saying so shows you know the layers rather than the marketing." },

    { q: "Why take `Hub<TClient>` rather than plain `Hub`?",
      a: ["It is faster",
          "The client methods become a compiled interface, so renaming one breaks the build instead of the screen",
          "It enables groups",
          "It is required for authentication"],
      c: 1,
      why: "Without it, the method name is a string nothing checks. This is the same argument as typed clients over stringly-typed HTTP calls." },

    { q: "A signed-in user invokes `WatchZone(\"zone:99\")` from the browser console for a zone that is not theirs. What stops them?",
      a: ["The Authorize attribute on the hub",
          "Nothing, unless the hub method itself checks that this user may watch that zone",
          "SignalR validates group names",
          "The backplane rejects it"],
      c: 1,
      why: "A hub method is a public endpoint taking arguments the caller chose. `[Authorize]` proves who they are; it says nothing about which zone." },

    { q: "What is a SignalR group?",
      a: ["A set of users",
          "A named set of connection ids, created on first use, that disappears when the last member leaves",
          "A permission role",
          "A partition of the backplane"],
      c: 1,
      why: "It holds connections, not users — one person with two tabs is two connections. `Clients.User(id)` is the one that reaches every connection a user has." },

    { q: "What is wrong with storing a connection id in your database as the way to reach a user later?",
      a: ["It is too long",
          "It changes on every reconnect, so the stored one stops naming anything the first time the laptop sleeps",
          "It is not unique",
          "It cannot be indexed"],
      c: 1,
      why: "`Context.UserIdentifier` is the stable identity. This is the bug behind “notifications stop working after a while” in most first implementations." },

    { q: "How does code that is not a hub method — a background worker, a broker consumer — push to clients?",
      a: ["By creating a new instance of the hub",
          "By injecting IHubContext for that hub",
          "By calling the hub over HTTP",
          "By writing to the backplane directly"],
      c: 1,
      why: "Hub instances are created per call and disposed immediately, so holding one is always a bug. Most sends in a real system come from outside the hub." },

    { q: "You scale the API to three instances and some users stop receiving updates. Why?",
      a: ["The load balancer is dropping frames",
          "Connections live in the memory of the instance that accepted them, so a message published on one instance never reaches clients attached to the others",
          "SignalR limits concurrent connections",
          "The JWT is not shared"],
      c: 1,
      why: "There is no error and no log — half the users are silently stale. It is the defining real-time production bug and it cannot happen on one machine." },

    { q: "What does a backplane such as Redis or Azure SignalR Service do?",
      a: ["Stores messages durably for later replay",
          "Carries messages between instances so each one forwards them to its own connected clients",
          "Balances connections across instances",
          "Caches the negotiate response"],
      c: 1,
      why: "It is fan-out between servers, not durability. A message published while you were disconnected is still gone." },

    { q: "Why are sticky sessions usually needed even with a backplane?",
      a: ["To keep the cache warm",
          "Because the negotiate handshake and the connection that follows it must land on the same instance, unless the client is pinned to WebSockets and skips negotiation",
          "Because Redis is single-threaded",
          "Because JWTs are per-instance"],
      c: 1,
      why: "Two separate concerns that get conflated: the backplane is about reaching clients, stickiness is about establishing the connection in the first place." },

    { q: "A client reconnects after two minutes offline. What happened to the messages sent meanwhile?",
      a: ["They were queued and are delivered on reconnect",
          "They are gone — a hub is not durable and nothing buffered them",
          "They are in the backplane",
          "SignalR replays the last hundred"],
      c: 1,
      why: "So the rule is: on reconnect, re-read state; never ask for a replay. Send a snapshot with a version and let the client diff." },

    { q: "After an automatic reconnect, what has silently changed?",
      a: ["The user's claims",
          "The connection id is new and the connection is in no groups, so group membership must be re-established",
          "The transport is downgraded",
          "The access token is reused unchanged"],
      c: 1,
      why: "This is the bug where a screen shows green, the truck went through a tunnel, and nothing has arrived since. Re-invoke the subscribe method in `onreconnected`." },

    { q: "Why is `accessTokenFactory` given a function rather than a token string?",
      a: ["To avoid storing the token",
          "Because it is called again on every reconnect, so a refreshed token is used",
          "Because the token must be encrypted",
          "Because SignalR needs it asynchronously"],
      c: 1,
      why: "Capturing a string at mount is how a long-lived screen quietly stops reconnecting after an hour — a failure nobody reproduces in a morning's testing." },

    { q: "Where does the token travel for a browser WebSocket, and what is the consequence?",
      a: ["In an Authorization header, as usual",
          "In the query string as `access_token`, so it lands in access and proxy logs and must be short-lived and scrubbed",
          "In a cookie, which is immune to this",
          "In the first frame after connect"],
      c: 1,
      why: "The browser WebSocket API cannot set headers. Knowing why the query string is used, and what it costs, is the whole of this question." },

    { q: "A user's access token expires while the hub connection is open. What happens?",
      a: ["The connection is closed immediately",
          "Nothing — authorisation was evaluated when the connection was established, so a long-lived screen must re-establish or re-check on the operations that matter",
          "SignalR refreshes it automatically",
          "Every subsequent message is rejected"],
      c: 1,
      why: "This surprises people, and it is a genuine security question for an operations screen left open all day." },

    { q: "Two hundred vehicles each send a position per second to forty watching operators. What is the fix?",
      a: ["Increase the client buffer",
          "Coalesce server-side to the latest position per vehicle and send one batch per second per zone, scoping groups so each operator gets only their zone",
          "Switch to long polling",
          "Ask the browser to throttle"],
      c: 1,
      why: "A client cannot apply back-pressure, so the answer is to not send it. Throttling in the browser only moves the queue to the place with the least memory." },

    { q: "The connection drops every sixty seconds, exactly. Where do you look first?",
      a: ["Your hub code",
          "An idle timeout in a load balancer, reverse proxy or gateway between the browser and the process",
          "The database connection pool",
          "The client's dependency array"],
      c: 1,
      why: "An interval that regular is never application code. Raise that timeout, or lower SignalR's keep-alive beneath it." },

    { q: "An event must not be lost — a dispatch assignment. Where does it belong?",
      a: ["On the hub, with a higher priority",
          "In a durable store or a broker, with the socket used only as the nudge that says something new is waiting",
          "In the backplane",
          "In the client's local storage"],
      c: 1,
      why: "The socket is the doorbell, never the parcel. That sentence keeps the hub and the broker in their right places." },
  ],

  /* ------------------------------------------------------------------ 20 */
  "20-redis-caching": [
    { q: "What problem does a cache actually solve?",
      a: ["Storing data permanently",
          "Not paying repeatedly for an answer that is expensive to compute and does not change often",
          "Replacing the database",
          "Encrypting data in transit"],
      c: 1,
      why: "Two words define the trade: staleness for speed. If you cannot say how stale the data may be, you cannot decide the TTL." },

    { q: "IMemoryCache versus IDistributedCache — when does the difference bite?",
      a: ["Never",
          "The moment there is more than one instance: in-memory caches diverge per process, so users see different answers depending on which instance served them",
          "Only in development",
          "When the data is large"],
      c: 1,
      why: "Also invalidation: clearing an in-memory cache clears one process's copy. Redis exists precisely so the cache is one shared thing." },

    { q: "Describe cache-aside.",
      a: ["Write to the cache first, then the database",
          "Read: try the cache, on a miss load from the source, store it, return it. Write: update the source and invalidate the key",
          "The cache updates itself on a schedule",
          "The database pushes changes to the cache"],
      c: 1,
      why: "Invalidate rather than update on write. Updating creates a race where two writers leave a stale value; deleting means the next reader reloads the truth." },

    { q: "What is a cache stampede?",
      a: ["The cache running out of memory",
          "A hot key expiring and hundreds of concurrent requests all missing at once and all hitting the database together",
          "Too many keys",
          "A network partition"],
      c: 1,
      why: "Mitigations worth naming: jittered expiry so keys do not expire in lockstep, and a per-key lock so only one caller recomputes." },

    { q: "What should NEVER go in a shared cache?",
      a: ["Product catalogues",
          "Anything whose staleness is unsafe — account balances at the moment of payment, stock levels at the moment of allocation, permission checks",
          "Reference data",
          "Rendered HTML fragments"],
      c: 1,
      why: "The other never: caching a per-user response under a key that is not scoped to the user. That is how one customer sees another's data, and it is a real incident class." },

    { q: "Redis is single-threaded for command execution. What follows from that?",
      a: ["It is slow",
          "Individual commands are atomic, so INCR and SETNX are safe without a lock — but one slow command (KEYS *) blocks everyone",
          "It cannot handle concurrency",
          "It needs a lock for every write"],
      c: 1,
      why: "KEYS in production is the classic outage. SCAN is the cursor-based replacement that does not block the server." },

    { q: "Select every sensible use for Redis beyond caching query results.",
      a: ["Distributed lock (with care — see Redlock's caveats)",
          "Rate-limiting counters",
          "ASP.NET Core distributed session or data-protection key ring",
          "The system of record for orders"],
      c: [0, 1, 2],
      why: "The last one is the line: Redis persistence exists but a cache is not a durable store, and treating it as one is how data disappears after a failover." },

    { q: "You set a TTL of 24 hours on a product's price. The price changes. What is the user-visible bug?",
      a: ["None",
          "Up to 24 hours of the wrong price being shown — and possibly charged, if the checkout reads the same cache",
          "The cache crashes",
          "The database rejects the update"],
      c: 1,
      why: "TTL is not invalidation; it is a bound on how wrong you are willing to be. Price display can tolerate it. The amount actually charged cannot, and must be read from the source." },
  ],

  /* ------------------------------------------------------------------ 21 */
  "21-search-elastic": [
    { q: "Why not just use `LIKE '%term%'` in SQL Server?",
      a: ["It is not supported",
          "A leading wildcard cannot use an index, so it scans; and it matches substrings, not words — no stemming, no relevance ranking, no typo tolerance",
          "It only works on short strings",
          "It is deprecated"],
      c: 1,
      why: "SQL Server full-text search closes part of the gap and is worth mentioning. Elasticsearch is what you reach for when relevance scoring and analysis pipelines are the requirement." },

    { q: "What is an inverted index?",
      a: ["An index sorted in reverse",
          "A map from each term to the list of documents containing it — the reason word search is fast",
          "A backup of the index",
          "An index on a computed column"],
      c: 1,
      why: "It also explains analysis: the terms in that map are the output of tokenising, lower-casing and stemming, which is why searching “running” can match “run”." },

    { q: "In Elasticsearch, what is the practical difference between a `text` and a `keyword` field?",
      a: ["None",
          "text is analysed into terms for full-text matching; keyword is stored whole, for exact filters, sorting and aggregations",
          "keyword is for numbers",
          "text cannot be searched"],
      c: 1,
      why: "The everyday bug: sorting or aggregating on a `text` field fails or gives nonsense. The convention is a multi-field — `name` as text plus `name.keyword`." },

    { q: "What is the difference between a `query` context and a `filter` context?",
      a: ["Filters are slower",
          "A query computes a relevance score; a filter answers yes/no, is cacheable, and does not affect ranking",
          "Filters cannot be combined",
          "Queries cannot use ranges"],
      c: 1,
      why: "Put “must match these words” in must, and “status = active, date in range” in filter. The scoring work you skip is free performance." },

    { q: "Keeping the search index in sync with SQL Server — what is the honest hard part?",
      a: ["The network",
          "Dual writes: the database commit and the index update can succeed independently, so a failure between them leaves the index wrong with nothing to notice",
          "The JSON format",
          "Index size"],
      c: 1,
      why: "The standard answers are the outbox pattern (write an event in the same transaction, publish it later) and a periodic reconciliation job. Saying “I would just update both” invites exactly this follow-up." },

    { q: "Elasticsearch is “near real-time”. What does that mean concretely?",
      a: ["Results are always instant",
          "A newly indexed document is not searchable until the next refresh — about one second by default",
          "It loses documents",
          "Queries are queued"],
      c: 1,
      why: "Which is why the write-then-immediately-search test fails intermittently. Forcing a refresh works in tests and is a bad idea in production." },

    { q: "Select every statement that is true about Elasticsearch and OpenSearch.",
      a: ["OpenSearch is an AWS-led fork of Elasticsearch 7.10",
          "The fork happened over a licence change",
          "The query DSL is largely compatible between them",
          "They share the same official .NET client"],
      c: [0, 1, 2],
      why: "The clients diverged: Elastic.Clients.Elasticsearch versus OpenSearch.Client. Adverts naming both usually mean “either” rather than both at once." },

    { q: "What does a `should` clause do inside a bool query with no `must`?",
      a: ["It is ignored",
          "At least one should-clause must match (minimum_should_match defaults to 1 when there is no must), and matches boost the score",
          "It always matches",
          "It behaves like must_not"],
      c: 1,
      why: "The default flips when a `must` is present: then `should` becomes purely a scoring boost and matches nothing extra. That surprise is worth remembering." },
  ],

  /* ------------------------------------------------------------------ 22 */
  "22-messaging-and-queues": [
    { q: "Why send a message instead of making a direct HTTP call to another service?",
      a: ["It is always faster",
          "It decouples availability: the sender succeeds even when the receiver is down, and load spikes are absorbed by the queue instead of by the receiver",
          "It avoids serialisation",
          "HTTP cannot cross networks"],
      c: 1,
      why: "The cost is real and worth conceding: you lose the immediate answer and gain eventual consistency, which the UI and the business rules now have to tolerate." },

    { q: "Queue versus topic?",
      a: ["Same thing",
          "A queue delivers each message to exactly one consumer (competing consumers); a topic fans the same message out to every subscriber",
          "A topic is a queue with a name",
          "Queues cannot scale"],
      c: 1,
      why: "“Send a command to a queue, publish an event to a topic” is the sentence. Commands have one owner; events have any number of interested parties." },

    { q: "Most brokers give you at-least-once delivery. What does that force on the consumer?",
      a: ["Nothing",
          "Idempotency — processing the same message twice must not double the effect, usually via a processed-message id table or a natural upsert",
          "A retry loop",
          "A larger queue"],
      c: 1,
      why: "Exactly-once delivery is essentially unavailable across a network; exactly-once *processing* is achieved by at-least-once delivery plus an idempotent handler. That distinction is the whole answer." },

    { q: "What bug does the transactional outbox pattern prevent?",
      a: ["Duplicate messages",
          "The database transaction committing while the message publish fails (or the reverse), leaving the two out of step with nobody aware",
          "Slow consumers",
          "Message ordering problems"],
      c: 1,
      why: "The message is written to an outbox TABLE in the same transaction as the business data, so it is atomic. A separate publisher then reads and sends it, which is where at-least-once comes from." },

    { q: "A message keeps failing and being redelivered forever. What is it, and what is the mechanism that stops it?",
      a: ["A memory leak; restart the consumer",
          "A poison message; a delivery-count limit moves it to a dead-letter queue after N attempts",
          "A deadlock; increase the timeout",
          "A network partition; nothing can help"],
      c: 1,
      why: "The dead-letter queue must be monitored, or it is a silent bin. “We have a DLQ” and “nobody has looked at it since March” usually go together." },

    { q: "RabbitMQ, Kafka and Azure Service Bus — which one is fundamentally a different kind of thing?",
      a: ["RabbitMQ",
          "Kafka — it is a durable, replayable, partitioned log, not a queue that deletes what has been consumed",
          "Azure Service Bus",
          "They are all equivalent"],
      c: 1,
      why: "Consequences: Kafka gives ordering per partition and lets a new consumer replay history; RabbitMQ and Service Bus give richer per-message semantics like scheduled delivery and per-message dead-lettering." },

    { q: "Select everything you should be able to say about MassTransit in 2026.",
      a: ["It is a popular .NET abstraction over brokers",
          "It moved to a commercial licence, and v8 reaches end of life at the end of 2026",
          "The same licensing story happened to MediatR",
          "It is part of the .NET SDK"],
      c: [0, 1, 2],
      why: "This is why a portfolio repository hand-writing its own dispatcher is defensible rather than naive — and it is a genuinely current thing to have an opinion about." },

    { q: "You need strict ordering of messages for one customer. What actually gives it to you?",
      a: ["A single queue with many consumers",
          "Partitioning or sessions keyed by customer id, so all of that customer's messages go to one consumer in order",
          "Sorting the messages on arrival",
          "Increasing the prefetch count"],
      c: 1,
      why: "Competing consumers destroy ordering by design. Kafka partition keys and Service Bus sessions are the same idea: order within a key, parallelism across keys." },
  ],

  /* ----------------------------------------------------------------- 22b */
  "22b-microservices": [
    { q: "What is the primary benefit microservices actually buy?",
      a: ["Better performance",
          "Independent deployment for independent teams — the benefits are organisational, the costs are technical",
          "Cleaner code",
          "Cheaper infrastructure"],
      c: 1,
      why: "That is why they pay off at fifteen teams and hurt at two: with one team you have bought every cost — network failure, no transactions, no joins, an operational tax — and gained nothing." },

    { q: "A team's code is tangled and hard to change, and they have four developers. What should they do first?",
      a: ["Split into microservices to force boundaries",
          "Build a modular monolith: enforced internal boundaries, one deployable, one transaction",
          "Rewrite from scratch",
          "Add more integration tests"],
      c: 1,
      why: "It fixes the actual problem — tangled code — without the network in between, and the seams it creates are exactly what makes a later split possible if teams ever need it." },

    { q: "Which is the right basis for a service boundary?",
      a: ["One service per database table",
          "A business capability — a bounded context such as Ordering, Shipping or Invoicing",
          "One service per technical layer: API, business logic, data",
          "Whatever the current team knows best"],
      c: 1,
      why: "The practical signal is where the vocabulary changes: if “order” means a picking list to the warehouse and an invoiceable event to finance, that is a boundary." },

    { q: "Shipping needs the customer's address, which Ordering owns. What is the correct arrangement?",
      a: ["Shipping queries Ordering's database directly",
          "Shipping keeps its own copy of the address it was told to deliver to — duplication across a boundary is correct, not a normalisation failure",
          "The address is moved to a shared database both services use",
          "The two services are merged"],
      c: 1,
      why: "It is not a cache to be invalidated; it is Shipping's own record of the instruction it received. A shared database re-couples the services and removes the point of splitting." },

    { q: "Four services each with 99.9% availability are chained synchronously. What is the resulting availability?",
      a: ["Still 99.9%",
          "About 99.6% — availabilities multiply along a synchronous chain, and latencies add",
          "99.99%, because failures are independent",
          "It cannot be calculated"],
      c: 1,
      why: "Roughly three hours a month of downtime that no single team caused. It is the argument for events over synchronous calls whenever the caller does not need the answer to render the response." },

    { q: "What is a distributed monolith?",
      a: ["A monolith deployed to several servers",
          "Services split badly — usually by layer or table — so changes touch several and they must be released together, paying every cost and gaining no independent deployment",
          "A monolith with a message queue",
          "Any system with more than three services"],
      c: 1,
      why: "The test: could you deploy one of them on a Tuesday afternoon without telling anyone? If not, they are not microservices whatever the diagram says." },

    { q: "Which of these must be in place BEFORE splitting a monolith into services? (Select every one that applies.)",
      a: ["Automated deployment per service",
          "Distributed tracing with a correlation id crossing every boundary",
          "Centralised logging",
          "A service mesh"],
      c: [0, 1, 2],
      why: "A mesh is an optional later concern. Without the first three, a split makes every problem harder to diagnose — and if releasing needs a person and a checklist, ten services means ten times the ceremony." },

    { q: "What replaces a database transaction across a service boundary?",
      a: ["A distributed transaction coordinator",
          "A saga: a sequence of local transactions with compensating actions when a later step fails",
          "A longer timeout",
          "Nothing — transactions still work"],
      c: 1,
      why: "There is no rollback across services, only compensation — and compensation is a business decision, not a technical one. This is one of the real costs of splitting." },

    { q: "You are extracting the first service from a monolith. Which do you take?",
      a: ["The domain core, since everything depends on it",
          "The least-coupled context on the edge — notifications or reporting — with its own data and few callers",
          "The largest one, for the biggest win",
          "The one with the most bugs"],
      c: 1,
      why: "Same strangler reasoning as a framework migration: prove the whole path end to end on something low-risk. Starting with the core is the hardest possible first step and where a failed migration strands you." },

    { q: "A BackgroundService with a PeriodicTimer runs a nightly reconciliation. The service is scaled to three replicas. What happens?",
      a: ["The platform elects one instance automatically",
          "It runs three times, concurrently — and nobody notices until it sends three emails or double-posts a ledger entry",
          "Only the first instance to start runs it",
          "The timer is shared between instances"],
      c: 1,
      why: "The moment there is more than one instance, a scheduled job becomes a distributed-systems problem. Nothing in BackgroundService coordinates across processes." },

    { q: "What is the best first fix for that duplicate job execution?",
      a: ["Make the work idempotent — claim rows with a conditional UPDATE and act only on what you claimed",
          "Take a distributed lock in Redis",
          "Scale the service back to one instance",
          "Move the job to Hangfire"],
      c: 0,
      why: "Idempotency keeps working even when a lock expires at the wrong moment, which locks eventually do. The others are valid, but they sit on top of this rather than replacing it." },

    { q: "Why is a distributed lock with a timeout described as a lease?",
      a: ["Because it must be paid for",
          "Because it can expire while you still believe you hold it — a garbage collection pause or a slow query is enough — so two processes can both think they own it",
          "Because it is granted by a central authority",
          "Because it is only valid on one machine"],
      c: 1,
      why: "This is why the underlying work should be safe to run twice regardless. A lock reduces the frequency of the collision; it does not eliminate it." },

    { q: "When is Hangfire or Quartz.NET the right choice over a plain BackgroundService?",
      a: ["Always — BackgroundService is not production-grade",
          "When you need retry history, an admin dashboard, or cron schedules maintained by someone other than a developer",
          "Whenever the job runs more than once a day",
          "Only in microservice architectures"],
      c: 1,
      why: "Both add a dependency, a database schema and an operational surface. BackgroundService with a PeriodicTimer is the right default — this repository's outbox dispatcher is exactly that." },

    { q: "An interviewer says “we're moving to microservices”. What is the most useful response?",
      a: ["Explain the benefits of microservices",
          "Ask which bounded contexts they have identified, and what deploying one service looks like today",
          "Warn them that microservices are usually a mistake",
          "Ask which message broker they chose"],
      c: 1,
      why: "The first question reveals whether they have done the thinking; the second is the readiness question in disguise. Both teach you more about the role than any answer you could give." },
  ],

  /* ----------------------------------------------------------------- 22c */
  "22c-distributed-state": [
    { q: "What does a single-writer topology — one primary, several read replicas — buy you?",
      a: ["Higher write throughput",
          "One order of events, so conflicts cannot exist at all",
          "Zero replication lag",
          "Automatic failover"],
      c: 1,
      why: "It is the correct default and most systems never outgrow it. Every technique in this chapter is what you take on when you give that up." },

    { q: "A user saves a record and immediately sees the old value. What is happening?",
      a: ["The cache was not invalidated",
          "Their read went to a replica that is still behind the primary — replication lag, and the read-your-own-writes problem",
          "The transaction rolled back",
          "The form posted twice"],
      c: 1,
      why: "The usual fix is to route a user's reads to the primary for a few seconds after they write. Recognising it as lag rather than a bug is the point." },

    { q: "What does multi-master actually buy?",
      a: ["Faster reads",
          "A site that keeps working when the link to the others is down, and writes served close to the person making them",
          "Stronger consistency",
          "Simpler operations"],
      c: 1,
      why: "Both are worth real money, which is why the design exists. What you pay is the single order of events — and from then on conflict resolution is the design, not an edge case." },

    { q: "What does CAP actually say?",
      a: ["You can have any two of consistency, availability and partition tolerance at all times",
          "During a network partition you must choose: refuse writes on the isolated side and stay consistent, or accept them and reconcile later",
          "Distributed systems cannot be consistent",
          "Availability always beats consistency"],
      c: 1,
      why: "It says nothing about the normal day. PACELC adds that part: with no partition you are still trading latency against consistency, because agreement costs a round trip." },

    { q: "What does eventual consistency promise?",
      a: ["That reads are never stale for more than a second",
          "That if writing stops, every copy converges on the same value — and nothing about any particular moment",
          "That conflicts are resolved automatically",
          "That writes are never lost"],
      c: 1,
      why: "Designing the meantime — what each user sees, and what they are allowed to do — is the actual job. A candidate who cannot describe the gap has memorised the wrong half." },

    { q: "What is wrong with last-write-wins based on timestamps?",
      a: ["It is slow",
          "It silently discards a change the user was told had succeeded, and it depends on clocks that disagree between machines",
          "It cannot be implemented in SQL",
          "It needs a coordinator"],
      c: 1,
      why: "Clock skew makes the ordering unrecoverable: a cancel at 09:14:01 and a load at 09:14:02 on two machines do not tell you which really happened first." },

    { q: "What does a version vector give you?",
      a: ["An automatic resolution of any conflict",
          "Reliable detection of whether one change happened after another or whether they are concurrent — detection, not a decision",
          "A total order across all nodes",
          "Guaranteed delivery"],
      c: 1,
      why: "Something still has to resolve it: a domain rule, a field-level merge, or a queue a human works through. Being precise about that boundary is the answer." },

    { q: "What is the shared property of the CRDT family — a grow-only counter, an add-only set, an append-only log?",
      a: ["They are stored in memory",
          "Merging is order-independent, so two nodes can both write and converge with no decision and no loser",
          "They are immutable",
          "They require a coordinator"],
      c: 1,
      why: "The idea is available long before the library: replicate “scanned parcel X at 10:04” rather than “the count is now 37”, and the merge becomes a union." },

    { q: "What is the strongest way to handle write conflicts across sites?",
      a: ["A faster resolver",
          "Partition ownership — give each site sole authority over its own rows and replicate everything else read-only, so the conflict is structurally impossible",
          "Last write wins with synchronised clocks",
          "Locking rows globally"],
      c: 1,
      why: "Most systems described as multi-master in an advert are really this. Proposing it keeps the availability and never builds the resolver." },

    { q: "Why can a database-generated IDENTITY or SERIAL not be the id of a record created offline?",
      a: ["It is too small",
          "The number is not available without a round trip, and two disconnected devices would both claim the same next value",
          "It cannot be indexed",
          "It is not unique across tables"],
      c: 1,
      why: "Hence client-generated identifiers, decided on the device before the record has ever been near a server, and stable forever." },

    { q: "What is the cost of random GUID primary keys, and the usual mitigation?",
      a: ["They are slow to compare; use integers instead",
          "They scatter inserts across a clustered index and fragment it, so use a time-ordered variant such as UUIDv7, ULID or NEWSEQUENTIALID",
          "They are not unique enough; add a checksum",
          "They break foreign keys; use natural keys"],
      c: 1,
      why: "The follow-up worth volunteering is that the human-readable reference people read aloud is a separate display field, allocated by the owning site." },

    { q: "Why does a sync endpoint use PUT with a client-known id rather than POST to a collection?",
      a: ["POST is deprecated",
          "Because it makes the call idempotent by shape — the same request twice names the same resource",
          "Because PUT is faster",
          "Because POST cannot carry a body over a slow link"],
      c: 1,
      why: "This is the practical payoff of chapter 13's verb distinction rather than a point of etiquette. The client already knows the id; it created it." },

    { q: "What is an idempotency key, in a sync endpoint?",
      a: ["The row's primary key",
          "A change id the server records as applied, so a retry of the same change has no second effect",
          "A hash of the request body",
          "The client's session token"],
      c: 1,
      why: "It must be recorded in the same transaction as the change itself, or a crash between the two reintroduces the duplicate it exists to prevent." },

    { q: "The client sends the version it was editing and the server's row has moved on. What has the server learned?",
      a: ["That the client is out of date and should refresh",
          "That the change was authored against a world that no longer exists — this is the conflict detector",
          "That the request is a duplicate",
          "That the clocks disagree"],
      c: 1,
      why: "Optimistic concurrency, carried across the offline gap. In EF Core it is normally a rowversion column; here the client has to carry it explicitly." },

    { q: "Some conflicts have no correct automatic answer. What should the system do?",
      a: ["Pick the newer one and move on",
          "Record both versions in a queue a human can work through, and tell the client its change needs review",
          "Reject the change with a 400",
          "Retry until one side gives up"],
      c: 1,
      why: "A conflict queue is a feature, not an admission of failure — it is what a dispatcher is for. Most designs omit it and most operations teams eventually demand it." },

    { q: "After a successful sync, what happens to the client's local copy?",
      a: ["It is merged with the response",
          "It is replaced by the server's response, which is the truth on every path",
          "It is kept, since the client wrote it",
          "It is deleted and refetched later"],
      c: 1,
      why: "Anything else and the device slowly drifts. Returning the current row even on a detected replay is what makes that possible." },

    { q: "In an offline client's replay loop, why stop the whole drain on a 5xx instead of skipping to the next item?",
      a: ["To avoid rate limiting",
          "Because order is part of the meaning — continuing past a failure permanently reorders history",
          "Because the connection is closed anyway",
          "To keep the outbox small"],
      c: 1,
      why: "“Assigned to Marco” then “cancelled” is a different day from “cancelled” then “assigned to Marco”. A 4xx is different: the server will never accept it, so it goes to a dead letter." },

    { q: "What is `navigator.onLine` worth?",
      a: ["It reliably reports whether your API is reachable",
          "Only a hint that a network interface exists — hotel wi-fi behind a captive portal reports online",
          "Nothing; it is deprecated",
          "It reports connection quality"],
      c: 1,
      why: "Use it as a cheap trigger for a drain attempt, and let the actual request decide what is true." },

    { q: "What must a degraded-mode screen tell the user?",
      a: ["Nothing; it should look normal so as not to alarm them",
          "Which of live, offline with changes pending, or reconnecting it is in — and how old the data on screen is",
          "The name of the failing service",
          "The retry count"],
      c: 1,
      why: "An operator who cannot tell a frozen map from a live one dispatches a van that was reassigned twenty minutes ago, and the software is blamed for it." },

    { q: "Which actions should be disabled offline rather than queued?",
      a: ["All of them",
          "The ones with no safe reconciliation — allocating the last unit of stock, taking a payment, claiming a slot somebody else may claim",
          "None; queue everything and resolve later",
          "Only deletes"],
      c: 1,
      why: "Disabling them converts an unsolvable consistency problem into a product decision, which is the only place it can actually be solved." },

    { q: "RTO and RPO — which does asynchronous replication affect?",
      a: ["RTO, because failover takes longer",
          "RPO: whatever had not reached the replica when the primary died is lost, so the RPO is roughly the replication lag",
          "Neither; it is a performance choice",
          "Both equally"],
      c: 1,
      why: "Synchronous replication takes RPO to zero and charges the round trip on every commit, on good days as well as bad. Knowing which one you are running is the point." },

    { q: "What is split-brain and how is it prevented?",
      a: ["Two services sharing a database; separate them",
          "Two nodes that cannot see each other both promoting themselves to primary — prevented by a quorum of an odd number of voters, plus fencing so the demoted node cannot write",
          "A cache and a database disagreeing; add invalidation",
          "A deadlock across two servers; add retries"],
      c: 1,
      why: "It is why clusters come in threes, and why a two-node pair with automatic failover can be less safe than a single node." },

    { q: "Is a replica a backup?",
      a: ["Yes, if it is synchronous",
          "No — it faithfully replicates a mistaken DELETE within seconds",
          "Yes, if there are at least two",
          "Only for read-only workloads"],
      c: 1,
      why: "Replication protects against a machine dying. Backups protect against a person, and the two are not substitutes for each other." },

    { q: "Somebody says “we need multi-master”. What is the right first response?",
      a: ["Agree and pick a conflict strategy",
          "Ask which specific requirement demands it — a site that must survive the link dropping, a latency number, a regulator, vans with no signal",
          "Say it is always a mistake",
          "Propose microservices first"],
      c: 1,
      why: "It is not ducking the question. The follow-up you will get — “all right, the vans, how would you do it?” — is the conversation you wanted." },
  ],

  /* ------------------------------------------------------------------ 23 */
  "23-architecture-and-cqrs": [
    { q: "In clean architecture, which way does the dependency arrow point?",
      a: ["Outward, from the domain to the database",
          "Inward: Infrastructure and API depend on Application, Application depends on Domain, and Domain depends on nothing",
          "Both ways",
          "There is no rule"],
      c: 1,
      why: "The one testable consequence: the Domain project has no NuGet references to EF Core, ASP.NET Core or anything else. An architecture test can enforce exactly that." },

    { q: "The interface for a repository lives in which project, and the EF Core implementation in which?",
      a: ["Both in Infrastructure",
          "The interface in Application (the layer that needs it), the implementation in Infrastructure",
          "Both in Domain",
          "The interface in the API project"],
      c: 1,
      why: "That is dependency inversion made physical. If the interface lived beside the implementation, Application would have to reference Infrastructure and the arrow would flip." },

    { q: "What does CQRS actually separate?",
      a: ["The database into two databases, always",
          "The MODEL for reading from the model for writing — commands change state and return little, queries return data and change nothing",
          "The API from the UI",
          "Synchronous from asynchronous code"],
      c: 1,
      why: "Separate databases are an optional extreme, not the definition. Most CQRS codebases have one database, a write model with invariants and a read model shaped for the screen." },

    { q: "Where does a business rule like “an order over €10 000 needs approval” belong?",
      a: ["In the controller",
          "In the Domain — as behaviour on the aggregate that enforces the rule, so no code path can bypass it",
          "In the database as a trigger",
          "In the front end"],
      c: 1,
      why: "The test is: could someone add a second entry point (a background job, an import) that skips it? If yes, the rule is in the wrong layer." },

    { q: "What is an anemic domain model?",
      a: ["A model with too few tables",
          "Classes that are only public getters and setters, with all the behaviour in services — so the objects cannot protect their own invariants",
          "A domain with no interfaces",
          "A model without a database"],
      c: 1,
      why: "It is not automatically wrong — plenty of successful CRUD applications are built that way. It is wrong when the rules are complex, because they then live in six services and drift apart." },

    { q: "Why does this style of architecture add value ONLY at a certain size?",
      a: ["It does not; it is always correct",
          "Every boundary costs indirection, and on a small CRUD application the layers cost more to read than the coupling they prevent",
          "It requires expensive tooling",
          "It only works with microservices"],
      c: 1,
      why: "Being able to say when NOT to use it is what makes the argument credible. An interviewer hearing “always” assumes you learned it from a tutorial." },

    { q: "Select every rule an architecture test can usefully enforce.",
      a: ["Domain does not reference Infrastructure",
          "Handlers live in Application and are sealed",
          "Entities have no public setters",
          "Every method is under 20 lines"],
      c: [0, 1, 2],
      why: "The last is a style opinion, and a linter's job. Architecture tests are for the rules that are invisible in a code review but fatal to the design." },

    { q: "A dispatcher routes a Command object to its handler. What has that bought you over calling the handler directly?",
      a: ["Speed",
          "One place to wrap every request in cross-cutting behaviour — validation, logging, transactions, caching — without touching a single handler",
          "Type safety",
          "Nothing at all"],
      c: 1,
      why: "That pipeline is the real prize, and it is why the pattern outlives the specific library. It is also why MediatR going commercial did not kill the idea." },
  ],

  /* ------------------------------------------------------------------ 24 */
  "24-testing": [
    { q: "What is the actual purpose of a test suite?",
      a: ["Proving the code has no bugs",
          "Making change safe — so you can refactor and know within seconds whether you broke something",
          "Satisfying a coverage target",
          "Documenting the requirements"],
      c: 1,
      why: "Framing it as confidence-to-change explains every other decision: why brittle tests are worse than none, and why coverage as a goal produces tests that assert nothing." },

    { q: "The test pyramid, bottom to top?",
      a: ["End-to-end, integration, unit",
          "Many fast unit tests, fewer integration tests, very few end-to-end tests",
          "Equal numbers of each",
          "Only integration tests"],
      c: 1,
      why: "The shape follows from cost and speed, not from ideology. The inverted pyramid — mostly end-to-end — is the one that takes 40 minutes and fails randomly." },

    { q: "What should a unit test have a test double for?",
      a: ["Everything the class touches",
          "Only what you cannot control or cannot afford in a test: the clock, the network, the database, randomness",
          "Nothing",
          "Only interfaces"],
      c: 1,
      why: "Mocking a pure value object or a simple mapper buys nothing and welds the test to the implementation. Over-mocking is how a suite ends up asserting that the code calls the code." },

    { q: "What is wrong with the EF Core in-memory provider for integration tests?",
      a: ["It is slow",
          "It is not a relational database — no real SQL, no constraints, no transactions, different LINQ translation — so it passes tests that fail in production",
          "It is deprecated and removed",
          "It cannot be used with xUnit"],
      c: 1,
      why: "Testcontainers with a real SQL Server image is the modern answer: a throwaway container per test run, real behaviour, at the cost of needing Docker." },

    { q: "In xUnit, how are the tests inside one test class executed?",
      a: ["In parallel with each other",
          "Sequentially within a class, but different classes (collections) run in parallel by default",
          "All in parallel",
          "In alphabetical order"],
      c: 1,
      why: "Which is why shared static state across test classes causes flakiness, and why a fixture that touches a real database usually needs a collection to serialise it." },

    { q: "What names the three parts of a good test?",
      a: ["Setup, Run, Check",
          "Arrange, Act, Assert",
          "Given, Then, When",
          "Mock, Call, Verify"],
      c: 1,
      why: "One Act per test is the discipline that makes a failure name its own cause. A test with three Acts tells you something broke, not what." },

    { q: "Select everything that is NOT worth unit testing.",
      a: ["Framework behaviour — that ASP.NET Core routes a request",
          "Auto-properties with no logic",
          "That a mapper copies field A to field A",
          "A discount calculation with several branches"],
      c: [0, 1, 2],
      why: "The last one is precisely what tests are for: branches, edge values and rounding. The first three test somebody else's code or nothing at all." },

    { q: "A test fails intermittently in CI and passes locally. What are the usual suspects?",
      a: ["The compiler",
          "Shared state between tests, real clocks and time zones, ordering assumptions, and unawaited async work",
          "Insufficient assertions",
          "Too much mocking"],
      c: 1,
      why: "DateTime.Now in a test is a flake generator — the reason a TimeProvider or an injected clock is worth the small ceremony." },
  ],

  /* ------------------------------------------------------------------ 25 */
  "25-observability": [
    { q: "Why is structured logging better than string interpolation in a log message?",
      a: ["It is shorter",
          "The message template and its named values are kept separate, so you can query for OrderId = 4711 across millions of lines instead of grepping text",
          "It is required by Serilog",
          "It produces smaller log files"],
      c: 1,
      why: "This is why `logger.LogInformation(\"Order {OrderId} shipped\", id)` and `logger.LogInformation($\"Order {id} shipped\")` look identical and are not remotely equivalent." },

    { q: "What is a correlation id for?",
      a: ["Identifying the user",
          "Tying together every log line, span and downstream call belonging to one request, so an error can be traced across services",
          "Caching",
          "Rate limiting"],
      c: 1,
      why: "In OpenTelemetry it is the TraceId, propagated over the W3C traceparent header. Returning it in the error response is what turns “it broke” from a support ticket into a search." },

    { q: "The three pillars of observability?",
      a: ["Logs, metrics, traces",
          "Errors, warnings, information",
          "CPU, memory, disk",
          "Alerts, dashboards, reports"],
      c: 0,
      why: "Logs say what happened, metrics say how much and how often, traces say where the time went. Alerting on metrics and diagnosing with traces and logs is the usual division of labour." },

    { q: "Liveness versus readiness health checks?",
      a: ["Identical",
          "Liveness asks “is the process alive?” and must not check dependencies; readiness asks “can it serve traffic?” and does",
          "Readiness restarts the pod",
          "Liveness checks the database"],
      c: 1,
      why: "Getting it backwards is a real outage: a liveness probe that checks the database restarts every instance during a database blip, turning a degradation into a total failure." },

    { q: "Which of these should NEVER appear in a log line?",
      a: ["An order id",
          "A password, a full card number, a bearer token or a personal identifier",
          "A duration in milliseconds",
          "An HTTP status code"],
      c: 1,
      why: "Logs are copied to aggregators, retained for months and read by more people than the database is. It is one of the most common real GDPR findings." },

    { q: "What log level should a caught-and-handled validation failure use?",
      a: ["Error",
          "Information or Warning — Error should mean something that needs a human, or the alert loses meaning",
          "Critical",
          "Debug"],
      c: 1,
      why: "Level discipline is what stops alert fatigue. If a normal Tuesday produces 4 000 Errors, nobody will notice the one that mattered." },

    { q: "Select every piece of context worth attaching to a request's logs automatically.",
      a: ["Trace id", "User id or tenant id", "The request path and status code", "The full request body"],
      c: [0, 1, 2],
      why: "Bodies contain secrets and personal data, and they are large. Log a hash or a few named fields if you must correlate on content." },

    { q: "Your API is slow but every individual log line looks fine. Which tool answers the question?",
      a: ["More logging",
          "A distributed trace — the span waterfall shows which call actually consumed the time",
          "A memory dump",
          "The database's error log"],
      c: 1,
      why: "This is the case metrics and logs handle badly and traces handle well: total time is fine to observe, but only a trace tells you it was one N+1 loop inside one handler." },
  ],

  /* ----------------------------------------------------------------- 25b */
  "25b-production-support": [
    { q: "The orders page has been returning 500s for four minutes. A release went out an hour ago. What is your first action?",
      a: ["Reproduce it locally to find the bug",
          "Roll back the release — mitigate before diagnosing; you do not need to know which change caused it",
          "Read the logs until you understand the cause",
          "Ask the developer who made the change what they did"],
      c: 1,
      why: "Mitigation makes the fix unhurried, and unhurried fixes are correct fixes. Leading with the rollback rather than the investigation is what distinguishes someone who has been in an incident from someone who has read about one." },

    { q: "Which of these are legitimate mitigations before the cause is known? (Select every one that applies.)",
      a: ["Roll back the last deploy",
          "Turn the feature off with a flag",
          "Scale out or disable an expensive batch job",
          "Push a hotfix written in the last ten minutes"],
      c: [0, 1, 2],
      why: "A hotfix written under pressure without understanding the cause is a second incident waiting to happen. Stop the bleeding with a reversible action, then diagnose calmly." },

    { q: "You are unsure whether an issue is serious enough to escalate. What is the rule?",
      a: ["Wait until you are certain, to avoid crying wolf",
          "Escalate — being wrong that way costs a colleague ten minutes, while not escalating and being wrong costs the company a day",
          "Escalate everything, always",
          "Ask in the channel and wait for someone to decide"],
      c: 1,
      why: "The counterweight matters too: a team where everything is Sev 1 stops responding to Sev 1. Severity is about who is stopped and whether there is a workaround." },

    { q: "Which of these is a good production alert?",
      a: ["CPU above 85%",
          "The 5xx rate above 1% for five minutes",
          "A service restarted",
          "Memory above 90%"],
      c: 1,
      why: "Alert on symptoms a user would notice, not on causes. CPU is high during healthy nightly batches and often normal during real outages — it fires when nothing is wrong and stays quiet when something is." },

    { q: "For a message queue or an outbox, what should the alert measure?",
      a: ["The number of items waiting",
          "The age of the oldest unprocessed item",
          "The rate of new items arriving",
          "The total processed today"],
      c: 1,
      why: "A big queue that is draining is healthy; a small one that is stuck is not. Age catches the stuck case and ignores the busy one." },

    { q: "An alert fires most mornings and the team's response is always “yeah, that happens”. What should you do?",
      a: ["Nothing — it is informational",
          "Fix the underlying issue, change the threshold, or delete the alert deliberately — an alert nobody acts on hides the one that matters",
          "Route it to a separate channel and ignore it there",
          "Increase its severity so people take it seriously"],
      c: 1,
      why: "Alert fatigue is a real failure mode. Every alert needs an action and ideally a runbook entry; writing one is an excellent first contribution from a junior." },

    { q: "A user reports “the orders page is broken”. What is the single most valuable next question?",
      a: ["Which browser are you using?",
          "Can you give me the correlation id from the error screen, or the order number and the time?",
          "Have you tried clearing your cache?",
          "Does it happen every time?"],
      c: 1,
      why: "An identifier turns an afternoon of guessing into five minutes of reading one request end to end. This is the payoff for structured logging and correlation ids." },

    { q: "Code works on your machine and fails in production. Which four differences should you check first?",
      a: ["Compiler version, IDE, operating system, RAM",
          "Configuration, culture and time zone, production data shapes, and concurrency",
          "Network speed, disk space, antivirus, firewall",
          "Framework version, NuGet versions, build configuration, target runtime"],
      c: 1,
      why: "Configuration is usually a key that is absent rather than wrong, binding silently to a default. The others are chapters 23, and 08 and 11 respectively." },

    { q: "Why are postmortems blameless?",
      a: ["To protect people's feelings",
          "Because blame destroys the information — in a blaming culture people stop reporting, so you find out later and worse",
          "Because it is a legal requirement",
          "Because the cause is never one person's fault"],
      c: 1,
      why: "It is not politeness, it is an information-gathering technique. The practical test is where the five whys stop." },

    { q: "A five whys analysis ends at “Marco forgot to add the configuration key”. What is wrong with that?",
      a: ["Nothing — that is the cause",
          "It stopped at a person rather than at a property of the system, so the only action it can produce is “be more careful”",
          "It should have been ten whys",
          "The name should be anonymised"],
      c: 1,
      why: "Keep going: why did that ship? Because nothing validates that required settings exist. That fix turns a 38-minute outage into an application that refuses to start." },

    { q: "What is the test of a good postmortem action item?",
      a: ["It has an owner and a date",
          "It is a change to code, configuration or process that would prevent this class of incident even with the same people making the same mistake",
          "It can be completed within the sprint",
          "It is agreed by everyone in the room"],
      c: 1,
      why: "An owner and a date are necessary but not sufficient. “Be more careful” has both and prevents nothing — nobody has ever been more careful." },

    { q: "What does an ADR record that a code comment does not?",
      a: ["How the code works",
          "The context, the alternatives that were rejected and why, and the consequences — including negative ones — that were knowingly accepted",
          "Who wrote the code",
          "The performance characteristics"],
      c: 1,
      why: "Recording the rejected alternatives stops a settled question being re-argued in eighteen months, and recording the accepted downside stops it being rediscovered as a bug." },

    { q: "What happens to an ADR when the decision it records is later reversed?",
      a: ["It is edited to describe the new decision",
          "It is left unchanged and marked superseded by a new ADR — the record of what was thought at the time is the point",
          "It is deleted",
          "It is moved to an archive folder and rewritten"],
      c: 1,
      why: "An edited ADR loses exactly the thing that made it worth writing: what was known and believed when the decision was made." },

    { q: "How do you reduce a vague bug report to something you can fix?",
      a: ["Add logging everywhere and wait for it to happen again",
          "Remove one variable at a time until it stops failing — the last thing removed is the cause",
          "Rewrite the affected component",
          "Ask the user to reproduce it while you watch"],
      c: 1,
      why: "“It fails for this customer” becomes “it fails when two addresses share a postcode”, and by then you have named the bug and can usually guess the line." },
  ],

  "16c-a-feature-end-to-end": [
    { q: "In what order do you build the layers of a new feature?",
      a: ["View first, then work backwards to the entity",
          "Entity first, then DbContext, migration, repository, service, view model, controller, view",
          "Whatever order the ticket lists them",
          "Controller and view together, the rest later"],
      c: 1,
      why: "Each layer is shaped by the one before it, so the entity comes first and the view last. Building outside-in means guessing at a shape you have not defined yet." },

    { q: "Which project does the entity class belong in, and what may it know about?",
      a: ["Infrastructure — it needs EF Core",
          "Domain — and it knows nothing about the database, HTTP or the screen",
          "Web — it is close to the controller",
          "Application — next to the service"],
      c: 1,
      why: "The entity is the thing itself, as the business understands it. Keeping EF, HTTP and UI out of it is what makes the domain testable with the database ripped out." },

    { q: "Why are an entity's setters private, with state changed through methods?",
      a: ["It is faster at runtime",
          "So the object cannot be put into an invalid state — the only doors in enforce the rules",
          "EF Core requires private setters",
          "To hide the data from the debugger"],
      c: 1,
      why: "A bag of public get/set properties lets any caller create a bad row. Guarded methods like Rename and SetCode keep each rule in exactly one place." },

    { q: "Why does the entity keep a private parameterless constructor?",
      a: ["To let other classes clone it",
          "EF Core materialises rows through it; private stops your own code building a nameless entity",
          "It is required by C# for any class",
          "To reset the object to defaults"],
      c: 1,
      why: "EF calls a parameterless constructor by reflection and then sets properties. Keeping it private means only EF uses it, while your code must go through the real constructor." },

    { q: "What two edits does adding one entity make to the DbContext area?",
      a: ["Two new methods on DbContext",
          "A DbSet on the context and one IEntityTypeConfiguration class",
          "A change to OnModelCreating and a new connection string",
          "A DbSet and a new migration inside the context"],
      c: 1,
      why: "The DbSet is the query root that tells EF the entity exists; the configuration class says how it maps. Because the context scans for configurations, you never touch OnModelCreating again." },

    { q: "Where should the table mapping (max length, required, indexes) live?",
      a: ["As data annotations on the entity",
          "In an IEntityTypeConfiguration class in Infrastructure",
          "In the migration file only",
          "In Program.cs"],
      c: 1,
      why: "Fluent configuration in a dedicated class keeps mapping out of the domain entity, which is what lets the entity stay free of EF. It is also found automatically by the assembly scan." },

    { q: "Why call HasMaxLength on a string property?",
      a: ["It makes queries case-insensitive",
          "The default is nvarchar(max), which cannot be indexed and wastes space",
          "It encrypts the column",
          "It is required or the migration fails"],
      c: 1,
      why: "Without a max length EF maps the column to nvarchar(max). Setting a length gives a sensible column type and lets the column be indexed." },

    { q: "Where is a unique-code rule best enforced?",
      a: ["Only in C# with a check-then-insert",
          "As a unique index in the database, carried there by a migration",
          "In the view model with a validation attribute",
          "In the controller before saving"],
      c: 1,
      why: "A C# check races under load; a unique index is enforced by the one component that sees every write. HasIndex(...).IsUnique() becomes a CreateIndex step in the migration." },

    { q: "What does `dotnet ef migrations add` actually do?",
      a: ["Applies changes to the database immediately",
          "Compares the model to the last migration's snapshot and writes a file for the difference",
          "Deletes the database and recreates it",
          "Generates the entity classes from the database"],
      c: 1,
      why: "It only writes the migration file; nothing touches the database until `database update`. The file is the versioned record of one intended schema change." },

    { q: "In a split solution, what is the `-s` (startup) project for in an EF command?",
      a: ["It names the migration",
          "EF runs it to find the connection string and DI configuration",
          "It is where the migration files are written",
          "It is optional and ignored"],
      c: 1,
      why: "`-p` is where the migration files land (the DbContext project); `-s` is the app EF runs to discover configuration. Forgetting `-s` is the most common first migration error." },

    { q: "You need to change a migration that has NOT shipped yet. What do you do?",
      a: ["Edit the applied migration file directly",
          "Run `migrations remove`, fix the model, and add it again",
          "Write a second migration to patch the first",
          "Delete the whole Migrations folder"],
      c: 1,
      why: "Editing an already-applied migration desyncs the model snapshot and the next add produces garbage. Remove the last one, adjust the model, regenerate. Never edit an applied migration." },

    { q: "Where does a repository INTERFACE live, and why there?",
      a: ["In Infrastructure, beside the implementation",
          "In Application — the layer that uses it owns the abstraction (dependency inversion)",
          "In Domain, on the entity",
          "In Web, next to the controller"],
      c: 1,
      why: "The service depends on an abstraction it owns; Infrastructure plugs in behind it. That inversion is also what lets the service be unit-tested with a fake repository." },

    { q: "Why not expose IQueryable from a repository method?",
      a: ["IQueryable is slower than a list",
          "It lets a caller three layers up compose the query, leaking EF concerns upward",
          "IQueryable cannot be awaited",
          "It breaks change tracking"],
      c: 1,
      why: "A repository that returns IQueryable is barely a boundary at all. Domain-named methods like GetByIdAsync keep query composition and EF types out of the application layer." },

    { q: "When should a read query use AsNoTracking()?",
      a: ["Always, on every query",
          "On read-only queries you only display — it skips the change-tracker snapshots and is faster",
          "Only inside a transaction",
          "Never, it causes stale data"],
      c: 1,
      why: "A query whose results you will not modify does not need tracking snapshots. Turn it off for reads; leave it on when you intend to change what you load." },

    { q: "A repository's Add method stages an entity but does not save. Who saves?",
      a: ["Add saves immediately",
          "The service, via one SaveChangesAsync at the transaction boundary",
          "The controller, after returning the view",
          "EF saves automatically on the next request"],
      c: 1,
      why: "Add only tells the change tracker the entity is new. Saving inside Add would make every insert its own transaction; the service owns the single commit point." },

    { q: "What type should a service method take as input?",
      a: ["The EF entity",
          "A DTO shaped for the use case — not the entity and not the view model",
          "The view model from the form",
          "The DbContext"],
      c: 1,
      why: "A record like CreateCarrier carries exactly the fields the use case needs. The web layer maps its form onto it, so the service never sees an HTTP or screen type." },

    { q: "The rule 'a carrier code must be unique' belongs where?",
      a: ["On the entity, in its constructor",
          "In the service — it spans the whole set and needs the repository",
          "In the view model as a data annotation",
          "In the migration"],
      c: 1,
      why: "One entity cannot check uniqueness about itself; it needs to consult the others via the repository. So it is a service rule, expressed as an early throw." },

    { q: "Why does a view model contain only Name and Code, not Id, IsActive or CreatedUtc?",
      a: ["To save memory",
          "The user does not set those, so leaving them off the posted model closes over-posting by construction",
          "EF ignores extra properties anyway",
          "They are added automatically by the binder"],
      c: 1,
      why: "Binding a form to a type that has fields the user should not control invites mass assignment. A view model of only the editable fields makes the attack impossible, not merely unlikely." },

    { q: "How long should the controller action be, and what does it do?",
      a: ["As long as needed; it holds the business logic",
          "About ten lines: bind, delegate to one service, choose a result",
          "It should contain the LINQ queries for the screen",
          "It formats the data for display"],
      c: 1,
      why: "If an action is longer than roughly ten lines, work has leaked into it. Queries belong in the repository, rules in the service, formatting in the view." },

    { q: "You wrote the repository and service but the app throws at the first request. Likely cause?",
      a: ["A missing migration",
          "The new repository and service were not registered with the DI container",
          "The entity has no Id",
          "The view model is not nullable"],
      c: 1,
      why: "The composition root must register the concrete types — AddScoped for the repository and the service — or the container cannot build the controller's dependencies." },
  ],

  "16d-mvc-mvvm-mvp": [
    { q: "What do MVC, MVP and MVVM all have in common?",
      a: ["They are all web frameworks",
          "They all separate the model (data and rules) from the view (the screen), differing only in the middle piece",
          "They all use data binding",
          "They are three names for the same pattern"],
      c: 1,
      why: "All three are separated presentation. The model and the view are the same idea in each; what changes is the mediator and how updates reach the screen." },

    { q: "Which single question best distinguishes the three patterns?",
      a: ["Which language they use",
          "Who updates the view — a new request, the presenter by hand, or data binding",
          "Whether they are open source",
          "How many classes they need"],
      c: 1,
      why: "MVC re-renders on a new request, MVP has the presenter push updates, MVVM lets binding update on notification. Naming the update mechanism is the heart of the answer." },

    { q: "In MVC, what is the mediator and where does a request arrive?",
      a: ["The ViewModel; the request arrives at the view",
          "The Controller; the request arrives at it, and it coordinates the model and view",
          "The Presenter; the request arrives at the model",
          "The View; it handles the request itself"],
      c: 1,
      why: "The controller is the entry point — the request arrives at it, unlike MVP and MVVM where the view exists first and the mediator hangs off it." },

    { q: "What is true of the view in MVC?",
      a: ["It holds screen state between requests",
          "It is stateless and disposable — built for one response and then discarded",
          "It updates itself through binding",
          "It contains the business rules"],
      c: 1,
      why: "There is no live object watching the model; the next update is simply the next request. That statelessness is exactly why MVC fits HTTP." },

    { q: "Is the 'view model' in ASP.NET Core MVC the same thing as MVVM's ViewModel?",
      a: ["Yes, identical",
          "No — MVC's is just a per-screen data class, with no binding or change notification",
          "Yes, but only in Razor Pages",
          "No, MVC has no view model at all"],
      c: 1,
      why: "Same two words, different pattern. MVVM's ViewModel is a live, bindable object with commands and notifications; MVC's is a plain class shaped for one view. A classic interview trap." },

    { q: "In MVP, what does 'the view is passive' mean?",
      a: ["The view is read-only to the user",
          "The view exposes properties and events through an interface and does nothing clever; the presenter drives it",
          "The view is rendered on the server",
          "The view has no controls"],
      c: 1,
      why: "A passive view behind an IView interface holds no logic. The presenter reads its properties and calls methods like ShowError to update it." },

    { q: "Why does an MVP presenter talk to the view through an interface (IView)?",
      a: ["To make the UI look consistent",
          "So the presenter depends on an abstraction, not a real window — which makes it unit-testable with a fake view",
          "Because WinForms requires interfaces",
          "To allow multiple languages"],
      c: 1,
      why: "The interface is the whole point: the presenter never touches a real control, so you can test all the UI logic with a fake IView — the thing WinForms otherwise makes almost impossible." },

    { q: "In MVP, who updates the view and how?",
      a: ["Data binding, automatically",
          "The presenter, explicitly and by hand, through the IView interface",
          "A new request re-renders it",
          "The model updates it directly"],
      c: 1,
      why: "There is no automatic binding — every update is a line of code like _view.ShowError(...). That is the cost, and the control, compared with MVVM." },

    { q: "What is the defining trait of MVVM's ViewModel?",
      a: ["It renders the HTML",
          "It has no reference to the view at all — binding connects them",
          "It holds a reference to the view like a presenter does",
          "It runs on the server only"],
      c: 1,
      why: "The ViewModel could not call the screen if it wanted to. It raises PropertyChanged and lets the binder react — that decoupling is what separates it from MVC and MVP." },

    { q: "What is the role of INotifyPropertyChanged in MVVM?",
      a: ["It validates input",
          "Raising it announces a state change so the binding framework updates the bound control",
          "It serialises the view model to JSON",
          "It registers the view model with DI"],
      c: 1,
      why: "Setting a property raises PropertyChanged; the data-binding engine hears it and updates the bound control. That is the automatic update MVP did by hand." },

    { q: "Why does a MVVM view bind a button to an ICommand instead of a click handler?",
      a: ["Commands are faster",
          "So the whole view model can be tested with no UI — you set state and invoke the command directly",
          "ICommand is required by XAML",
          "To disable the button by default"],
      c: 1,
      why: "Binding the button to a command object, rather than wiring a code-behind event, keeps the view model free of the view — which is what makes it testable in isolation." },

    { q: "In MVVM, who updates the view?",
      a: ["The view model calls the view",
          "Data binding does, on notification — the view model changes state and announces it, never touching the screen",
          "The presenter pushes updates",
          "A new HTTP request"],
      c: 1,
      why: "The view model changes state and raises a notification; the binder updates the screen. It never references the view. That, plus commands, is the entire pattern." },

    { q: "Which .NET technologies are the natural home of MVVM?",
      a: ["ASP.NET Core MVC and Razor Pages",
          "WPF, .NET MAUI, Avalonia and Uno — and Blazor in a flavoured form",
          "WinForms and classic Web Forms",
          "Console applications"],
      c: 1,
      why: "MVVM needs a data-binding engine, which is what those UI stacks provide. It is the pattern greenfield rich-client work reaches for." },

    { q: "Which pattern is ASP.NET Core MVC (and Razor Pages) an instance of?",
      a: ["MVVM",
          "MVC",
          "MVP",
          "None of them"],
      c: 1,
      why: "Stateless request in, rendered view out, with a controller as the entry point. Razor Pages is a page-centric take on the same request/response model." },

    { q: "Where does MVP still show up in .NET work today?",
      a: ["New Blazor apps",
          "In WinForms and classic Web Forms code you maintain — an event-driven, stateful view with no binding",
          "In ASP.NET Core Web APIs",
          "In microservices"],
      c: 1,
      why: "MVP is how you pull logic out of a WinForms/Web Forms code-behind into a testable presenter behind an IView. New desktop work uses MVVM instead, so MVP is mostly a legacy-maintenance pattern." },

    { q: "Why does the web tend to use MVC while WPF uses MVVM?",
      a: ["MVC is newer than MVVM",
          "WPF has a binding engine so a view model can notify and let the framework update; the web is stateless, so the next update is the next request",
          "MVVM does not work over HTTP for security reasons",
          "It is purely a matter of taste"],
      c: 1,
      why: "The platform's update model picks the pattern: binding ⇒ MVVM, stateless request ⇒ MVC. There is no live object to bind to on a classic web request." },

    { q: "Is Blazor textbook MVVM?",
      a: ["Yes, exactly like WPF",
          "MVVM-flavoured — a component binds and re-renders on state change, but view and view model are not separate objects",
          "No, it is pure MVC",
          "No, it is MVP"],
      c: 1,
      why: "The binding-driven update is MVVM's idea, but the component holds both markup and state rather than splitting them the way WPF splits XAML from a view-model class. Saying both halves is the strong answer." },

    { q: "You open a project with .xaml files, {Binding}, INotifyPropertyChanged and ICommand. Which pattern?",
      a: ["MVC",
          "MVVM",
          "MVP",
          "No pattern"],
      c: 1,
      why: "Binding markup plus change notification plus commands is the MVVM signature — a WPF or MAUI app." },

    { q: "You see a Form, an IView interface and a presenter that pushes updates to it. Which pattern?",
      a: ["MVVM",
          "MVP",
          "MVC",
          "Razor Pages"],
      c: 1,
      why: "A passive view behind an interface with a presenter holding the logic and a reference to the view is textbook MVP — the shape you retrofit onto WinForms." },

    { q: "What is the key difference between MVVM and MVP?",
      a: ["MVVM is for the web, MVP is for desktop",
          "In MVP the presenter holds a reference to the view and pushes by hand; in MVVM the view model has no reference to the view and binding syncs automatically",
          "MVP has no model",
          "MVVM does not separate the view from the logic"],
      c: 1,
      why: "Both keep the view logic-free, but the coupling differs: presenter-to-view is a direct reference with manual updates, while a view model never references the view and relies on binding." },
  ],
});
