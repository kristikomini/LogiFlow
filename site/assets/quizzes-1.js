/* ==========================================================================
   quizzes-1.js — the question bank, chapters 00 to 12.

   Split across three files purely so each one stays editable; they all merge
   into the same `window.QUIZZES` map, keyed by chapter id.

   Shape of a question:

     q     the stem. Plain text; &lt; and &gt; must be escaped.
     code  optional snippet shown above the options, in a <pre>.
     a     2–5 options.
     c     index of the correct option, OR an array of indices for
           "select every one that applies".
     why   shown after answering, always — including when the answer was
           right. Feedback that only appears on failure teaches guessing.

   Every question doubles as a spaced-repetition card (see store.js), so the
   stem has to make sense on its own two weeks later, out of context. "Which
   of these is true?" is a bad stem for that reason; name the subject.
   ========================================================================== */

window.QUIZZES = Object.assign(window.QUIZZES || {}, {

  /* ------------------------------------------------------------------ 00 */
  "00-the-job-posting": [
    { q: "An advert lists a skill under “Requisiti” rather than “A completamento del profilo”. What does that distinction usually mean in practice?",
      a: ["Requisiti are legally binding; the rest is optional",
          "Requisiti are what they will actually test you on; the second list is what would make you cheaper to onboard",
          "The second list is more important because it is more specific",
          "There is no difference — Italian adverts always list everything twice"],
      c: 1,
      why: "The first list is the filter and the interview agenda. The second is a wish list — being weak there is survivable, being weak on the first list is not." },

    { q: "The advert asks for “da 1 a 3 anni” of .NET experience. What is the most useful way for someone without commercial experience to read that?",
      a: ["Do not apply; the requirement is absolute",
          "It describes the level of independence they need, so evidence of that level can substitute for the years",
          "Add years to the CV — nobody checks",
          "It only matters for the salary band"],
      c: 1,
      why: "Year counts are a proxy for “can work a ticket without supervision”. A repository that demonstrates that answers the real question. Inventing years does not survive a technical round." },

    { q: "An advert names .NET, C#, T-SQL, REST, Web API and EF Core, and nothing about front-end frameworks. What is the most likely shape of their codebase?",
      a: ["A microservice mesh with a React front end",
          "A server-side .NET application over SQL Server, probably with an MVC or Razor UI, where the database is the centre of gravity",
          "A mobile-first application",
          "A greenfield project with no existing code"],
      c: 1,
      why: "The absence of a named SPA framework plus the emphasis on T-SQL says the data model is the system. Expect an existing codebase, not a blank page." },

    { q: "Which two words in an Italian advert most reliably tell you the company works for other companies rather than on its own product?",
      a: ["“startup” and “innovativa”", "“consulenza” and “clienti”", "“smart working” and “flessibilità”", "“stage” and “tirocinio”"],
      c: 1,
      why: "Consulenza/clienti means a consultancy: multiple codebases, more process, an automated screening test, and travel to client sites. It changes what to prepare for." },

    { q: "Why is “Ottima conoscenza del linguaggio C#” a more demanding line than “Buona conoscenza di T-SQL” in the same advert?",
      a: ["C# is harder than SQL",
          "The adjectives are graded: ottima > buona > familiarità, and they tell you where the bar is highest",
          "It is not — the adjectives are interchangeable filler",
          "T-SQL is optional in .NET roles"],
      c: 1,
      why: "Italian adverts grade requirements with adjectives. Reading them tells you where to spend your revision time, and which topic the interviewer will go deepest on." },

    { q: "The advert asks for “capacità di analisi e autonomia decisionale nel proprio dominio”. What is that testing for?",
      a: ["Whether you can work without ever asking questions",
          "Whether you can take an underspecified ticket, decide the parts nobody wrote down, and be able to justify the decision",
          "Whether you have management experience",
          "Whether you are comfortable working alone in an office"],
      c: 1,
      why: "It is not about never asking. It is about not needing a decision made for you on every ambiguity, and being able to say why you chose what you chose." },

    { q: "You match seven of nine “Requisiti” and none of the “completamento” list. What is the reasonable action?",
      a: ["Do not apply until you match all of them",
          "Apply, and in the covering note address the two gaps directly with what you are doing about them",
          "Apply and hope the gaps are not noticed",
          "Apply to a different, easier advert instead"],
      c: 1,
      why: "Adverts describe an ideal candidate who usually does not apply. Naming your own gaps first is read as self-awareness; hiding them reads as a surprise in the interview." },

    { q: "Why does learning the layers in the order platform → language → data → web → shipping beat learning them in the order the advert lists them?",
      a: ["The advert is written in a random order and later items depend on earlier ones",
          "The advert lists skills by hiring priority, not by dependency, and you cannot understand EF Core before SQL or async before the CLR",
          "It does not matter what order you learn them in",
          "The advert's order is alphabetical"],
      c: 1,
      why: "Each layer is only comprehensible once the one under it is. EF Core is unlearnable as a topic if you cannot read the SQL it emits." },
  ],

  /* ----------------------------------------------------------------- 00a */
  "00a-your-first-program": [
    { q: "What is the difference between the .NET SDK and the .NET runtime?",
      a: ["The SDK runs applications; the runtime builds them",
          "The SDK builds and runs — it contains the compiler, the CLI and the runtime; the runtime only executes an already-built application",
          "They are two names for the same download",
          "The SDK is for Windows and the runtime is for Linux"],
      c: 1,
      why: "Your machine and a build agent need the SDK. A production container only needs the runtime, which is why a Dockerfile builds in an SDK image and copies the output into a smaller runtime image." },

    { q: "In C#, what is the difference between writing Console.WriteLine and Console.WriteLine()?",
      a: ["Nothing — the parentheses are optional",
          "The first only names the method; the parentheses are what actually call it",
          "The first is faster because it skips argument checking",
          "The first is the property form and the second is the method form"],
      c: 1,
      why: "Naming a method and calling it are different acts. This is also why a property has no parentheses and a method does — Length versus ToUpper()." },

    { q: "A build fails with: Program.cs(2,42): error CS1002: ; expected. What do the numbers 2 and 42 mean?",
      a: ["Two errors were found, on line 42",
          "Line 2, column 42 — in that order",
          "Error severity 2, error group 42",
          "Column 2, line 42"],
      c: 1,
      why: "Line first, column second, always. The real fault is often on the line above the one reported, because the compiler only notices something is missing when it reaches whatever came next." },

    { q: "What is the difference between a compile-time error and a run-time error?",
      a: ["Compile-time errors are warnings; run-time errors stop the program",
          "A compile-time error is found by the compiler and the program never runs; a run-time error happens while it is running, after it compiled successfully",
          "Compile-time errors happen in Debug builds only",
          "They are the same thing reported by different tools"],
      c: 1,
      why: "Most of what a static type system does is move errors from the second category into the first, where they cost a minute instead of a user." },

    { q: "In C#, what is the difference between 'A' and \"A\"?",
      a: ["Nothing — C# accepts either quote style for text",
          "'A' is a char, exactly one character; \"A\" is a string, which happens to contain one character. They are different types",
          "'A' is a string literal and \"A\" is a verbatim string",
          "Single quotes are only valid inside interpolated strings"],
      c: 1,
      why: "Unlike JavaScript or Python, the quote style selects the type in C#. Passing a char where a string is expected is a compile error." },

    { q: "Why must the Main method be static?",
      a: ["To make it run faster at start-up",
          "Because at start-up no object exists yet, so there is nothing to call an instance method on",
          "Because static methods are the only ones allowed to use string[] parameters",
          "It does not have to be static; that is a convention"],
      c: 1,
      why: "Static means the method belongs to the type rather than to an instance. The runtime needs an entry point it can invoke before any of your objects exist." },

    { q: "A modern Program.cs contains only Console.WriteLine(\"Hello\"); with no class and no Main. What is going on?",
      a: ["The file is incomplete and will not compile",
          "Top-level statements — the compiler generates the namespace, class and Main around it; only one file per project may do this",
          "C# no longer needs an entry point",
          "It only works in Debug builds"],
      c: 1,
      why: "It is the same entry point with the ceremony generated, not a lesser form of it. args and await are still available because the generated Main adapts." },

    { q: "What does the using directive at the top of a C# file actually do?",
      a: ["It downloads the library at run time",
          "It imports names, not code — so you can write Console instead of System.Console",
          "It copies the referenced assembly into your output folder",
          "It creates an instance of the namespace"],
      c: 1,
      why: "The assembly is already referenced by the project; the directive only affects how short a name you may write. Modern templates enable implicit usings, which is why new files often have none." },
  ],

  /* ----------------------------------------------------------------- 00b */
  "00b-variables-and-types": [
    { q: "Why must a monetary amount be stored in a decimal rather than a double?",
      a: ["decimal is faster for arithmetic",
          "double is binary floating point, so values like 0.1 have no exact representation and the error accumulates; decimal is base-10 and exact for the values money takes",
          "double cannot hold numbers large enough for currency",
          "decimal automatically applies the correct currency symbol"],
      c: 1,
      why: "0.1 + 0.2 is 0.30000000000000004 as a double. An invoice out by a fraction of a cent fails reconciliation, and an accountant finds it months later." },

    { q: "Both a and b are int, with a = 5 and b = 2. What is a / b?",
      code: "int a = 5, b = 2;\nint result = a / b;",
      a: ["2.5", "2", "3", "It does not compile"],
      c: 1,
      why: "Integer division: when both operands are whole numbers the result is whole and the fraction is discarded. Nothing warns you. Cast one side first — (double)a / b — and note that (double)(a / b) would be too late." },

    { q: "What is the value of (int)3.99?",
      a: ["4 — it rounds to the nearest whole number",
          "3 — a cast to int truncates, it does not round",
          "3.99 — the cast has no effect",
          "It throws an InvalidCastException"],
      c: 1,
      why: "A cast is an assertion that you accept the loss. (int)3.99 and (int)3.01 are both 3. Use Math.Round if you meant rounding." },

    { q: "Casting a long that is too large into an int — what happens?",
      code: "long huge = 5_000_000_000;\nint wrapped = (int)huge;",
      a: ["A compile error", "An OverflowException at run time",
          "It silently produces a wrong number with no error or warning",
          "It clamps to int.MaxValue"],
      c: 2,
      why: "By default the extra bits are discarded and you get nonsense — no diagnostic at compile time or run time. This is why a cast should be a decision, not a way to silence a red squiggle." },

    { q: "You are converting text a user typed into a number. Which should you use, and why?",
      a: ["int.Parse, because it is faster",
          "int.TryParse, because a typo is expected input rather than an exceptional condition — it returns a bool instead of throwing",
          "Convert.ToInt32, because it never fails",
          "A cast: (int)theString"],
      c: 1,
      why: "Parse throws FormatException on bad text, so it belongs where bad text would be a bug. Exceptions are also expensive, which makes Parse-in-a-try-catch slow as well as wrong over a large file." },

    { q: "int.TryParse(\"banana\", out int z) returns false. What is z?",
      a: ["null", "0", "unchanged — it keeps whatever it had before", "-1"],
      c: 1,
      why: "It is set to the default for the type, which is 0. That is why you must check the bool: a 0 from a failed parse is indistinguishable from a genuine 0 if you ignore the return value." },

    { q: "Is var in C# the same kind of thing as var in JavaScript?",
      a: ["Yes — both defer the type until run time",
          "No — var in C# is compile-time type inference; the type is fixed from the initialiser and cannot change afterwards",
          "Yes, but C# only allows it for local variables",
          "No — var in C# means the variable is read-only"],
      c: 1,
      why: "var total = 10; makes an int, and total = \"twenty\"; is then a compile error. The keyword that actually defers typing to run time is dynamic, which is a different feature." },

    { q: "first is a string holding \"Mario\". After calling first.ToUpper(); on a line by itself, what does first contain?",
      code: "string first = \"Mario\";\nfirst.ToUpper();\nConsole.WriteLine(first);",
      a: ["\"MARIO\"", "\"Mario\" — strings are immutable, so ToUpper returns a new string that was discarded",
          "An empty string", "It does not compile"],
      c: 1,
      why: "Nothing can modify a string once it exists. Every method that appears to change one actually returns a new one, so ignoring the return value accomplishes precisely nothing — and the compiler will not stop you." },
  ],

  /* ----------------------------------------------------------------- 00c */
  "00c-control-flow-and-methods": [
    { q: "Why does if (5) not compile in C#, when the equivalent is legal in C and JavaScript?",
      a: ["Because 5 is not a constant expression",
          "Because a condition must be a bool — C# does not treat numbers or objects as truth values",
          "Because the parentheses are missing",
          "It does compile; 5 is treated as true"],
      c: 1,
      why: "The same rule is why if (x = 5) does not compile either: assignment produces an int where a bool is required. That is a deliberate fix for a famous C bug." },

    { q: "Why prefer a switch expression over a long if / else if chain when selecting a value from an enum?",
      a: ["It runs measurably faster",
          "It is exhaustive — the compiler warns about cases you have not handled, which an if chain can never do",
          "It is the only form that can return a value",
          "if chains cannot be used with enums"],
      c: 1,
      why: "Exhaustiveness is the real prize. Add a member to the enum and the switch tells you where to go; the if chain silently falls through to its else." },

    { q: "A for loop should visit all three items of an array. Which condition is correct?",
      code: "string[] cities = { \"Modena\", \"Bologna\", \"Parma\" };\nfor (int i = 0; /* ??? */ ; i++)",
      a: ["i <= cities.Length", "i < cities.Length", "i < cities.Length - 1", "i <= cities.Count"],
      c: 1,
      why: "Indexes run 0 to Length - 1, so < is right and <= walks off the end into an IndexOutOfRangeException. Arrays also use Length, not Count." },

    { q: "What happens if you call list.Remove(x) inside a foreach loop over that same list?",
      a: ["The item is removed and the loop continues normally",
          "An InvalidOperationException is thrown at run time — the collection cannot be modified while it is being enumerated",
          "A compile error",
          "The loop silently skips the next item"],
      c: 1,
      why: "Collect what you want to change during the loop and act on it afterwards, or iterate a copy. The exception is at run time, so a test that never removes anything will not catch it." },

    { q: "In C#, what is the difference between a parameter and an argument?",
      a: ["They are two words for the same thing",
          "A parameter is the variable declared in the method signature; an argument is the actual value passed at the call site",
          "A parameter is optional and an argument is required",
          "Parameters belong to static methods and arguments to instance methods"],
      c: 1,
      why: "The distinction matters as soon as ref, out and pass-by-value come up, because all three are statements about how an argument reaches a parameter." },

    { q: "What does the => in static bool IsEven(int n) => n % 2 == 0; mean?",
      a: ["It declares a lambda that must be invoked separately",
          "It is an expression-bodied member — shorthand for a body that is a single expression, whose value is returned",
          "It makes the method asynchronous",
          "It marks the method as an extension method"],
      c: 1,
      why: "Identical to the braced form once compiled, and no return keyword appears because the expression's value is the return value. The same arrow means 'goes to' in a lambda and separates pattern from result in a switch expression." },

    { q: "Which is right: array.Count and list.Length, or array.Length and list.Count?",
      a: ["array.Count and list.Length", "array.Length and list.Count",
          "Both types expose both members", "Both use Size"],
      c: 1,
      why: "Arrays and strings use Length; List<T> and other collections use Count. There is no principle to derive it from — it is history — but mixing them up is a compile error rather than a bug." },

    { q: "Why do C# classes expose properties like { get; set; } rather than public fields?",
      a: ["Properties use less memory",
          "A property is a pair of methods behind field-like syntax, so validation or logging can be added later without any caller changing",
          "Fields cannot be made public in C#",
          "Properties are required for a class to compile"],
      c: 1,
      why: "It preserves your freedom to add a rule to set without breaking callers. Data binding and many serialisers also only look at properties." },
  ],

  /* ----------------------------------------------------------------- 00d */
  "00d-how-a-project-is-built": [
    { q: "Building one feature, why is the entity (Product.cs) written before the controller rather than after it?",
      a: ["Controllers are harder, so they are left until last",
          "The entity references nothing, so it is the only file that compiles on its own — everything else needs what is below it to exist first",
          "Entity Framework requires the entity to be the first file in the project",
          "It is a naming convention, with no technical reason"],
      c: 1,
      why: "The build order is the dependency order, backwards. ProductsController mentions the service, which mentions the DbContext, which mentions the entity; only the entity mentions nothing." },

    { q: "What is a DTO, and what is it for?",
      a: ["A copy of the entity used to make queries faster",
          "A separate type describing what may cross the boundary, so the storage shape and the public API can change independently",
          "A pattern required by Entity Framework for every table",
          "An interface the controller implements"],
      c: 1,
      why: "Data Transfer Object. Without one, adding a column to a table adds a field to your public JSON, and renaming a column breaks every client." },

    { q: "Name two things that go wrong when a controller returns the EF Core entity directly instead of a DTO.",
      a: ["It is slower to serialise, and it uses more memory",
          "Fields that were never meant to be public are exposed, and navigation properties can serialise in a cycle",
          "Entity Framework refuses to serialise entities at all",
          "The response is not valid JSON"],
      c: 1,
      why: "Over-sharing, over-posting, cycles through navigation properties, and coupling the API to the schema. Any two of those is a good interview answer." },

    { q: "Which of these belongs in the controller, and which belongs one file down in the service?",
      code: "// A: return dto is null ? NotFound() : Ok(dto);\n// B: if (order.Total > customer.CreditLimit) return Reject();",
      a: ["Both belong in the controller",
          "A belongs in the controller (it is a decision about HTTP); B belongs in the service (it is a business rule)",
          "Both belong in the service",
          "A belongs in the service; B belongs in the controller"],
      c: 1,
      why: "A controller may bind the request, call one thing, and choose a status code. An if about the business is the marker that logic has leaked upwards." },

    { q: "An application compiles cleanly but throws at start-up: \"Unable to resolve service for type 'IProductService' while attempting to activate 'ProductsController'\". What was skipped?",
      a: ["The migration was never applied to the database",
          "The service was never registered in Program.cs, so the container has no implementation to hand over",
          "The controller is missing the [ApiController] attribute",
          "IProductService needs to inherit from ControllerBase"],
      c: 1,
      why: "Of the four ways files connect in C#, DI registration is the only one the compiler does not check — which is why it fails at run time and why Program.cs is the first place to look." },

    { q: "Why is the DTO usually decided before the service is written, rather than after?",
      a: ["Because a DTO takes longer to write than a service",
          "Because it is the service's return type — the signature cannot be written until the shape it returns has been chosen",
          "Because Entity Framework generates the service from the DTO",
          "It is not; the DTO is always written last"],
      c: 1,
      why: "Each step in the nine exists because the next one needs it. The DTO is step 5 for the same reason the entity is step 2." },

    { q: "In a small project, what is the practical argument for folders named Products/ and Orders/ over folders named Controllers/, Services/ and Models/?",
      a: ["Feature folders produce smaller compiled assemblies",
          "A change is nearly always one feature and nearly never one kind, so feature folders keep everything a change touches in one place",
          "ASP.NET Core requires feature folders for routing to work",
          "There is no practical difference — it is purely cosmetic"],
      c: 1,
      why: "Organise by axis of change. The by-kind layout is clearer for the first two features and worse from about the tenth onwards." },

    { q: "You are given a fixed API contract by a mobile team before you have designed the database. Which build order fits, and does the result differ?",
      a: ["Outside-in — start from the DTO and the URL — and the finished files and their dependencies are the same either way",
          "Outside-in, which produces a fundamentally different architecture from inside-out",
          "Inside-out is the only correct order in every situation",
          "Test-first, because a fixed contract must be tested before it is written"],
      c: 0,
      why: "Inside-out, outside-in and test-first differ only in which end you push from. They converge on the same nine files with the same dependency direction." },
  ],

  /* ------------------------------------------------------------------ 00e */
  "00e-console-output-and-input": [
    { q: "What is the difference between `Console.Write` and `Console.WriteLine`?",
      a: ["Write is faster",
          "WriteLine appends a newline and moves the cursor to the next line; Write leaves the cursor where it is",
          "Write goes to the error stream",
          "Write cannot take a string"],
      c: 1,
      why: "Use Write for a prompt, so the user types their answer on the same line. A prompt written with WriteLine makes the program look broken before it has done anything wrong." },

    { q: "When does `Console.ReadLine()` return null?",
      a: ["When the user presses Enter on an empty line",
          "At end of input — Ctrl+Z on Windows, Ctrl+D on Unix, or the end of a redirected file",
          "Never; it returns an empty string",
          "When the input contains only spaces"],
      c: 1,
      why: "Empty string means the user pressed Enter; null means there will never be any more input. A prompt loop that only tests for empty string spins forever under redirected input." },

    { q: "Why use `int.TryParse` rather than `int.Parse` for something a user typed?",
      a: ["TryParse is faster",
          "A typo is expected, and expected failure should be a return value rather than an exception",
          "Parse does not exist for int",
          "TryParse handles larger numbers"],
      c: 1,
      why: "Parse is right for input you control — a constant, a value already validated. For a human at a keyboard, failure is the normal case, and exceptions are for the exceptional." },

    { q: "You call `int.TryParse(input, out int qty)` and ignore the return value. The user types `twelve`. What is qty?",
      a: ["Unassigned; the code will not compile",
          "0 — on failure the out parameter is set to default, which looks like a legitimate answer downstream",
          "null",
          "It throws a FormatException"],
      c: 1,
      why: "This is strictly worse than calling Parse, because at least Parse would have told somebody. Never call TryParse without using its result." },

    { q: "In `$\"{sku,-12}{qty,6:N0}\"`, what does the `-12` do?",
      a: ["Truncates the value to 12 characters",
          "Pads the value to a minimum width of 12, left-aligned; negative means left, positive means right",
          "Subtracts 12 from the value",
          "Sets 12 decimal places"],
      c: 1,
      why: "The width is a minimum, not a maximum — a longer value pushes the rest of the row out rather than being cut off, so choose widths from the longest value that can really occur." },

    { q: "Why write diagnostics to `Console.Error` rather than `Console.Out`?",
      a: ["It is faster",
          "They are separate streams, so a user redirecting results to a file still sees the errors on screen",
          "Console.Out is deprecated",
          "Error output is automatically logged"],
      c: 1,
      why: "It costs nothing and it is what makes a console program usable inside a script. Results to Out, diagnostics to Error." },

    { q: "What does the return value of a console program mean?",
      a: ["Nothing; it is ignored",
          "It is the process exit code: zero means success and anything else means failure, and it is the only thing a script or build server reads",
          "It is printed to the console",
          "It is the number of lines written"],
      c: 1,
      why: "A program that prints ERROR and returns 0 is treated by the build server as having succeeded. That is how a broken deployment goes green." },

    { q: "In `static void Main(string[] args)`, what is `args[0]`?",
      a: ["The program's own executable name",
          "The first real argument passed on the command line — unlike C, the program name is not included",
          "Always null",
          "The current directory"],
      c: 1,
      why: "A C and C++ convention C# deliberately does not follow. Check args.Length before indexing, and print a usage line when it is zero." },

    { q: "Why should a password or API key never be passed as a command-line argument?",
      a: ["Arguments have a length limit",
          "The full command line of a running process is visible to other users on the machine and lands in shell history",
          "Arguments cannot contain special characters",
          "The runtime encrypts them anyway"],
      c: 1,
      why: "Secrets come from environment variables or a secret store. This is a security bug, not a style preference." },

    { q: "`Console.ReadKey(intercept: true)` — what does the argument do?",
      a: ["Waits for Enter as well",
          "Stops the pressed character being echoed to the screen",
          "Intercepts Ctrl+C",
          "Returns immediately without waiting"],
      c: 1,
      why: "What you want for a menu, and what you must have for a password. Note the named argument: a bare `true` at that call site would tell a reader nothing." },

    { q: "Why use `char.ToUpperInvariant` rather than `char.ToUpper` when matching a menu key?",
      a: ["It is shorter to type",
          "ToUpper uses the machine's culture, and in Turkish the upper-case of `i` is not `I`, so culture-aware casing breaks identifier matching",
          "ToUpper only works on strings",
          "ToUpperInvariant handles digits"],
      c: 1,
      why: "For anything a human reads, culture-aware casing is right. For comparisons and identifiers, always invariant." },

    { q: "`decimal.Parse(\"1,5\")` on a machine set to Italian gives what, and on one set to English?",
      a: ["1.5 both times",
          "1.5 under it-IT and 15 under en-US, with no exception either way, because the comma is a thousands separator in English",
          "It throws under it-IT",
          "15 both times"],
      c: 1,
      why: "The rule: current culture for text a human typed, InvariantCulture for anything a machine wrote — a file, an API, a log — in both directions." },

    { q: "What is the correct shape of a console input validation loop?",
      a: ["Read once and hope",
          "Loop: prompt with Write, Trim and TryParse, range check, and a separate null check that exits rather than re-prompting",
          "try/catch around Parse, retrying on exception",
          "Read everything first, validate at the end"],
      c: 1,
      why: "Re-prompting on null input is an infinite loop when input is redirected from a file. The same three steps appear in a web API as model binding, a validation attribute and a 400 response." },

    { q: "Why call `.Trim()` before parsing user input?",
      a: ["It converts to lower case",
          "A trailing space is the most common reason a perfectly typed number fails to parse",
          "It removes the decimal point",
          "Parse requires it"],
      c: 1,
      why: "Trim before you parse, always. StringSplitOptions.TrimEntries does the same job when you are splitting." },

    { q: "`Console.WriteLine(\"Picked {0} of {1}\", a, b)` — when is this composite form still preferred over interpolation?",
      a: ["Never; it is obsolete",
          "In logging APIs, which want the template and the values separately so the fields can be indexed and searched",
          "When the values are numbers",
          "When there are more than three values"],
      c: 1,
      why: "ILogger deliberately kept it for exactly that reason. Everywhere else, interpolation is the default." },

    { q: "Why is `Console.WriteLine` the wrong way to trace what a web application is doing?",
      a: ["It is too slow",
          "In production nothing is attached to that stream, so the output goes nowhere anybody can search or filter",
          "It is not thread-safe",
          "It only works on Windows"],
      c: 1,
      why: "Real applications log through ILogger, which adds a level, a timestamp, a category and structured fields. Console output is for console programs." },

    { q: "`Console.Clear()` throws an IOException in which situation?",
      a: ["When the console is too small",
          "When output has been redirected, because there is no console to clear",
          "When called twice",
          "On Linux"],
      c: 1,
      why: "Guard it with Console.IsOutputRedirected, or leave it out. A program that crashes when its output is piped is a program that cannot be automated." },

    { q: "What does `string.IsNullOrWhiteSpace` catch that `string.IsNullOrEmpty` does not?",
      a: ["Nothing; they are the same",
          "A value made only of spaces or tabs, which is empty to a human and non-empty to IsNullOrEmpty",
          "A null value",
          "A value longer than the field"],
      c: 1,
      why: "For anything a person typed, the whitespace version is almost always the one you meant." },

    { q: "Why does `Console.Write(\"Name: \"); Console.ReadLine();` read better than using WriteLine for the prompt?",
      a: ["It uses less memory",
          "The cursor stays on the prompt line, so the user types their answer beside the question the way every command-line tool behaves",
          "ReadLine requires it",
          "WriteLine cannot be followed by ReadLine"],
      c: 1,
      why: "A tiny thing that makes a program feel finished or broken. It is also the first sign to a reviewer of whether somebody has written a console tool before." },

    { q: "A console validation loop and an ASP.NET controller share which three steps?",
      a: ["They share nothing",
          "Parse, range check, and report which field was wrong — model binding, a validation attribute and a 400 response are the same three with the prompting removed",
          "Authentication, authorisation, logging",
          "Read, write, flush"],
      c: 1,
      why: "Which is why learning validation at the console is worth the time: a developer who never wrote it tends to write the web version by trusting the input." },
  ],

  /* ------------------------------------------------------------------ 01 */
  "01-dotnet-platform": [
    { q: "What is the difference between the .NET SDK and the .NET runtime?",
      a: ["The SDK runs applications; the runtime builds them",
          "The SDK builds and runs (it contains the runtime, compiler and CLI); the runtime only executes an already-built application",
          "They are two names for the same download",
          "The SDK is for Windows, the runtime for Linux"],
      c: 1,
      why: "Developer machines and build agents need the SDK. A production container only needs the runtime — which is why the ASP.NET runtime image is used for the final stage of a Dockerfile and the SDK image is thrown away." },

    { q: "Your C# compiles to IL. What actually turns IL into machine code, and when?",
      a: ["The C# compiler, at build time",
          "The JIT compiler in the CLR, the first time each method runs",
          "The operating system loader, when the process starts",
          "MSBuild, during restore"],
      c: 1,
      why: "csc.exe produces IL in an assembly. The JIT compiles each method to native code on first call and caches it — which is why the first request to a freshly started API is slower than the second." },

    { q: "What is the single most important practical difference between .NET Framework 4.8 and .NET 5+ for a developer being asked to migrate?",
      a: [".NET 5+ is faster",
          ".NET Framework is Windows-only and shipped with the OS; .NET 5+ is cross-platform, side-by-side versioned and can be deployed self-contained",
          ".NET 5+ does not support C#",
          ".NET Framework has no garbage collector"],
      c: 1,
      why: "Windows-only and machine-wide versioning are the two things that block containerisation and independent deployment. That is the migration driver, not raw speed." },

    { q: "In a modern SDK-style .csproj, why is there no list of .cs files?",
      a: ["The list is stored in the .sln instead",
          "The SDK includes every .cs file under the project directory by default; you only write an entry to exclude one or add something unusual",
          "Visual Studio keeps the list in a hidden file",
          "Files are discovered at runtime"],
      c: 1,
      why: "Globbing by default is why an SDK-style project file is fifteen lines instead of six hundred, and why adding a file no longer produces a merge conflict in the project file." },

    { q: "What does <TargetFramework>net10.0</TargetFramework> actually control?",
      a: ["Which version of Visual Studio can open the project",
          "Which set of framework APIs the code compiles against, and therefore which runtime can execute it",
          "The version of C# only",
          "Nothing — it is documentation"],
      c: 1,
      why: "It is the API surface contract. The C# language version is a separate property (LangVersion), though it defaults from the target framework." },

    { q: "What is an assembly?",
      a: ["A single .cs source file",
          "A compiled .dll or .exe: the unit of deployment, versioning and type identity in .NET",
          "A namespace",
          "A NuGet package"],
      c: 1,
      why: "Type identity includes the assembly, which is why two identically-named types from different assemblies are different types — the cause of the classic “cannot convert MyType to MyType” error." },

    { q: "Select every command that would be part of a normal build-and-test loop from the CLI.",
      a: ["dotnet restore", "dotnet build", "dotnet test", "dotnet publish -c Release"],
      c: [0, 1, 2, 3],
      why: "All four are standard. restore and build are implicit in later commands now, but knowing they are separate stages is what lets you debug a restore failure that looks like a compile error." },

    { q: "A NuGet package you never referenced appears in your output folder. Why?",
      a: ["A bug in NuGet",
          "It is a transitive dependency — a package your package depends on",
          "Visual Studio added it automatically",
          "It was committed to source control"],
      c: 1,
      why: "Transitive dependencies are also why a security advisory can apply to code you never chose. Central Package Management with transitive pinning is how a solution gets to override one." },
  ],

  /* ------------------------------------------------------------------ 02 */
  "02-csharp-fundamentals": [
    { q: "What is the actual difference between a value type and a reference type in C#?",
      a: ["Value types live on the stack, reference types on the heap — always",
          "A variable of a value type holds the data itself; a variable of a reference type holds a reference to data elsewhere",
          "Value types cannot have methods",
          "Reference types are always slower"],
      c: 1,
      why: "The stack/heap answer is the one everybody gives and it is not reliable — a struct field inside a class lives on the heap, and a captured local is moved to the heap. Copy semantics versus reference semantics is the real distinction." },

    { q: "What does this print?",
      code: "var a = new int[] { 1, 2, 3 };\nvar b = a;\nb[0] = 99;\nConsole.WriteLine(a[0]);",
      a: ["1", "99", "0", "It does not compile"],
      c: 1,
      why: "Arrays are reference types even when the elements are value types. `b` and `a` point at the same array object." },

    { q: "Why does string concatenation in a loop allocate so much?",
      a: ["Strings are stored on the stack",
          "Strings are immutable, so each += creates a whole new string and abandons the previous one",
          "The garbage collector runs on every concatenation",
          "It does not — the compiler optimises it away"],
      c: 1,
      why: "This is the canonical StringBuilder question. Immutability is also why strings are safe to share across threads and safe as dictionary keys." },

    { q: "With nullable reference types enabled, what does `string?` mean?",
      a: ["The variable is stored differently in memory",
          "It is a compile-time annotation saying null is expected here, so the compiler warns when you dereference it unchecked",
          "The runtime will throw if it is ever null",
          "It makes the string a value type"],
      c: 1,
      why: "Nullable reference types are entirely a compile-time device — there is no runtime representation. That is why a null can still arrive from an unannotated library or from deserialisation." },

    { q: "What is the difference between `const` and `readonly`?",
      a: ["None",
          "const is baked into the calling assembly at compile time; readonly is assigned at runtime and can differ per instance",
          "readonly can only be used on strings",
          "const fields can be changed in the constructor"],
      c: 1,
      why: "The compile-time baking matters: change a public const in a library and every consumer must be recompiled, or they keep the old value. That is a real versioning bug." },

    { q: "What does the `record` keyword give you that `class` does not, by default?",
      a: ["Automatic database persistence",
          "Value-based equality, a ToString that prints the members, and `with` expressions for non-destructive mutation",
          "Thread safety",
          "Immutability that the runtime enforces"],
      c: 1,
      why: "Note what it does NOT give you: a positional record's properties are init-only, not deeply immutable — a record holding a List can still have that list mutated." },

    { q: "Select every expression that is TRUE for `string.Empty` compared with `null`.",
      a: ["\"\".Length is 0, null.Length throws",
          "string.IsNullOrEmpty covers both",
          "\"\" == null evaluates to false",
          "Both are reference values that point at nothing"],
      c: [0, 1, 2],
      why: "The last is wrong: string.Empty is a real, interned string object with a length of zero. Confusing the two is the source of a great many NullReferenceExceptions." },

    { q: "In `if (obj is Order { Total: > 100 } o)`, what has happened?",
      a: ["A cast that throws if obj is not an Order",
          "A type test, a property pattern and a declaration of `o` in one expression — and `o` is only in scope where the test passed",
          "A LINQ query",
          "A null check only"],
      c: 1,
      why: "Pattern matching replaces the is/cast/null-check trio. The scoping is the part people miss: `o` is definitely assigned only on the true branch." },
  ],

  /* ----------------------------------------------------------------- 02b */
  "02b-the-properties-of-data": [
    { q: "A variable of a reference type holds what, exactly?",
      a: ["The object itself, inline",
          "The address of the object — so assigning the variable copies the address, and both names then refer to one object",
          "A copy of the object that is synchronised with the original",
          "A weak handle the garbage collector may clear at any time"],
      c: 1,
      why: "Everything else about reference types follows from this one sentence: aliasing, why a method can change your object, and why null is possible." },

    { q: "Why is 'value types live on the stack' a risky thing to say in an interview?",
      a: ["It is completely false",
          "It is true often enough to pass but wrong in real cases — an int field of a class lives on the heap inside that object, and a captured local lives on the heap too",
          "Value types never live on the stack",
          "The stack does not exist in .NET"],
      c: 1,
      why: "The statement that is true everywhere is about copying: value types are copied by value, reference types by reference. That is also what the behaviour actually follows from." },

    { q: "`long ms = 1000 * 60 * 60 * 24 * 365;` produces a wrong number. Why?",
      a: ["long cannot hold a number that large",
          "Every literal is an int, so the multiplication is done in int and overflows before the result is widened into the long",
          "The compiler refuses to multiply five numbers",
          "Multiplication is evaluated right to left"],
      c: 1,
      why: "The declared type of the destination never reaches back into the expression. Put the L suffix on the first operand, not on the variable." },

    { q: "What does C# do by default when an int calculation exceeds int.MaxValue?",
      a: ["Throws OverflowException",
          "Wraps round silently to the negative end of the range, with no error and no warning",
          "Promotes the result to long automatically",
          "Saturates at int.MaxValue"],
      c: 1,
      why: "Unchecked is the default, for speed. `checked(expr)` or a project-wide switch turns the wrap into an exception, which is what you want anywhere money or quantities are involved." },

    { q: "A DateTime property that nobody assigned reads as what?",
      a: ["null",
          "DateTime.MinValue — the year 0001, a real date that sorts first and looks like corrupt data",
          "DateTime.Now at the moment the object was created",
          "The Unix epoch, 1970-01-01"],
      c: 1,
      why: "DateTime is a value type, so it cannot be null. That is exactly why a date that may be missing should be declared DateTime? instead." },

    { q: "Why is a default value more dangerous than an exception?",
      a: ["It is not — exceptions are always worse",
          "Because the default is usually a plausible answer: 0 reads as a real quantity and false reads as a decision somebody made, so nothing anywhere reports that nobody answered",
          "Because defaults are slower",
          "Because defaults cannot be overridden"],
      c: 1,
      why: "This is the whole argument for nullable types in a domain model: null cannot be mistaken for data, and 0 can." },

    { q: "`name.ToUpper();` on its own line does what?",
      a: ["Uppercases name in place",
          "Nothing — strings are immutable, so the method builds a new string and this line discards it",
          "Throws, because the result is unused",
          "Uppercases name only if it is not null"],
      c: 1,
      why: "A statement whose whole body is a method call on a string is dead code. The compiler does not consider it an error; the CA1806 analyser rule does." },

    { q: "Why does concatenating strings in a long loop perform badly?",
      a: ["String concatenation calls the database",
          "Each += builds an entirely new string and discards the previous one, so a ten-thousand-iteration loop allocates ten thousand strings",
          "The garbage collector is disabled during loops",
          "It does not — it is the fastest option"],
      c: 1,
      why: "StringBuilder is a mutable buffer, so it grows one allocation instead of one per iteration. Immutability is the reason the naive loop is slow." },

    { q: "Two Order objects have identical property values. What does `a == b` return, and why?",
      a: ["True — C# compares contents by default",
          "False — the default == for a class compares references, and these are two different objects",
          "True, but only if Order has no fields",
          "It does not compile without an explicit cast"],
      c: 1,
      why: "That default is correct: two different orders are two orders. A record is the type whose generated == compares every field instead, which is why DTOs and value objects should be records." },

    { q: "`object x = 5; object y = 5;` — what do `x == y` and `x.Equals(y)` return?",
      a: ["True and True",
          "False and True — == is chosen at compile time from the declared object type, so it compares references to two separate boxes, while Equals is virtual and compares the numbers",
          "True and False",
          "False and False"],
      c: 1,
      why: "Boxing plus the operator/method distinction in four lines. Change object to int and both print True, because a different == is selected." },

    { q: "You override Equals on a class but not GetHashCode. What breaks?",
      a: ["Nothing — GetHashCode is optional",
          "Dictionary and HashSet lookups: the hash picks a bucket before Equals is ever called, so two equal objects that hash differently land in different buckets and a lookup reports a miss",
          "The == operator stops working entirely",
          "Serialisation throws"],
      c: 1,
      why: "The cruellest part is that a direct comparison in the debugger keeps saying they are equal. Equal objects must return equal hashes — or use a record, which generates both." },

    { q: "When comparing two order codes, which StringComparison should you reach for?",
      a: ["CurrentCulture, because it respects the user's locale",
          "Ordinal or OrdinalIgnoreCase, because codes are machine data and culture-aware comparison can genuinely disagree between machines",
          "InvariantCulture, always",
          "It makes no difference"],
      c: 1,
      why: "Culture-aware comparison is for text a human will read and sort. Identifiers, file names, protocol tokens and codes are ordinal." },

    { q: "In .NET, when is an object eligible for collection?",
      a: ["As soon as the variable goes out of scope",
          "When nothing can still reach it from a root — a local, a static or a field of something reachable",
          "When you set it to null",
          "Immediately after its last method call returns"],
      c: 1,
      why: "Alive means reachable, not 'in use'. A managed leak is not lost memory: it is memory something can still get to, which is why a forgotten event subscription leaks." },

    { q: "Why does `+=` on an event risk a memory leak?",
      a: ["Events copy the subscriber object",
          "The subscription creates a reference from the publisher to the subscriber, so a long-lived publisher keeps a short-lived subscriber alive indefinitely",
          "Events are never collected in .NET",
          "It does not — events are weak references by default"],
      c: 1,
      why: "The direction of the reference is what people miss. The rule is symmetry: whoever subscribes, unsubscribes — usually in Dispose." },

    { q: "What is `using` for, given that memory is already the garbage collector's job?",
      a: ["It frees memory sooner",
          "It guarantees Dispose runs at the closing brace, including on an exception — for resources that are somebody else's scarce handle, like a connection, a file or a socket, where 'eventually' is not good enough",
          "It prevents the object being collected",
          "It is only for performance"],
      c: 1,
      why: "This is the deterministic half of lifetime. The collector handles memory; using handles everything that is not memory." },

    { q: "Which of these are value types in C#? Select every one that applies.",
      a: ["int",
          "string",
          "DateTime",
          "a struct you declared"],
      c: [0, 2, 3],
      why: "string is a reference type that behaves like a value only because it is immutable and overloads ==. struct, enum, the built-in numerics, bool, char and DateTime are the value types." },

    { q: "`double euros = 5997 / 100;` prints 59 rather than 59.97. What happened?",
      a: ["double cannot store 59.97",
          "Both operands are int, so integer division discards the fraction before the result is widened into the double",
          "WriteLine rounds down by default",
          "The compiler optimised the division away"],
      c: 1,
      why: "A cast or a wider destination on the outside cannot fix arithmetic on the inside. And the right fix here is decimal with 100m, because this is money." },

    { q: "What does `shipped?.Year` evaluate to when `shipped` is a null DateTime?, and what is its type?",
      a: ["Throws NullReferenceException; type int",
          "null, and the type is int? — the null-conditional operator makes the whole expression nullable even though Year is an int",
          "0, and the type is int",
          "It does not compile"],
      c: 1,
      why: "That type change is what forces the ?? 0 you so often see on the end of these chains. It is the compiler making the missing case impossible to ignore." },

    { q: "Why does `if (flag)` fail to compile when flag is a bool?",
      a: ["Nullable types cannot be used in conditions at all",
          "Because there is no sensible branch for null — a third state exists, and if has only two",
          "Because bool? is not a real type",
          "It compiles fine"],
      c: 1,
      why: "Writing `flag == true` compiles and silently treats unknown as no. Sometimes that is right; decide it deliberately, because == false and !(== true) then ask different questions." },

    { q: "Which property is the bug in: 'I copied the list into another variable, changed the copy, and the original changed too'?",
      a: ["Mutability only",
          "Location and mutability together — the assignment copied a reference rather than the list, and List is mutable so the change was visible through both names",
          "Equality",
          "Lifetime"],
      c: 1,
      why: "Both properties have to line up for the surprise. Make it a struct and it disappears; make the collection immutable and it disappears. That is why naming the properties separately is useful." },
  ],

  /* ----------------------------------------------------------------- 02c */
  "02c-wildcards-and-wordplay": [
    { q: "In `int.TryParse(text, out _)`, what is the underscore?",
      a: ["A variable named underscore",
          "A discard — it satisfies the out parameter without creating a variable, so nothing is left in scope to misuse",
          "A null literal",
          "A placeholder the compiler fills with 0"],
      c: 1,
      why: "It is the 'I do not care' wildcard, and its value is that the compiler keeps the promise: you cannot accidentally use it afterwards." },

    { q: "`private readonly ILogger _logger;` — is that leading underscore a discard?",
      a: ["Yes, it discards the field",
          "No — an identifier that starts with an underscore is an ordinary name, and the leading underscore is only a convention marking a private field",
          "Yes, but only for readonly fields",
          "It makes the field compiler-generated"],
      c: 1,
      why: "The compiler attaches no meaning to it whatsoever. This is the fifth and most confusing use of the character, precisely because it is not a language feature." },

    { q: "In a list pattern, what is the difference between `_` and `..`?",
      a: ["They are synonyms",
          "`_` matches exactly one element and `..` matches any number, including none",
          "`_` matches any number and `..` matches exactly one",
          "`..` is only valid in ranges, not in patterns"],
      c: 1,
      why: "So `codes is _, _, _` means exactly three items, and `codes is 1, ..` means starts with 1. Only one slice pattern is allowed per list pattern, for the same reason a glob cannot have two stars and stay unambiguous." },

    { q: "Why must the `_` arm come last in a switch expression?",
      a: ["Style convention only",
          "Arms are tried top to bottom and the discard pattern matches everything, so anything after it is unreachable — which is a compile error",
          "Because the compiler sorts them anyway",
          "It does not have to; order is irrelevant"],
      c: 1,
      why: "The same rule kills a `var x` arm placed first, since the var pattern also matches everything, including null. Specific patterns above general ones, always." },

    { q: "You are switching over your own enum. Why might adding a `_` arm be a bad idea?",
      a: ["It is slower",
          "Without it the compiler warns when a new enum member is added and nobody handled it — adding the catch-all silences that warning forever",
          "Discard patterns are not allowed over enums",
          "It makes the switch non-exhaustive"],
      c: 1,
      why: "You are choosing between a runtime surprise and a compile-time reminder. On unbounded input like object a catch-all is right; over your own enum, think twice." },

    { q: "What does the `!` in `customer!.Name` do at run time?",
      a: ["Throws if customer is null",
          "Nothing at all — it is the null-forgiving operator and only tells the compiler to stop warning",
          "Converts null to a default instance",
          "Performs a null check and skips the call"],
      c: 1,
      why: "If you were wrong you get exactly the same NullReferenceException, minus the warning that predicted it. Treat every one in a review as a claim needing evidence." },

    { q: "`string s = null; Console.WriteLine(s + \"x\");` does what?",
      a: ["Throws NullReferenceException",
          "Prints x — concatenation treats a null string as empty",
          "Prints nullx",
          "Does not compile"],
      c: 1,
      why: "Being an operand of + is safe; calling a member on null is not. That asymmetry is why string.IsNullOrEmpty gets used everywhere." },

    { q: "`\"\" ?? \"missing\"` evaluates to what?",
      a: ["missing",
          "The empty string — an empty string is not null, so ?? passes it straight through",
          "null",
          "It does not compile"],
      c: 1,
      why: "The single most common misunderstanding of the operator. When empty should count as missing, say so with IsNullOrWhiteSpace." },

    { q: "What is the difference between `const` and `readonly`?",
      a: ["None, they are synonyms",
          "const is resolved at compile time and substituted into every caller, so a library changing it leaves already-compiled callers on the old value; readonly is assigned at run time in the constructor and then frozen",
          "readonly can only be used on value types",
          "const fields can be changed by reflection and readonly ones cannot"],
      c: 1,
      why: "Use const only for things true by definition, like minutes in an hour, and static readonly for everything else. Optional parameter defaults have the identical versioning trap." },

    { q: "A `readonly List` field — what exactly cannot change?",
      a: ["Nothing about the list can change",
          "The field cannot be pointed at a different list; the list itself can still be added to and cleared",
          "The list contents are frozen but the field can be reassigned",
          "Both the field and the contents are frozen"],
      c: 1,
      why: "readonly freezes the reference, not the object behind it. Immutability is not transitive by default — the same hole a record holding a List has." },

    { q: "What is the difference between `throw;` and `throw ex;` inside a catch block?",
      a: ["None",
          "`throw;` rethrows keeping the original stack trace; `throw ex;` resets the origin to that line, so the log claims the error started in your catch block",
          "`throw ex;` is faster",
          "`throw;` swallows the exception"],
      c: 1,
      why: "A one-word difference that destroys the single most useful piece of evidence you have. Bare throw, always — or wrap in a new exception with the original as InnerException." },

    { q: "`ref`, `out` and `in` differ in what, precisely?",
      a: ["Only in performance",
          "In who must have assigned the variable and what the method may do: ref needs a value already and may read and write, out need not and must write before returning, in must have a value and may only be read",
          "ref is for value types and out is for reference types",
          "in is a synonym for ref"],
      c: 1,
      why: "All three let the method operate on the caller's variable. out is the Try pattern; ref is rare enough to be a design smell; in exists to avoid copying a large struct." },

    { q: "A user types `50%` into a search box and the query uses `LIKE @t`, parameterised. Is there a problem?",
      a: ["Yes — SQL injection",
          "Yes, but not injection: % and _ are LIKE wildcards, so the search matches far more rows than asked for, and a bare % scans the whole table",
          "No, parameterisation handles everything",
          "No, LIKE ignores special characters in parameters"],
      c: 1,
      why: "Parameterising prevents injection; escaping prevents this. They are different defences and you need both — escape %, _ and the bracket, with LIKE ... ESCAPE." },

    { q: "In a C# file glob and in SQL LIKE, what plays the role of 'any run of characters'?",
      a: ["* in both",
          "* in a glob, % in LIKE — and in LIKE the single-character wildcard is the underscore",
          "% in both",
          "? in a glob, * in LIKE"],
      c: 1,
      why: "Three text-matching systems with three different alphabets: glob uses * and ?, LIKE uses % and _, regex uses .* and dot. Mixing them up is the most common cause of a filter that quietly matches nothing." },

    { q: "In `items^1` written as an index from the end, what does the caret mean — and what is `items^0`?",
      a: ["It means exponent; ^0 is 1",
          "It means 'from the end', so ^1 is the last element and ^0 is one past the end, which throws",
          "It means 'from the end', and ^0 is the last element",
          "It is a bitwise XOR on the index"],
      c: 1,
      why: "Read it as 'length minus this'. And note the same character means 'start of string' in a regex, which is exactly the kind of collision this chapter exists for." },

    { q: "`items1..3` as a range expression gives you how many elements?",
      a: ["Three",
          "Two — the start is inclusive and the end is exclusive, matching Substring and every other .NET convention",
          "Four",
          "It depends on the array length"],
      c: 1,
      why: "Read it as 'from 1, up to but not including 3'. Off-by-one errors here are expected the first few times." },

    { q: "What does `@` do in front of a string literal?",
      a: ["Marks it as interpolated",
          "Makes it verbatim: backslashes are literal and newlines are allowed, which is why paths and regexes almost always carry one",
          "Marks it as a raw string with no escaping at all",
          "Turns it into a byte array"],
      c: 1,
      why: "The $ prefix is interpolation and the two can be combined. And @ in front of an identifier is a completely different job: 'this word is a name, not a keyword'." },

    { q: "What is the difference between `First()` and `FirstOrDefault()`?",
      a: ["Performance only",
          "First throws when there is nothing, FirstOrDefault returns the default — which for a class is null, and a silent null is how a NullReferenceException appears three methods away",
          "FirstOrDefault is only for value types",
          "They are identical"],
      c: 1,
      why: "Choose by intent: 'there is one and I want to hear about it if not' versus 'there may not be and I will handle that'. Neither is safer in general." },

    { q: "Why is `virtual`/`override` preferred over `new` for a method in a derived class?",
      a: ["new is not valid C#",
          "override replaces the implementation so the call goes to the derived version whatever the variable is declared as; new only hides it, so which method runs depends on the declared type of the variable rather than the object",
          "new is slower",
          "override cannot be used with interfaces"],
      c: 1,
      why: "One object can then give two different answers depending on which variable you asked through. That is the entire argument against new." },

    { q: "In Italian technical conversation, what does 'eventualmente' mean?",
      a: ["Eventually, in the end",
          "If it turns out to be necessary",
          "Frequently",
          "Immediately"],
      c: 1,
      why: "A classic false friend, alongside attualmente for 'currently' and controllare for 'to check'. Getting one wrong mid-answer derails a sentence you otherwise had." },
  ],

  /* ----------------------------------------------------------------- 02d */
  "02d-predict-the-output": [
    { q: "What does `Console.WriteLine((double)(5 / 2));` print?",
      a: ["2.5",
          "2",
          "2.0",
          "It does not compile"],
      c: 1,
      why: "The brackets decide everything: the int division happens first and loses the fraction, then the cast widens the answer 2. Converting after the loss does not undo the loss." },

    { q: "`Console.WriteLine(0.1 + 0.2 == 0.3);` prints what, and why?",
      a: ["True — decimals are exact",
          "False — double is binary floating point and 0.1 has no exact binary form, so both inputs are already slightly wrong before the addition",
          "False, because == is never allowed on doubles",
          "It throws"],
      c: 1,
      why: "Every language with IEEE 754 doubles does this. Compare doubles against a tolerance, and use decimal for money, where 0.1m + 0.2m == 0.3m is True." },

    { q: "What does `-7 % 3` evaluate to in C#?",
      a: ["2",
          "-1 — the result takes the sign of the left operand",
          "1",
          "-2"],
      c: 1,
      why: "It follows from the requirement that (a / b) * b + a % b == a. The practical bite is wrapping a circular index: use ((i % n) + n) % n." },

    { q: "`int i = 5; Console.WriteLine(i++); Console.WriteLine(i);` prints what?",
      a: ["6 then 6",
          "5 then 6 — post-increment yields the old value and increments afterwards",
          "6 then 5",
          "5 then 5"],
      c: 1,
      why: "As a statement on its own, i++ and ++i are identical. Inside a larger expression neither belongs, because nobody should have to work out what it means." },

    { q: "`int big = 2_000_000_000; Console.WriteLine((long)(big + big));` prints what?",
      a: ["4000000000",
          "A negative number — the addition overflows in int first and the wrong answer is then widened",
          "OverflowException",
          "2000000000"],
      c: 1,
      why: "Writing (long)big + big instead gives the right answer, because the cast makes the left operand long and the right is promoted to match. Widen an operand, not the result." },

    { q: "`string s = \"hello\"; s.ToUpper(); Console.WriteLine(s);` prints what?",
      a: ["HELLO",
          "hello — strings are immutable, so ToUpper returns a new string and this line discards it",
          "An empty line",
          "It does not compile"],
      c: 1,
      why: "A statement that is nothing but a method call on a string is dead code, and the compiler does not consider it an error." },

    { q: "`ReferenceEquals(\"MI-42\", \"MI-42\")` with both as literals in the same assembly returns what?",
      a: ["False — they are two objects",
          "True — identical string literals are interned, so the runtime keeps one copy and hands out the same reference",
          "It does not compile",
          "It depends on the string length"],
      c: 1,
      why: "Never program against it: build the same text at run time from variables and it is a different object. Interning is an optimisation, not a guarantee." },

    { q: "Removing an item from a List inside a `foreach` over that same list does what?",
      a: ["Works correctly",
          "Throws InvalidOperationException on the next iteration, because the enumerator recorded the list's version when it started",
          "Silently skips the next item",
          "Throws immediately at the Remove call"],
      c: 1,
      why: "It throws on the NEXT iteration, which is why removing the last item can appear to work — a bug that depends on the data. Use RemoveAll, or rebuild with LINQ, or loop backwards by index." },

    { q: "`d[\"zeta\"] = 1;` on a Dictionary that already has that key does what, compared with `d.Add(\"zeta\", 1)`?",
      a: ["Both throw on a duplicate key",
          "The indexer replaces silently; Add throws on a duplicate key",
          "The indexer throws; Add replaces",
          "Both replace silently"],
      c: 1,
      why: "Writing is forgiving and reading is not — an absent key read through the indexer throws KeyNotFoundException. TryGetValue is the forgiving read." },

    { q: "Calling `.Count()` twice on the same un-materialised LINQ query does what?",
      a: ["Uses a cached result the second time",
          "Enumerates the source twice, running the whole pipeline again",
          "Throws on the second call",
          "Returns 0 the second time"],
      c: 1,
      why: "A query is a recipe, not a result, and asking twice cooks twice. Harmless on a three-item list; two database round trips behind EF Core. ToList once, then reuse." },

    { q: "A List of a mutable struct. You write `var item = list0; item.X = 99;` — what is list0.X afterwards?",
      a: ["99",
          "Unchanged, because the indexer returned a copy of the struct and you modified the copy",
          "It does not compile",
          "It throws at run time"],
      c: 1,
      why: "Change struct to class and the same code prints 99, with the compiler silent in both directions. This is the strongest argument for making structs immutable." },

    { q: "A method takes an Order parameter and assigns a brand-new Order to it. What does the caller see?",
      a: ["The new order",
          "Nothing changed — the parameter is the method's own copy of the address, so reassigning it does not touch the caller's variable",
          "A NullReferenceException",
          "The old order, but with the new order's values copied in"],
      c: 1,
      why: "C# passes everything by value; for a reference type, the thing passed by value is the reference. Following it changes the shared object; replacing it does not. `ref` is what makes reassignment visible." },

    { q: "A record holds a List. You use `with` to make a modified copy and then add to the copy's list. What happens to the original's list?",
      a: ["Nothing — with makes a deep copy",
          "It grows too, because `with` copies shallowly and both records hold the same list reference",
          "The original record is frozen, so it throws",
          "The original's list is replaced with an empty one"],
      c: 1,
      why: "Immutability is not transitive by default. A record of records is genuinely safe; a record holding a List is a promise with a hole in it." },

    { q: "Three lambdas created in a `for` loop over 0..2 and invoked afterwards print what?",
      a: ["0 1 2",
          "3 3 3 — they capture the variable, not its value, and a for loop has one loop variable shared by every iteration",
          "2 2 2",
          "Nothing — the lambdas are collected"],
      c: 1,
      why: "Copy into a fresh variable inside the loop body and capture that. foreach does not have the bug, because its iteration variable is new each pass — changed in C# 5 for exactly this reason." },

    { q: "`Side(\"a\") > 0 || Side(\"b\") > 0` where Side prints its argument. What is printed?",
      a: ["ab",
          "a only — || short-circuits, so the right side is never evaluated once the left is true",
          "b only",
          "Nothing"],
      c: 1,
      why: "The single-pipe | evaluates both. Which is why writing & instead of && in a null guard is a real bug with a stack trace, not a style preference." },

    { q: "A method whose try block returns 1 and whose finally block prints something — what happens first?",
      a: ["The return, then nothing; finally is skipped on return",
          "The return value is computed, then finally runs, and only then does control leave the method",
          "finally runs before the try block",
          "The compiler rejects a return inside try with a finally"],
      c: 1,
      why: "This is exactly what `using` compiles into, which is why a resource is still disposed when you return from inside the block. A return inside finally would replace the value and swallow exceptions, which is why it is forbidden." },

    { q: "`string s = \"\"; for (int i = 0; i < 3; i++) s += i;` — what is s and how many strings were allocated?",
      a: ["The number 3, one allocation",
          "The text 012, and three strings were built and discarded along the way",
          "The text 0 1 2, one allocation",
          "It does not compile"],
      c: 1,
      why: "An int meeting a string under + becomes text, not a sum. The answer is right and the method is wrong — nothing in the output tells you the loop is quadratic." },

    { q: "`var b = a;` and `var c = new List(a);` on a List — what is the difference?",
      a: ["None",
          "b is an alias for the same list; c is a new list holding the same items — and it is a shallow copy, so with reference types both lists point at the same objects",
          "b is a copy and c is an alias",
          "c performs a deep copy of every item"],
      c: 1,
      why: "Copying the container is not copying the contents. That distinction is what makes 'I copied it and it still changed' possible." },

    { q: "What is the most useful way to practise this chapter a second time?",
      a: ["Memorise the outputs",
          "Change exactly one token in each — struct to class, for to foreach, double to decimal — and predict the new output before running",
          "Read the explanations again",
          "Write the same programs in another language"],
      c: 1,
      why: "The second pass is where these stop being trivia: you are no longer remembering answers, you are deriving them from a model of what the language does." },

    { q: "Why say your prediction out loud before reading the explanation?",
      a: ["It helps with pronunciation for the interview",
          "Because an unspoken prediction gets silently revised while reading, which feels like learning and teaches nothing",
          "It makes no difference",
          "Because the answers are ordered by difficulty"],
      c: 1,
      why: "Retrieval that costs something is what makes a fact stick. It is the same principle the spaced-repetition engine on this site is built on." },
  ],

  /* ----------------------------------------------------------------- 02e */
  "02e-functions-and-the-rest-of-the-syntax": [
    { q: "In a method signature, what does `static` tell you?",
      a: ["The method cannot be changed",
          "The method belongs to the type rather than to an instance, so there is no `this` inside it and callers do not need an object",
          "The method is thread-safe",
          "The method is cached after the first call"],
      c: 1,
      why: "Static is right for pure helpers. The moment a method depends on an injected dependency it must be an instance method, or that dependency becomes global state and the code stops being testable." },

    { q: "Is `async` part of the signature a caller sees?",
      a: ["Yes, callers must also be async",
          "No — callers see the return type, typically a Task; async is only permission to use await in the body, and can be removed without breaking anyone",
          "Yes, it changes the method's name at compile time",
          "Only for public methods"],
      c: 1,
      why: "That is why you can return a task directly rather than awaiting and returning it, and why async void is a separate, much worse thing." },

    { q: "What makes two methods overloads of each other?",
      a: ["Different return types",
          "The same name with different parameter lists — the return type is not part of it, so two methods differing only in what they return will not compile",
          "The same name in different classes",
          "Different access modifiers"],
      c: 1,
      why: "Overload when the methods genuinely do the same thing to different inputs. When they do different things, give them different names." },

    { q: "`void Show(object o)` and `void Show(string s)` both exist. `object o = \"hello\"; Show(o);` calls which?",
      a: ["The string overload, because the object is a string",
          "The object overload — overload resolution happens at compile time from declared types, and the compiler only knows object",
          "It is ambiguous and does not compile",
          "The string overload, but only in release builds"],
      c: 1,
      why: "Overloads are chosen by declared type; overrides by actual type. That one sentence also explains virtual versus new, and boxed-int equality." },

    { q: "A library method has `int retries = 3` as an optional parameter. The library ships a new version with the default changed to 5. A caller compiled against the old version does what?",
      a: ["Retries 5 times",
          "Retries 3 times — the default was substituted into the call site at compile time and the caller was not rebuilt",
          "Throws a version mismatch",
          "Retries 3 times, but logs a warning"],
      c: 1,
      why: "The same versioning trap const has. Across an assembly boundary, use an overload instead so the default lives in the library's own code." },

    { q: "What does `out var n` inside an argument list do?",
      a: ["Discards the out value",
          "Declares the variable right there, and it stays in scope after the enclosing statement",
          "Creates a copy that the method cannot write to",
          "Is only valid inside an if condition"],
      c: 1,
      why: "Before C# 7 this needed a separate declaration above. The scope is what makes the Try pattern read well: test and use in two adjacent lines." },

    { q: "`int Sum(params int[] n)` called as `Sum()` gives what?",
      a: ["A NullReferenceException",
          "0, because params with no arguments produces an empty array rather than null",
          "A compile error",
          "The default value of int[]"],
      c: 1,
      why: "params must be the last parameter and there can only be one, which is the same uniqueness rule a slice pattern has, for the same reason." },

    { q: "Why is a named argument worth writing at a call site?",
      a: ["It is faster",
          "It makes an otherwise unreadable argument readable — Ship(\"MI-42\", express: true) says what the boolean means, and Ship(\"MI-42\", true) says nothing",
          "It is required for optional parameters",
          "It allows the method to be overloaded"],
      c: 1,
      why: "Named arguments may also be given out of order, and can be mixed with positional ones as long as the positional ones come first." },

    { q: "What is an extension method, mechanically?",
      a: ["A method injected into the original class at run time",
          "A static method in a static class whose first parameter is marked `this` — the compiler rewrites the instance-style call back into the plain static call",
          "A partial class member",
          "A method defined on an interface with a default body"],
      c: 1,
      why: "Nothing is added to the original type, which is why it works on types you do not own. LINQ is entirely extension methods on IEnumerable." },

    { q: "Why do `Where` and `Select` sometimes 'not exist' on a collection?",
      a: ["The collection is the wrong type",
          "The namespace holding the extension methods is not imported — without `using System.Linq;` they are invisible",
          "They require a NuGet package",
          "They only work on List"],
      c: 1,
      why: "Extension methods are found by namespace, not by type. The other consequence: a real instance method of the same name always wins over an extension." },

    { q: "What happens when you call an iterator method containing `yield return`?",
      a: ["The whole body runs and a list is returned",
          "None of the body runs — the call only builds a state machine, and the body executes lazily as items are requested",
          "The first item is computed eagerly, the rest lazily",
          "It throws unless the result is assigned"],
      c: 1,
      why: "Which is why an exception thrown at the top of an iterator surfaces at the foreach rather than at the call, in a line containing no obvious call." },

    { q: "Where should argument validation in an iterator method live?",
      a: ["At the top of the iterator, as usual",
          "In a non-iterator wrapper method that checks its arguments and then returns a private iterator, so the exception is thrown at the call rather than at whatever line happens to enumerate",
          "In the caller",
          "Nowhere — iterators cannot be validated"],
      c: 1,
      why: "Otherwise ArgumentNullException arrives at a foreach, possibly in a different class, with nothing in the stack pointing at the real mistake." },

    { q: "What is the real advantage of returning IEnumerable with yield return over returning a List, when reading a huge file?",
      a: ["It is easier to write",
          "Memory stays flat, because only one line exists at a time rather than the whole file being held in RAM",
          "It is type-safe and List is not",
          "The file is closed automatically"],
      c: 1,
      why: "That is the payoff of laziness. The cost is that the sequence can be enumerated twice by accident, which reruns everything." },

    { q: "What does `static` on a local function mean?",
      a: ["The function is shared between calls",
          "It may not capture anything from the enclosing method, so it depends only on its arguments — and it avoids a hidden allocation",
          "It makes the function public",
          "It is not valid on local functions"],
      c: 1,
      why: "Write it whenever it compiles. It documents the dependency and stops a future edit quietly reaching into the enclosing scope." },

    { q: "`public int Count => _lines.Count;` — what is this?",
      a: ["A field initialised once",
          "An expression-bodied property: a getter that runs every time it is read and stores nothing",
          "A method named Count",
          "A constant"],
      c: 1,
      why: "The arrow is the tell. A property with => is computed; one written get; set; has a hidden backing field behind it." },

    { q: "What do `required` and `init` do together on a property?",
      a: ["Nothing — they are redundant",
          "init allows assignment in an object initialiser and never again; required forces the caller to supply it — so together they give 'must be set, cannot change' with no constructor",
          "They make the property thread-safe",
          "They mark the property for serialisation"],
      c: 1,
      why: "This is also the honest alternative to sprinkling the null-forgiving ! on properties the compiler cannot see being assigned." },

    { q: "When is returning a tuple the wrong choice?",
      a: ["Whenever more than one value comes back",
          "On a public API boundary — tuple element names are a compiler convenience with no documentation, no validation and no room to add a field without breaking callers",
          "Inside a private method",
          "Tuples are never the wrong choice"],
      c: 1,
      why: "Perfect between two private helpers; use a record for anything public. Note that a tuple without names forces callers to write Item1, which is how you lose an afternoon." },

    { q: "What is an attribute, such as `Obsolete`?",
      a: ["A compiler keyword",
          "An ordinary class deriving from Attribute, attached to a declaration as metadata and read by the compiler, a framework or a tool",
          "A comment the compiler parses",
          "A preprocessor directive"],
      c: 1,
      why: "ASP.NET routing, EF Core mapping, JSON serialisation and test discovery all work this way. They look like magic exactly until you know what they are." },

    { q: "Why prefer `nameof(Shipper)` over typing the string \"Shipper\"?",
      a: ["It is faster at run time",
          "It is resolved at compile time, so renaming the symbol updates it and a hand-typed string silently rots",
          "It produces the fully qualified name",
          "It works with obfuscation"],
      c: 1,
      why: "Use it in exceptions, logging and property-changed notifications — anywhere a string is really a reference to a symbol." },

    { q: "When is overloading an operator, such as `+` on a Cart, a bad idea?",
      a: ["Always — it is never legal",
          "When it hides a mutation, because every reader expects a symbol like + to be pure and return something new",
          "Only for value types",
          "Only when the class is public"],
      c: 1,
      why: "Overload operators on value-like types — money, vectors, dates — and nowhere else. `cart + line` that mutates the cart is a genuine trap." },
  ],

  /* ------------------------------------------------------------------ 02f */
  "02f-numbers-and-math": [
    { q: "Why should money never be stored in a `double`?",
      a: ["Doubles are slower",
          "double is binary floating point, so a decimal fraction like 0.1 has no exact representation and the error accumulates across a sum",
          "Doubles cannot hold large enough values",
          "double is not supported by SQL Server"],
      c: 1,
      why: "decimal stores a scaled integer in base ten, so 0.1 is exact and so is the total of a column. It is slower and has a smaller range, and neither cost matters for money." },

    { q: "What does `Math.Round(2.5m)` return?",
      a: ["3",
          "2 — the default is banker's rounding, where an exact half goes to the nearest even number",
          "2.5",
          "It throws"],
      c: 1,
      why: "Math.Round(3.5m) is 4 for the same reason. Over a column the upward and downward pushes cancel, so the rounded total matches the unrounded one — which is what an accountant wants." },

    { q: "For an invoice line that must round halves upward, what do you pass?",
      a: ["Nothing; that is the default",
          "MidpointRounding.AwayFromZero",
          "MidpointRounding.ToEven",
          "A second call to Math.Round"],
      c: 1,
      why: "And pass it everywhere, because the worst outcome is the same amount rounding two different ways in two parts of the system." },

    { q: "`(int)Math.Ceiling(41 / 12)` gives 3, not 4. Why?",
      a: ["Ceiling rounds down for small numbers",
          "Both operands are int, so integer division runs first and Math.Ceiling receives 3 — there is no fraction left to round up",
          "The cast to int truncates",
          "41 divided by 12 is 3 in any language"],
      c: 1,
      why: "Cast one operand before dividing: 41 / (double)12. Or avoid floating point entirely with the integer idiom (41 + 12 - 1) / 12." },

    { q: "What is `Math.Floor(-2.1)`?",
      a: ["-2",
          "-3 — Floor means toward negative infinity, not `round down`",
          "-2.1",
          "2"],
      c: 1,
      why: "Math.Truncate(-2.1) is -2, because Truncate means toward zero. If you meant `drop the fraction`, you wanted Truncate." },

    { q: "You split 10.00 three ways and round each share to two places. What goes wrong?",
      a: ["Nothing",
          "3.33 times three is 9.99 — a cent has evaporated, and no rounding mode fixes it because the problem is rounding three times independently",
          "The shares are unequal",
          "It throws an OverflowException"],
      c: 1,
      why: "Compute n-1 shares, then make the last one the total minus the sum of the others. Which party absorbs the remainder is a business decision worth asking about." },

    { q: "A gross figure of 122.00 includes 22% VAT. How much VAT is in it?",
      a: ["122 * 0.22 = 26.84",
          "22.00 — the VAT is 22% of the net, so the net is 122 / 1.22 and the tax is the difference",
          "122 * 1.22",
          "122 / 0.22"],
      c: 1,
      why: "Extracting VAT is a division, not a multiplication. Multiplying the gross overstates the tax by 22% of the tax itself, and the error scales with the invoice." },

    { q: "Why should you never compare two `double` values with `==`?",
      a: ["The operator is not defined for double",
          "Values that are mathematically equal can differ in their last bits, so 0.1 + 0.2 == 0.3 is false",
          "It is slower than Equals",
          "It boxes both values"],
      c: 1,
      why: "Compare within a tolerance chosen from what the number means. decimal compares exactly, which is one more reason money lives there." },

    { q: "A 10% discount followed by a 5% discount is equivalent to what single discount?",
      a: ["15%",
          "14.5%, because the second applies to the already reduced figure",
          "5%",
          "10%"],
      c: 1,
      why: "Never apply two percentages by adding them. Write the two steps out; do not simplify them into one number." },

    { q: "`int big = int.MaxValue; int x = big + 1;` What happens?",
      a: ["OverflowException",
          "It wraps to a large negative number, silently — no error, no warning, no log entry",
          "x becomes int.MaxValue",
          "The program terminates"],
      c: 1,
      why: "Wrap it in a `checked` block, or set CheckForOverflowUnderflow in the csproj — for business code that is almost always the right setting." },

    { q: "What does `double.NaN == double.NaN` evaluate to?",
      a: ["true",
          "false — NaN is equal to nothing, including itself; the only correct test is double.IsNaN",
          "It throws",
          "null"],
      c: 1,
      why: "This is the IEEE 754 standard, not a C# quirk. It is why a sort can throw and a Distinct can keep duplicates once a NaN is in the data." },

    { q: "Why does `new Random()` inside a tight loop sometimes return the same number every time?",
      a: ["Random is cached",
          "On older runtimes the seed came from a clock that had not ticked between calls, so every instance started from the same point",
          "Random only generates one number per process",
          "The loop is optimised away"],
      c: 1,
      why: "Use Random.Shared for ordinary work — one thread-safe instance for the whole process — and a fixed seed in tests so a failure is reproducible." },

    { q: "`Random.Shared.Next(1, 61)` can return which values?",
      a: ["1 to 61",
          "1 to 60 — the lower bound is inclusive and the upper is exclusive",
          "0 to 60",
          "0 to 61"],
      c: 1,
      why: "Every off-by-one with Random is this line. NextDouble follows the same rule: it can return 0.0 and never 1.0." },

    { q: "Which class would you use to generate a password reset token?",
      a: ["Random, with a good seed",
          "RandomNumberGenerator — Random is predictable by design and its next output can be computed from a few previous ones",
          "Guid.NewGuid",
          "DateTime.Now.Ticks"],
      c: 1,
      why: "Using Random for anything an attacker profits from guessing is a security bug, not a style preference." },

    { q: "`0.155m.ToString(\"P1\")` gives what, and what does that tell you about the P format?",
      a: ["`0.2 %` — it rounds first",
          "`15.5 %` — the P format multiplies by 100, so it wants the rate, not the percentage",
          "`155 %`",
          "`0.155 %`"],
      c: 1,
      why: "Pass it 15m expecting `15 %` and you get `1,500 %`. Rates and percentages are different units and the variable name should say which." },

    { q: "Why must a number written to a file or an API be formatted with `CultureInfo.InvariantCulture`?",
      a: ["It is faster",
          "Otherwise the same code writes 18.40 on one server and 18,40 on another, and the reader parses it as a different number with no error",
          "InvariantCulture uses fewer bytes",
          "It is required by JSON"],
      c: 1,
      why: "The most common `works on my machine` defect in the whole framework, and every Parse reading it back needs the same argument." },

    { q: "`amount.ToString(\"N2\")` — does that round the stored value?",
      a: ["Yes, permanently",
          "No — it is display only, so a total computed from stored values will not match a total the user adds up from what they can see",
          "Only for decimal",
          "Only above 1000"],
      c: 1,
      why: "If the two must agree — and on an invoice they must — round the value itself with Math.Round and store that, then format the rounded value." },

    { q: "Why does `double x = 0.0; while (x != 1.0) x += 0.1;` never terminate?",
      a: ["0.1 is not a valid double",
          "Ten additions of the nearest double to a tenth land just short of 1.0, so the equality test never holds",
          "The compiler optimises the loop away",
          "It does terminate, after 10 passes"],
      c: 1,
      why: "Compare with `<` rather than for equality, or better, count in integers and divide only when you need the value. A loop counter should never be floating point." },

    { q: "What is the `m` suffix in `19.99m` for?",
      a: ["It means metric",
          "It makes the literal a decimal; without it the literal is a double and assigning it to a decimal is a compile error",
          "It marks the value as money for the debugger",
          "It rounds to two places"],
      c: 1,
      why: "It stands for money, historically. The f suffix does the same job for float." },

    { q: "Rounding a value in the middle of a calculation and again at the end is called what, and why does it matter?",
      a: ["Defensive rounding; it improves accuracy",
          "Double rounding — it can move the answer by a full unit, because two nearest-value steps are not the same as one",
          "Banker's rounding; it has no effect",
          "Truncation; it only affects negatives"],
      c: 1,
      why: "Round once, at the edge, when you write the value down or show it." },
  ],

  /* ------------------------------------------------------------------ 02g */
  "02g-strings": [
    { q: "`string s = \"mi-42\"; s.ToUpper();` — what is s afterwards?",
      a: ["`MI-42`",
          "`mi-42` — strings are immutable, so ToUpper returned a new string that nobody caught",
          "An empty string",
          "It does not compile"],
      c: 1,
      why: "Every string method returns the new value; none of them mutate. The compiler does not warn, because calling a method and discarding its result is legal." },

    { q: "Why are strings immutable in C#?",
      a: ["To save memory",
          "It makes them thread-safe for free, safe as dictionary keys, and allows identical literals to be interned into one object",
          "Because they are value types",
          "To support Unicode"],
      c: 1,
      why: "The cost is an allocation per operation, invisible outside a loop and quadratic inside one — which is the gap StringBuilder fills." },

    { q: "What does `\"MI-42\".IndexOf('#')` return when the character is absent?",
      a: ["null",
          "-1 — never an exception",
          "0",
          "It throws ArgumentException"],
      c: 1,
      why: "And -1 + 1 is 0, so passing the result straight into Substring turns `not found` into `the whole string` rather than crashing. Always test against -1." },

    { q: "Why is `if (header.ToLower() == \"invoice\")` a bad comparison?",
      a: ["It is case-sensitive",
          "It allocates a new string and applies culture rules to an identifier — on a Turkish machine the lower-case of `I` is a dotless character and the match fails",
          "ToLower does not exist on string",
          "The == operator compares references"],
      c: 1,
      why: "Say what you mean: string.Equals(header, \"invoice\", StringComparison.OrdinalIgnoreCase). It is also faster, because it allocates nothing." },

    { q: "Which `StringComparison` should you use for SKUs, file paths and keys?",
      a: ["CurrentCulture",
          "Ordinal — it compares the raw character values, is faster, and gives the same answer on every machine",
          "InvariantCulture",
          "It does not matter"],
      c: 1,
      why: "Culture-aware comparison is right only for sorting text a person will read, because `which comes first` is genuinely a question about somebody's alphabet." },

    { q: "`\"MI-42;12;;Modena\".Split(';', StringSplitOptions.RemoveEmptyEntries)` gives how many parts, and why is that dangerous?",
      a: ["4, and it is fine",
          "3 — the empty column is deleted, which silently shifts every column after the gap and corrupts positional data",
          "5, including a null",
          "It throws"],
      c: 1,
      why: "RemoveEmptyEntries must be a deliberate choice, never a habit. For positional data, use TrimEntries alone." },

    { q: "When is `StringBuilder` the right choice over interpolation?",
      a: ["Whenever the string is long",
          "When the concatenation is inside a loop — outside one it is more code, more allocation and nothing gained",
          "Whenever there are more than three values",
          "Only in multi-threaded code"],
      c: 1,
      why: "Each += on a string copies everything accumulated so far, so an 80,000-line export that way is billions of character copies, usually misdiagnosed as a database problem." },

    { q: "Why is `Split` not a CSV parser?",
      a: ["It is too slow",
          "A real CSV can quote a field containing the separator, and can have quotes inside quotes and newlines inside fields",
          "It only accepts one separator",
          "It cannot handle headers"],
      c: 1,
      why: "Every attempt to fix this with Split plus a couple of ifs grows into a broken state machine. Use a library the moment the data comes from outside your program." },

    { q: "What is the difference between `s.Substring(3, 2)` and `s[3..5]`?",
      a: ["Nothing at all",
          "Substring takes a start and a length; the range takes a start and an exclusive end — two different mental models producing the same result here",
          "The range is faster",
          "Substring throws on the last character"],
      c: 1,
      why: "Mixing the two models up is an off-by-one waiting to happen. Prefer the range syntax in new code." },

    { q: "`ReferenceEquals(\"MI-42\", \"MI-42\")` is true. What is that showing?",
      a: ["That strings are value types",
          "Interning — the compiler puts identical literals in a shared pool, so two literals with the same text are one object",
          "That == is being used",
          "A compiler bug"],
      c: 1,
      why: "It is an optimisation, not a promise to rely on, and it does not apply to strings built at run time. Note that == on string compares characters, unlike every other reference type." },

    { q: "Why prefer `string.IsNullOrWhiteSpace` over `string.IsNullOrEmpty` for form input?",
      a: ["It is faster",
          "A field containing three spaces is empty to a human and non-empty to IsNullOrEmpty",
          "IsNullOrEmpty does not handle null",
          "It also trims the value"],
      c: 1,
      why: "For anything a person typed, the whitespace version is what you actually meant." },

    { q: "What does `string.Join(\", \", skus)` handle that a hand-written loop usually gets wrong?",
      a: ["Null elements",
          "The separator after the last element — no trailing comma, without a special case",
          "Sorting",
          "Escaping"],
      c: 1,
      why: "It works on any IEnumerable, calling ToString on each element, and it beats a StringBuilder for both length and clarity." },

    { q: "Why use `ToUpperInvariant` rather than `ToUpper` when normalising a SKU for storage?",
      a: ["It handles digits correctly",
          "ToUpper applies the machine's culture, so the same SKU can normalise differently on different servers and stop matching the database",
          "ToUpper is obsolete",
          "It is required for ASCII"],
      c: 1,
      why: "Use ToUpper only for text a human reads. For identifiers, always invariant." },

    { q: "`\"7\".PadLeft(5, '0')` gives `00007`. When is `n.ToString(\"D5\")` better?",
      a: ["Never",
          "When you have a number rather than a string — it says the intent better and cannot be defeated by an already-long value",
          "When the value has decimals",
          "When the width is over 10"],
      c: 1,
      why: "Padding is a string operation; zero-padding a number is a formatting one. Reach for the format string when you have the number." },

    { q: "What is the alignment and format syntax inside interpolation?",
      a: ["`{value:10,N2}`",
          "`{value,10:N2}` — comma then width, colon then format, in that order",
          "`{value|10|N2}`",
          "`{value 10 N2}`"],
      c: 1,
      why: "Getting the order backwards is a compile error rather than a bug, mercifully. It is how a console report gets columns that line up with no PadLeft at all." },

    { q: "How do you make a `Dictionary<string, T>` look up keys case-insensitively?",
      a: ["Call ToUpper on every key at every call site",
          "Pass StringComparer.OrdinalIgnoreCase to its constructor, so the behaviour is a property of the collection",
          "It is not possible",
          "Use a List instead"],
      c: 1,
      why: "That way nobody has to remember at the call sites, and there is one place the decision is recorded." },

    { q: "What is a raw string literal, and when is it useful?",
      a: ["A string prefixed with @",
          "A string opened with three or more double quotes, where nothing needs escaping and indentation is stripped relative to the closing quotes",
          "A byte array",
          "A string that cannot be interned"],
      c: 1,
      why: "Ideal for embedded SQL, JSON or a multi-line template. Prefix it with a dollar sign and interpolation still works." },

    { q: "Splitting a Windows-written file on `'\\n'` leaves what behind on each line?",
      a: ["Nothing",
          "A stray carriage return at the end of each line, because Windows line endings are two characters",
          "A null character",
          "A byte order mark"],
      c: 1,
      why: "Use TrimEnd, or better, read lines with File.ReadLines, which handles both conventions." },

    { q: "`s[^1]` means what?",
      a: ["The first character",
          "The last character — the index-from-end operator, the readable form of taking Length minus one",
          "A bitwise XOR",
          "The character at position 1"],
      c: 1,
      why: "s.Length - 1 is the expression people get wrong by one, which is the whole reason the operator exists." },

    { q: "Why is `a.ToLower() == b.ToLower()` worse than one `string.Equals` call with OrdinalIgnoreCase?",
      a: ["It gives different results",
          "It allocates two throwaway strings and introduces culture-dependent casing into what is really an identifier comparison",
          "ToLower cannot be chained",
          "Equals is deprecated"],
      c: 1,
      why: "Two allocations and one Turkish bug, to avoid typing one argument. It is the pattern to stop writing." },
  ],

  /* ------------------------------------------------------------------ 02h */
  "02h-booleans-and-conditions": [
    { q: "Does `if (count)` compile in C# when count is an int?",
      a: ["Yes, zero means false",
          "No — C# has no truthiness, and there is no implicit conversion from int to bool",
          "Only in unsafe code",
          "Only for unsigned types"],
      c: 1,
      why: "Deliberate, and it removes a whole family of bugs. Say what you mean: count != 0." },

    { q: "What is the difference between `&` and `&&` on two boolean expressions?",
      a: ["None",
          "&& short-circuits — if the left side is false the right side is never evaluated; & always evaluates both",
          "& is faster",
          "& works only on integers"],
      c: 1,
      why: "Short-circuiting is a guarantee in the language specification, which is why a null guard before a dereference is correct code rather than a lucky habit." },

    { q: "`if (order != null & order.Lines.Count > 0)` — what happens when order is null?",
      a: ["The condition is false",
          "A NullReferenceException, because the single ampersand evaluates both sides before combining them",
          "A compile error",
          "The guard works as intended"],
      c: 1,
      why: "One missing character turns a working guard into a crash. A review should stop on any single ampersand between two boolean expressions." },

    { q: "Why is `if (s != \"A\" || s != \"B\")` always true?",
      a: ["It is not; it depends on s",
          "No single value can be both, so at least one half always holds — the author meant &&",
          "Because != is not defined for strings",
          "Because of operator precedence"],
      c: 1,
      why: "Its mirror, two == joined by &&, is always false. Both read correctly in English and neither is flagged by the compiler." },

    { q: "In `a || b && c`, which operator binds more tightly?",
      a: ["||",
          "&& — so it parses as a || (b && c)",
          "They are equal, left to right",
          "It is ambiguous and will not compile"],
      c: 1,
      why: "The one precedence fact worth actually knowing, because the mistake it causes reads perfectly well aloud. Brackets cost nothing at run time." },

    { q: "How many states does a `bool?` have?",
      a: ["Two",
          "Three — true, false, and null meaning nobody has answered yet",
          "Four",
          "Two, plus an exception state"],
      c: 1,
      why: "It comes out of a nullable bit column constantly, and code written for two states handles it wrong in a specific, predictable way." },

    { q: "`bool? approved = null; if (approved != false) Ship();` — what happens?",
      a: ["Nothing ships",
          "Unreviewed orders ship, because null != false is true",
          "A NullReferenceException",
          "A compile error"],
      c: 1,
      why: "With three-state data, test for the state you want — approved == true — rather than against the one you do not." },

    { q: "What does `approved ?? false` express, beyond its mechanics?",
      a: ["Nothing; it just avoids null",
          "A decision: which way an unanswered question defaults, which is a business choice and this operator is where it is recorded",
          "That approved can never be null",
          "A cast"],
      c: 1,
      why: "Defaulting an unreviewed order to `not approved` is right; defaulting an unanswered consent box to `consented` is a legal problem." },

    { q: "`null | true` evaluates to what, in three-valued logic?",
      a: ["null",
          "true — unknown OR true is true whatever the unknown turns out to be",
          "false",
          "It throws"],
      c: 1,
      why: "The same rules SQL uses for NULL, and the reason WHERE x != 'A' does not return the rows where x is null." },

    { q: "Why is `if (x > 0) return true; else return false;` worth rewriting?",
      a: ["It is slower",
          "The comparison is already a bool, so `return x > 0;` says the same thing with a third of the words",
          "else is deprecated",
          "It does not handle negatives"],
      c: 1,
      why: "The single most common thing to be told to delete in a first code review." },

    { q: "What is a guard clause?",
      a: ["A lock around shared state",
          "An early return that rejects a bad case immediately, so the happy path ends up unindented at the bottom",
          "A try/catch at the top of a method",
          "A null check on every parameter"],
      c: 1,
      why: "Inverting the nesting is the single structural habit that most separates junior from mid-level code — every rejection reason ends up on one visible line." },

    { q: "`Dispatch(order, true, false, true)` — what are the three fixes, in increasing strength?",
      a: ["Comments, comments and more comments",
          "A named argument, an enum instead of a bool, or two methods with names that say what they do",
          "Reorder the parameters, shorten them, document them",
          "Make them optional"],
      c: 1,
      why: "A method with three or more boolean parameters is usually two or three methods that have not been separated yet." },

    { q: "Why write `qty is > 0 and <= 100` rather than `qty > 0 && qty <= 100`?",
      a: ["It is faster",
          "The subject is written once, so it cannot drift — the operator version is how `x > 0 && y <= 100` gets written by accident",
          "Patterns are required in modern C#",
          "It handles null differently"],
      c: 1,
      why: "The and/or/not inside a pattern are pattern keywords rather than operators: they combine patterns, which is why no subject is repeated after them." },

    { q: "Why prefer `is not null` over `!= null`?",
      a: ["It is shorter",
          "It reads better and cannot be hijacked by a type that overloads the != operator",
          "!= does not work on reference types",
          "It handles nullable value types differently"],
      c: 1,
      why: "A small, free improvement, and one of the easiest habits to pick up." },

    { q: "`order?.Lines.Count > 0` when order is null gives what?",
      a: ["A NullReferenceException",
          "false — the null-conditional produces a nullable int, and a lifted comparison against null is false rather than an error",
          "true",
          "null"],
      c: 1,
      why: "Worth knowing that both `null > 0` and `null < 0` are false, so you cannot assume one of the two must hold." },

    { q: "What is the risk of short-circuiting past a method call?",
      a: ["There is none",
          "If that call had a side effect — logging, auditing, counting — it silently stops happening for half your inputs",
          "It can deadlock",
          "The compiler will warn"],
      c: 1,
      why: "Anything with a side effect belongs on its own line, not inside a condition." },

    { q: "Why put the cheap test first in a compound condition?",
      a: ["It reads better",
          "Short-circuiting means the expensive one — a network or database call — runs only when the cheap one has not already settled the answer",
          "The compiler reorders them anyway",
          "It avoids boxing"],
      c: 1,
      why: "Ordering a condition is a performance decision you get for free." },

    { q: "A `switch` expression over a value needs which arm, and what happens without it?",
      a: ["A default case; nothing happens without it",
          "A discard arm — without it an unmatched value throws SwitchExpressionException at run time",
          "A null arm; it returns null",
          "None; it is exhaustive automatically"],
      c: 1,
      why: "A switch expression returns a value, so it must be exhaustive. The exception is the compiler telling you it cannot prove you covered everything." },

    { q: "Why is `if (!isNotReady)` worth renaming?",
      a: ["Double negation does not compile",
          "A double negative takes a reader two passes; naming the flag IsReady makes the confusing version impossible to write",
          "The ! operator is slow",
          "It breaks short-circuiting"],
      c: 1,
      why: "Prefer positive names. It is the cheapest readability win in the language." },

    { q: "What is the strongest way to make a five-clause condition readable?",
      a: ["Reformat it over five lines",
          "Move it onto the type it asks about as a property like IsDispatchable, so it cannot drift across call sites",
          "Add a comment above it",
          "Split it into nested ifs"],
      c: 1,
      why: "Naming it in a local bool is already a large improvement; making it a property of the data is the version that cannot get out of step." },
  ],

  /* ------------------------------------------------------------------ 02i */
  "02i-arrays-and-shapes": [
    { q: "What is `new string[3]` filled with?",
      a: ["Empty strings",
          "Three nulls — the slots hold the element type's default, and for any reference type that is null",
          "Nothing; it has no elements",
          "Three spaces"],
      c: 1,
      why: "new int[3] is three zeros for the same reason. Iterating a string array without a null check is an exception waiting to happen." },

    { q: "Can an array grow?",
      a: ["Yes, with Add",
          "No — the length is fixed at creation; Array.Resize allocates a new array and copies everything across",
          "Yes, up to its capacity",
          "Only for reference types"],
      c: 1,
      why: "If you find yourself calling Resize in a loop, you wanted a List." },

    { q: "On `int[,] stock = new int[4, 6]`, what does `stock.Length` return?",
      a: ["4",
          "24 — the total number of cells, not the number of rows",
          "6",
          "It does not compile"],
      c: 1,
      why: "Rows and columns are GetLength(0) and GetLength(1). This is the single most common mistake with rectangular arrays." },

    { q: "What is the difference between `int[,]` and `int[][]`?",
      a: ["Only syntax",
          "The first is one contiguous rectangular block where every row is the same length; the second is an array of references to separate arrays of any length",
          "The first is faster to index by key",
          "The second cannot be iterated"],
      c: 1,
      why: "Jagged is usually right in business code, because real data is ragged — orders have different numbers of lines. Rectangular earns its place for genuine grids." },

    { q: "`int[][] grid = new int[3][]; grid[0][0] = 1;` — what happens?",
      a: ["It works",
          "NullReferenceException — only the outer array was allocated, and its three slots hold null",
          "IndexOutOfRangeException",
          "It does not compile"],
      c: 1,
      why: "Each row has to be created before it can be indexed. A rectangular new int[3, 6] would have worked immediately, which is one honest advantage it has." },

    { q: "You copy an array of objects with `ToArray()` then mutate an element. What does the other array see?",
      a: ["Nothing; they are independent",
          "The change — every built-in copy in .NET is shallow, duplicating the slots and sharing the objects in them",
          "A cloned copy of the old value",
          "It throws"],
      c: 1,
      why: "Replacing an element affects only one array; mutating an element affects both. A deep copy has to be written by hand, or avoided by making the elements immutable." },

    { q: "What is the difference between `Array.Sort(a)` and `a.OrderBy(x => x)`?",
      a: ["None",
          "Sort mutates the array in place and returns void; OrderBy returns a new sequence and leaves the original alone",
          "OrderBy is always faster",
          "Sort works only on numbers"],
      c: 1,
      why: "Which you want is a real decision, because mutating also changes the array anybody else is holding a reference to." },

    { q: "What does `Array.BinarySearch` do on an unsorted array?",
      a: ["Throws InvalidOperationException",
          "Returns a wrong answer, silently — it is fast because it assumes order, and keeping that assumption is your side of the bargain",
          "Sorts it first",
          "Returns -1"],
      c: 1,
      why: "The kind of API that trusts you, and the kind of bug that only appears on some inputs." },

    { q: "Why return `Array.Empty<T>()` rather than null from a method?",
      a: ["It is shorter",
          "It returns a shared cached instance, allocates nothing, and removes a null guard from every caller",
          "null is not allowed for arrays",
          "It is faster to iterate"],
      c: 1,
      why: "The caller cannot forget a guard that is not needed. The same reasoning applies to returning an empty list." },

    { q: "Why should a class never return its internal array from a property?",
      a: ["It is slow",
          "An array is a reference, so the caller is holding your state and can rewrite it past every validation method you own",
          "Arrays cannot be properties",
          "It prevents garbage collection"],
      c: 1,
      why: "Return IReadOnlyList for a cheap view, or ToArray for a copy the caller owns." },

    { q: "`allSkus.Contains(sku)` inside a loop over orders is what complexity, and what is the fix?",
      a: ["O(1); nothing to fix",
          "O(n times m), because Contains on an array or list is a linear scan — build a HashSet once outside the loop",
          "O(log n); use BinarySearch",
          "O(n); it is already optimal"],
      c: 1,
      why: "The characteristic symptom is code that is fine for a year and gradually becomes unbearable as reference data grows, and gets blamed on the database." },

    { q: "What happens if you call `Remove` on a `List<T>` inside a `foreach` over it?",
      a: ["The item is removed",
          "InvalidOperationException — the list keeps a version number and the enumerator checks it on every step",
          "The loop silently skips an element",
          "Nothing until the loop ends"],
      c: 1,
      why: "It is a mercy: the alternative would be silently skipping elements. Materialise the matches first, or use RemoveAll." },

    { q: "Why can you not assign to the loop variable in a `foreach` over an `int[]`?",
      a: ["foreach is read-only by design",
          "The loop variable is a copy of each value, so assigning to it would change nothing — and the compiler forbids it rather than letting you believe otherwise",
          "Arrays are immutable",
          "It would throw at run time"],
      c: 1,
      why: "Over an array of reference types the variable is a reference to the real object, so mutating a property does work. That asymmetry catches everyone once." },

    { q: "What is `s[^1]` and `c[1..]` on an array?",
      a: ["A bitwise operation and a comment",
          "The last element, and a new array containing everything from index 1 onward",
          "The first element and a slice that shares memory",
          "Invalid syntax for arrays"],
      c: 1,
      why: "On an array a range allocates and copies. On a Span the same syntax copies nothing, which is the whole point of spans." },

    { q: "When does `Span<T>` earn its place?",
      a: ["Everywhere, as the default collection",
          "In a parsing hot path where slicing without allocating matters — its restrictions, stack-only and no async, are what make it safe",
          "When the collection must grow",
          "When storing data in a field"],
      c: 1,
      why: "It cannot be stored in a field or used in an async method, deliberately. Most of the time the honest answer is List." },

    { q: "Which type should a method return for a collection callers should not modify?",
      a: ["a plain array",
          "IReadOnlyList<T>",
          "List<T>",
          "object"],
      c: 1,
      why: "It is a view rather than a snapshot — cheap, and it still reflects later changes to the underlying list. For a stable picture, hand over ToArray and accept the copy." },

    { q: "`Array.Fill(counts, -1)` — why would you do that rather than leave the zeros?",
      a: ["Performance",
          "Because 0 is a legitimate count, so distinguishing `we counted zero` from `we have not counted` needs a sentinel or a nullable element type",
          "Negative numbers sort first",
          "It initialises the array"],
      c: 1,
      why: "Choosing the sentinel deliberately is the point; an unexamined default zero is how uncounted bins get reported as empty." },

    { q: "How do you get the index alongside the value in a `foreach`?",
      a: ["It is not possible",
          "Use the Select overload that supplies an index, projecting into a tuple",
          "Use foreach with two variables",
          "Call IndexOf inside the loop"],
      c: 1,
      why: "Cleaner than maintaining your own counter, and impossible to get out of step. Calling IndexOf inside the loop is both slow and wrong when there are duplicates." },

    { q: "`Array.Resize(ref a, 100)` takes its argument by ref. Why?",
      a: ["To avoid copying the array",
          "Because it cannot resize anything — it creates a new array and has to repoint your variable at it",
          "To allow null",
          "Because arrays are value types"],
      c: 1,
      why: "The name is a lie the signature quietly admits to." },

    { q: "What does `new int[3]` cost compared with `Array.Empty<int>()`?",
      a: ["The same",
          "An allocation, whereas Array.Empty returns a shared cached instance and allocates nothing",
          "Less, because it is smaller",
          "Array.Empty is slower"],
      c: 1,
      why: "Which is why Array.Empty is the right thing to return for `nothing found`, and why new int[0] is worth replacing wherever it appears." },
  ],

  /* ------------------------------------------------------------------ 02j */
  "02j-enums": [
    { q: "What is an enum, underneath?",
      a: ["A sealed class of constants",
          "A value type backed by an integral type, usually int, with names attached at compile time",
          "A string lookup table",
          "A struct with one field per member"],
      c: 1,
      why: "That is why casting works both ways and costs nothing at run time — and why the names are a compile-time convenience rather than a run-time restriction." },

    { q: "Why should you assign enum values explicitly rather than letting them default?",
      a: ["It is only a style preference",
          "Implicit numbering starts at 0 and counts up, so inserting a member in the middle silently renumbers the rest while stored rows do not move",
          "Implicit values are not supported in .NET 8",
          "Explicit values are faster"],
      c: 1,
      why: "The single most expensive enum mistake, and completely invisible in a diff. Add new members at the end with a new number, and leave gaps." },

    { q: "What does `(OrderStatus)57` do when no member has the value 57?",
      a: ["Throws InvalidCastException",
          "Compiles and runs, giving a value whose ToString is `57` — an enum variable is not restricted to its named values",
          "Returns the default member",
          "Fails to compile"],
      c: 1,
      why: "Exactly how a value with no case reaches a switch. Validate anything cast from outside with Enum.IsDefined." },

    { q: "Why should the value zero always be given a meaningful name?",
      a: ["Zero is reserved",
          "It is the default for an unassigned field and for a NOT NULL DEFAULT 0 column, so leaving it unnamed means the default value has no name",
          "Otherwise the enum will not serialise",
          "Because ToString fails on zero"],
      c: 1,
      why: "Make it Draft, None or Unknown — a state that genuinely is the starting point." },

    { q: "Why does every `switch` over an enum need a default arm that throws?",
      a: ["The compiler requires it for enums",
          "C# cannot enforce exhaustiveness over an enum, because a value with no name is legal — a silent fallback hides the member somebody forgot to handle",
          "To catch null",
          "For performance"],
      c: 1,
      why: "A silent `unknown` string turns a forgotten member into a cosmetic oddity; an exception turns it into a failing test on the day it is added." },

    { q: "`Enum.TryParse<OrderStatus>(\"57\", out var s)` returns what?",
      a: ["false",
          "true, with a status of 57 — TryParse accepts numbers as well as names",
          "It throws",
          "true, with the default member"],
      c: 1,
      why: "Which is why it must be chained with Enum.IsDefined. Separately, each is insufficient; together they are a correct parse." },

    { q: "What does the Flags attribute actually change?",
      a: ["It enforces powers of two",
          "Only how ToString prints — a comma-separated list of set members rather than a number",
          "It makes the enum bitwise-capable",
          "It changes the underlying type to uint"],
      c: 1,
      why: "What makes a flags enum work is that the values are powers of two. Nothing checks that, and colliding values are undetectable afterwards." },

    { q: "A flags enum is numbered 1, 2, 3, 4. What breaks?",
      a: ["Nothing",
          "Combining the first two gives 3, which is indistinguishable from the third member",
          "ToString stops working",
          "The enum will not compile"],
      c: 1,
      why: "Values must be 1, 2, 4, 8 so each occupies its own bit. The compiler will never tell you otherwise." },

    { q: "`h.HasFlag(A | B)` is true when?",
      a: ["When either A or B is set",
          "Only when both are set — HasFlag tests whether every bit in the argument is present",
          "Never; it does not accept combinations",
          "When exactly one is set"],
      c: 1,
      why: "For `any of these`, use a bitwise and against the mask and compare to zero. The two read almost identically and answer different questions." },

    { q: "Why should an API serialise an enum as its name rather than its number?",
      a: ["Numbers are larger on the wire",
          "The number makes your internal numbering part of somebody else's contract, and an internal renumber becomes a silent breaking change",
          "Names are faster to parse",
          "JSON does not support integers"],
      c: 1,
      why: "JsonStringEnumConverter at the boundary. Then a rename fails loudly at deserialisation instead of quietly meaning something else." },

    { q: "You need to remove an enum member that is no longer used. What is the correct action?",
      a: ["Delete it and renumber",
          "Leave it where it is and mark it obsolete — deleting it does not delete the values already stored, it only removes the name for them",
          "Delete it; stored values are migrated automatically",
          "Set it to zero"],
      c: 1,
      why: "Never renumber, never reuse, never delete a member once anything outside the program has seen a value." },

    { q: "What are the trade-offs of storing an enum as a string column rather than an int?",
      a: ["There are none; int is always correct",
          "A string costs a few bytes and makes the table readable in a query window, and a rename fails loudly on read instead of silently meaning something else",
          "A string cannot be indexed",
          "EF Core does not support it"],
      c: 1,
      why: "One HasConversion call in EF Core. Either way, the never-renumber rule still applies." },

    { q: "Why is `Enum.GetValues<T>()` better than a hand-written list for a dropdown?",
      a: ["It is sorted alphabetically",
          "There is no parallel list to fall out of step when a member is added",
          "It returns display names",
          "It is the only way to enumerate an enum"],
      c: 1,
      why: "Pair it with a Display attribute when the on-screen wording must differ from the identifier — and it must, as soon as a second language is involved." },

    { q: "Why prefer `nameof(OrderStatus.Picked)` over `status.ToString()` in a hot loop?",
      a: ["They are identical",
          "nameof is resolved at compile time into a plain string constant, while ToString uses reflection",
          "ToString can return null",
          "nameof handles flags better"],
      c: 1,
      why: "nameof also stops compiling if somebody renames the member, which a string literal does not." },

    { q: "When has an enum outgrown itself?",
      a: ["Past ten members",
          "When members start needing behaviour or extra data, and the same switch over the enum appears in five places",
          "When it needs the Flags attribute",
          "When it is stored in a database"],
      c: 1,
      why: "That is the signal to move to a class with static instances, a dictionary keyed by the enum, or the strategy pattern. The enum is not wrong; it has been asked a question it cannot answer." },

    { q: "Why leave gaps in enum numbering, such as 0, 10, 20, 30?",
      a: ["It makes sorting faster",
          "So a new state can be inserted in logical order later without renumbering anything already stored",
          "It is required for flags",
          "Small numbers are reserved"],
      c: 1,
      why: "Cheap now, priceless in two years. The alternative is either renumbering — which corrupts data — or a state that sorts in the wrong place forever." },

    { q: "Why is `None = 0` conventional in a flags enum?",
      a: ["It is required to compile",
          "Zero is the default value and should mean `no flags set`; a flag valued zero can never be tested, since HasFlag always returns true for it",
          "It makes the enum nullable",
          "It reserves the first bit"],
      c: 1,
      why: "One of those conventions that exists because the alternative silently misbehaves rather than failing." },

    { q: "How do you remove a single flag from a combined value?",
      a: ["Subtract it",
          "Use a bitwise and with the complement of that flag, keeping every bit except the one",
          "Call Remove on it",
          "Cast to int and divide"],
      c: 1,
      why: "Subtraction happens to work when the flag is definitely set and silently corrupts the value when it is not." },

    { q: "Why is `MidpointRounding` worth mentioning as an example of an enum?",
      a: ["It is the only enum in the framework",
          "It is the most useful one in everyday code — the argument that decides whether 2.5 rounds to 2 or 3",
          "It is a flags enum",
          "It has no zero member"],
      c: 1,
      why: "A good reminder that an enum is how a framework offers you a decision rather than a magic boolean." },

    { q: "What is the practical difference between an enum and a set of const ints?",
      a: ["None; the enum is syntactic sugar",
          "The enum is a distinct type, so a method taking it will not silently accept any int, and IntelliSense offers exactly the valid values",
          "Const ints are faster",
          "Enums cannot be used in a switch"],
      c: 1,
      why: "Type safety at the call site is most of the value. The rest is that there is one place the set is defined." },
  ],

  /* ------------------------------------------------------------------ 02k */
  "02k-files-and-folders": [
    { q: "Why build paths with `Path.Combine` rather than concatenating with a separator?",
      a: ["It is faster",
          "It uses the platform's separator and handles the trailing-slash question, so a hard-coded backslash does not break the moment the code runs on Linux",
          "It validates that the file exists",
          "Concatenation does not compile"],
      c: 1,
      why: "The commonest reason perfectly good code fails the moment it is containerised. On Linux the backslash becomes part of the file name." },

    { q: "`Path.Combine(\"/data\", \"/etc/passwd\")` returns what?",
      a: ["/data/etc/passwd",
          "/etc/passwd — if any argument is rooted, everything before it is discarded",
          "An empty string",
          "It throws ArgumentException"],
      c: 1,
      why: "A security hole when the second piece came from a user. Take Path.GetFileName of user input first, then confirm the resolved full path is still inside your folder." },

    { q: "What is the difference between `File.ReadAllLines` and `File.ReadLines`?",
      a: ["Nothing but the name",
          "ReadAllLines loads the whole file into an array; ReadLines returns a lazy sequence read as you enumerate, in constant memory",
          "ReadLines is asynchronous",
          "ReadAllLines handles encoding"],
      c: 1,
      why: "One letter, and the difference between code that works on a test file and code that survives a four-million-row export." },

    { q: "Why does every stream need a `using`?",
      a: ["To flush the buffer only",
          "It holds an unmanaged OS handle, so without disposal the file stays locked until the garbage collector eventually runs — and a writer can also lose its last buffer",
          "Because streams are structs",
          "To make it thread-safe"],
      c: 1,
      why: "using guarantees release at the end of the scope, including on an early return and including when the body throws — which is the case that matters." },

    { q: "How do you write a file that a folder watcher cannot catch half-written?",
      a: ["Write slowly and flush often",
          "Write to a temporary name in the same folder, then rename it onto the final name, because a rename within a volume is atomic",
          "Lock the file with FileShare.None while writing",
          "Write it twice and compare"],
      c: 1,
      why: "The temp file must be in the same folder — across volumes the move becomes a copy and delete and the guarantee evaporates." },

    { q: "A number written as 18.40 on one server is read back as 1840 on another. Why?",
      a: ["A rounding error",
          "Culture: 18,40 written under an Italian culture is parsed as a thousands separator by an English one, with no exception either way",
          "The file encoding was wrong",
          "The decimal overflowed"],
      c: 1,
      why: "Files are read by machines. InvariantCulture on both write and read, and the round-trip format for dates." },

    { q: "Why write dates to a file with the `\"O\"` format?",
      a: ["It is the shortest",
          "It is the round-trip format, ISO 8601 with offset, unambiguous everywhere — unlike 03/04/2026, which is March to half the world and April to the other half",
          "It is required by JSON",
          "It avoids time zones entirely"],
      c: 1,
      why: "A date that parses successfully under both cultures and means different months is far worse than one that fails." },

    { q: "What does `File.Exists` guarantee?",
      a: ["That you can open the file",
          "Nothing useful — anything can happen between the check and the open, and it also returns false for a path you lack permission to see",
          "That the file is not locked",
          "That the path is valid"],
      c: 1,
      why: "Checking before opening is a hint; the try is the guard." },

    { q: "Which exception should you expect when a file is locked by another process?",
      a: ["UnauthorizedAccessException",
          "IOException",
          "FileNotFoundException",
          "InvalidOperationException"],
      c: 1,
      why: "Log it and let the next sweep try again. Do not catch it and continue silently, and do not treat it as a bug — a sender still writing is normal." },

    { q: "What does `FileShare.Read` declare?",
      a: ["That you will only read",
          "What other processes may do while you hold the file: read but not write",
          "That the file is read-only on disk",
          "That reads are cached"],
      c: 1,
      why: "FileShare.None is an exclusive lock, which is a crude but effective way to ask whether the sender has finished." },

    { q: "Why does a UTF-8 CSV sometimes open wrongly in Excel on an Italian machine?",
      a: ["Excel cannot read UTF-8",
          "Without a byte order mark Excel guesses the encoding, and guesses the local code page — so accented characters come out as symbols",
          "The separator is wrong",
          "Excel requires UTF-16"],
      c: 1,
      why: ".NET writes UTF-8 without a BOM by default. Constructing a UTF8Encoding that emits the identifier fixes the most common export support ticket there is." },

    { q: "What is the difference between `Directory.EnumerateFiles` and `Directory.GetFiles`?",
      a: ["None",
          "Enumerate is lazy and yields as it goes; Get builds the whole array before returning anything",
          "Get is recursive by default",
          "Enumerate returns FileInfo objects"],
      c: 1,
      why: "On a folder with two hundred thousand files that is the difference between starting immediately and waiting. The same pairing as ReadLines and ReadAllLines." },

    { q: "`File.Delete` on a file that does not exist does what?",
      a: ["Throws FileNotFoundException",
          "Succeeds silently — unlike Directory.Delete, which throws",
          "Returns false",
          "Creates the file"],
      c: 1,
      why: "An inconsistency worth remembering rather than trying to understand." },

    { q: "Why use the async file APIs inside a web application?",
      a: ["They are faster",
          "A synchronous read blocks a thread-pool thread for the whole disk or network round trip, and under load that is how a server stops responding",
          "They handle encoding better",
          "They are required by ASP.NET"],
      c: 1,
      why: "And pass a cancellation token — a cancelled request should stop reading a two-gigabyte file rather than finishing it politely." },

    { q: "Is the search pattern in `EnumerateFiles(dir, \"*.csv\")` a regular expression?",
      a: ["Yes",
          "No — it is a glob, supporting only the asterisk and the question mark",
          "Yes, but only a subset",
          "It is a LIKE pattern"],
      c: 1,
      why: "It also has a Windows heritage quirk: it can match names with extra trailing characters, so filter on Path.GetExtension if it matters." },

    { q: "`Directory.CreateDirectory` on a folder that already exists does what?",
      a: ["Throws",
          "Nothing — it is idempotent and creates the whole path, so wrapping it in an Exists check is noise",
          "Deletes and recreates it",
          "Returns null"],
      c: 1,
      why: "One of the few APIs in the framework that is comfortably idempotent, and worth knowing so you stop writing the guard." },

    { q: "What does `AppContext.BaseDirectory` give you that `Directory.GetCurrentDirectory()` does not?",
      a: ["A writable path",
          "Where the application was deployed, rather than wherever the process happened to be started — which under a service or scheduled task is rarely what you assumed",
          "The user's home folder",
          "A temporary folder"],
      c: 1,
      why: "A classic difference between running from an IDE and running as a Windows service." },

    { q: "A file referenced as `Orders.csv` and stored as `orders.csv` works locally and vanishes on the server. Why?",
      a: ["A caching problem",
          "Windows paths are case-insensitive and Linux paths are not",
          "The file was moved",
          "A BOM in the filename"],
      c: 1,
      why: "Match the case exactly. It is the second most common containerisation surprise after the path separator." },

    { q: "Why is `File.WriteAllText` dangerous on an existing file?",
      a: ["It appends instead of replacing",
          "It overwrites without asking, and there is no fail-if-exists overload",
          "It locks the file permanently",
          "It changes the encoding"],
      c: 1,
      why: "If that matters, check first and accept that the check and the write are not atomic — or write to a temp name and rename." },

    { q: "What is the shape of a well-behaved inbox folder processor?",
      a: ["Read everything at startup",
          "Enumerate lazily in creation order, open each inside a try because it may still be being written, stream it line by line, and move it to a done folder when finished",
          "Poll every file every second",
          "Delete each file after reading it"],
      c: 1,
      why: "The folder listing then doubles as the work queue, and an IOException is `not ready yet` rather than an error." },
  ],

  /* ------------------------------------------------------------------ 02l */
  "02l-exceptions": [
    { q: "When is an exception the wrong tool?",
      a: ["Whenever performance matters",
          "When the caller could reasonably have expected the outcome — not found, wrong password, a file that has not arrived yet",
          "Inside a library",
          "In async code"],
      c: 1,
      why: "Those are ordinary results and belong in a nullable return, a Try method or a result object. An exception says the method's contract could not be honoured at all." },

    { q: "What is wrong with `catch (Exception ex) { throw ex; }`?",
      a: ["It does not compile",
          "It resets the stack trace to that line, so the log points at your catch block rather than where the failure happened",
          "It swallows the exception",
          "It converts the exception type"],
      c: 1,
      why: "A bare `throw;` preserves the trace. There is no ordinary case where throw ex is what you wanted, and nothing warns you." },

    { q: "Why is an empty catch block worse than letting the program crash?",
      a: ["It is not; crashing is always worse",
          "It removes the report rather than the failure, so the program continues in a broken state and the problem surfaces later somewhere unrelated with no trace",
          "It is slower",
          "It catches only some exceptions"],
      c: 1,
      why: "An API returning 200 for an order that was never saved is the canonical example. If you really intend to ignore something, catch the specific type and say why in a comment." },

    { q: "In what order must catch blocks be written?",
      a: ["Any order",
          "Most specific first — a broad catch above a narrow one makes the narrow one unreachable, which the compiler rejects",
          "Broadest first",
          "Alphabetically by type name"],
      c: 1,
      why: "One of the few places the compiler protects you from this class of mistake." },

    { q: "What does an exception filter, `catch (…) when (…)`, do that catching and rethrowing does not?",
      a: ["Nothing; they are equivalent",
          "The filter is evaluated before the stack unwinds, so declining to handle preserves information that a catch-and-rethrow destroys",
          "It catches more types",
          "It runs asynchronously"],
      c: 1,
      why: "It is also more readable: the condition sits beside the type rather than as an if inside the block." },

    { q: "Which exception type would you throw for `you cannot dispatch an order with no lines`?",
      a: ["ArgumentException",
          "InvalidOperationException — the argument was fine, the object's state was not",
          "NotSupportedException",
          "Exception"],
      c: 1,
      why: "The one to reach for most often and the one people forget exists." },

    { q: "Why should you never throw a bare `Exception`?",
      a: ["It is abstract",
          "It gives the caller no way to handle your failure specifically, so their only option is a broad catch that then swallows everything else too",
          "It has no message property",
          "It cannot carry an inner exception"],
      c: 1,
      why: "Your carelessness becomes their bug." },

    { q: "Should you ever catch a `NullReferenceException`?",
      a: ["Yes, as a safety net",
          "No — it is a report of a bug in your own code, not a condition to manage, so catching one hides a defect",
          "Only in library code",
          "Only at the top level"],
      c: 1,
      why: "The same applies to IndexOutOfRangeException. Fix the dereference rather than handling the symptom." },

    { q: "Why should `OperationCanceledException` be allowed to pass?",
      a: ["It cannot be caught",
          "It is normal behaviour — a user navigated away — so logging it as an error fills the alerting with noise and makes the error rate meaningless",
          "It is always rethrown automatically",
          "It has no stack trace"],
      c: 1,
      why: "An overly broad catch below the top level is usually how it gets there." },

    { q: "When does `finally` run?",
      a: ["Only when no exception was thrown",
          "Always — whether the block completed, returned, or threw",
          "Only when an exception was thrown",
          "Only if there is a matching catch"],
      c: 1,
      why: "It is for releasing things and nothing else. Never return from a finally, which silently discards an exception on its way past." },

    { q: "`try { return 1; } finally { Console.WriteLine(\"f\"); }` — what is printed and what is returned?",
      a: ["Nothing is printed; 1 is returned",
          "`f` is printed, then 1 is returned — the return value is evaluated first, then finally runs, then the return lands",
          "`f` is printed and nothing is returned",
          "It does not compile"],
      c: 1,
      why: "A favourite puzzle question, and the reason returning from a finally is so dangerous." },

    { q: "What is `using` in terms of exception handling?",
      a: ["A form of catch",
          "try/finally with the Dispose call written for you",
          "A way to suppress exceptions",
          "An import statement only"],
      c: 1,
      why: "If the only thing in your finally is a Dispose, delete the whole construction and write using." },

    { q: "When is a custom exception type worth defining?",
      a: ["For every service",
          "Only when a caller might plausibly want to catch that particular failure and do something specific about it",
          "Whenever the message is long",
          "Never; the framework types are enough"],
      c: 1,
      why: "If nobody would ever write a catch for it, an existing type with a good message is better." },

    { q: "What should a custom exception derive from?",
      a: ["Always Exception",
          "The closest meaningful base — deriving from InvalidOperationException means code that already catches that keeps working",
          "ApplicationException",
          "SystemException"],
      c: 1,
      why: "And carry its data as properties, so a handler can act on the order id rather than reading it out of the message text." },

    { q: "Why pass the original exception as `inner` when translating one?",
      a: ["It is required by the base constructor",
          "Otherwise you have replaced the evidence with your summary of it, and nobody can see that the real cause was a timeout to the courier's API",
          "It improves performance",
          "It preserves the exception type"],
      c: 1,
      why: "Always provide the inner overload on a custom exception, and use it whenever you wrap." },

    { q: "Why is `try { return int.Parse(s); } catch { return 0; }` a problem beyond performance?",
      a: ["It is not a problem",
          "The bare catch also swallows genuine failures such as out of memory, and it hides the malformed rows somebody should be told about",
          "Parse cannot throw",
          "It will not compile without a type"],
      c: 1,
      why: "int.TryParse exists for exactly this, and it is the same length." },

    { q: "Where should exceptions be caught in a web application?",
      a: ["In every service method",
          "Where something can actually be done about them, which is normally one global handler that maps known types to status codes and logs the rest",
          "In the controller only",
          "Never; let them propagate to the client"],
      c: 1,
      why: "A try/catch in every service that logs and rethrows produces six stack traces per failure, each looking like a separate incident." },

    { q: "Roughly how expensive is throwing an exception compared with a plain return?",
      a: ["About the same",
          "On the order of a thousand times more, because of the stack capture and unwinding",
          "Ten times",
          "It depends only on the message length"],
      c: 1,
      why: "Which matters only in a hot path — and `customer not found` on a lookup endpoint is precisely a hot path. The cost is the confirmation, not the reason." },

    { q: "What is `ArgumentNullException.ThrowIfNull(x)` for?",
      a: ["Checking a return value",
          "A one-line guard at the top of a method that produces a better message than a hand-written check, using the caller's expression as the parameter name",
          "Suppressing nullable warnings",
          "Converting null to default"],
      c: 1,
      why: "There is a matching family for ranges, such as ThrowIfNegative. Worth adopting; the messages are better than the ones people write." },

    { q: "Why is returning null or an empty list for a genuine failure also a mistake?",
      a: ["It is not; failures should be silent",
          "It turns an outage into silently missing data, which nobody notices for a week",
          "Because null is never valid",
          "Because callers always check"],
      c: 1,
      why: "The rule cuts both ways: expected outcomes are return values, and genuinely broken things must be loud." },
  ],

  /* ------------------------------------------------------------------ 03 */
  "03-oop": [
    { q: "What is encapsulation actually protecting?",
      a: ["The source code from being read",
          "The object's invariants — the rules that must stay true about its state, no matter who calls it",
          "Performance",
          "The class from being inherited"],
      c: 1,
      why: "A class with a public setter on every field has no invariants and is a struct with extra ceremony. Encapsulation means there is no legal call sequence that leaves the object invalid." },

    { q: "When should you choose an abstract class over an interface?",
      a: ["Always — abstract classes are more powerful",
          "When there is shared implementation and state that every derived type needs, and the types genuinely form one family",
          "When you want multiple inheritance",
          "When the type has no methods"],
      c: 1,
      why: "The hard constraint is single inheritance: an abstract base spends the one inheritance slot a class has. Interfaces are cheap; base classes are a commitment." },

    { q: "What is the difference between `override` and `new` on a method?",
      a: ["They are synonyms",
          "override replaces the virtual method for every caller; new hides it only for callers holding the derived type — call through the base reference and you get the base method",
          "new is faster",
          "override only works on properties"],
      c: 1,
      why: "Method hiding produces behaviour that depends on the static type of the variable, which is almost never what anyone intends. It is a favourite interview trap." },

    { q: "What does this print?",
      code: "class A { public virtual string Who() => \"A\"; }\nclass B : A { public new string Who() => \"B\"; }\n\nA x = new B();\nConsole.WriteLine(x.Who());",
      a: ["A", "B", "It does not compile", "null"],
      c: 0,
      why: "`new` hides rather than overrides, so dispatch uses the static type `A`. Change `new` to `override` and it prints B." },

    { q: "“Composition over inheritance” — what is the practical argument?",
      a: ["Inheritance is slower at runtime",
          "Inheritance couples you to a base class's entire surface and its future changes; composition lets you depend on one small interface you can substitute",
          "Composition uses less memory",
          "Inheritance is deprecated in modern C#"],
      c: 1,
      why: "The fragile base class problem: a change in a base class silently changes every descendant. With composition, the dependency is explicit and swappable — which is also what makes it testable." },

    { q: "The Liskov Substitution Principle is violated. What does that look like in code?",
      a: ["A derived class adds new methods",
          "A derived class throws NotSupportedException from an inherited method, or tightens what the base accepted",
          "A class implements two interfaces",
          "A base class has a virtual method"],
      c: 1,
      why: "The classic example is a read-only collection deriving from a mutable one. If callers of the base type have to check what they actually got, substitution is broken." },

    { q: "Select every statement that is true about polymorphism in C#.",
      a: ["It requires the method to be virtual, abstract, or on an interface",
          "It lets one call site invoke different implementations depending on the runtime type",
          "It is resolved at compile time",
          "Interface implementations participate in it"],
      c: [0, 1, 3],
      why: "Runtime dispatch is the point — that is why it is called dynamic dispatch. Compile-time selection is overload resolution, which is a different mechanism." },

    { q: "What is the Dependency Inversion Principle asking you to invert?",
      a: ["The order of method calls",
          "The direction of the source-code dependency: both the policy and the detail depend on an abstraction the policy owns",
          "The inheritance hierarchy",
          "The database schema"],
      c: 1,
      why: "The interface belongs with the caller, not with the implementation. That is precisely why the repository interface lives in the Application layer and the EF Core class lives in Infrastructure." },
  ],

  /* ------------------------------------------------------------------ 03b */
  "03b-classes-objects-members": [
    { q: "What is the difference between a field and a property?",
      a: ["Only naming convention",
          "A field is storage; a property is a pair of methods that look like storage, so it can validate, compute, raise an event, be virtual or sit on an interface",
          "Properties are slower and should be avoided",
          "Fields cannot be private"],
      c: 1,
      why: "Turning a public field into a property is a binary-breaking change, which is why the rule is simply that fields are always private." },

    { q: "What should a constructor do, and what should it not?",
      a: ["Load everything the object will need from the database",
          "Bring the object into a valid state and stop — no I/O, nothing that can block or fail environmentally, since a constructor cannot be async",
          "Nothing; use an initialise method",
          "Register the object with a static registry"],
      c: 1,
      why: "Validate there and no instance can ever exist broken. Validate in the caller and you are trusting every caller, including the ones not written yet." },

    { q: "Why is calling a `virtual` method from a constructor dangerous?",
      a: ["It is slower",
          "The base constructor runs before the derived class's fields are initialised, so the override executes while the fields it needs are still null",
          "It causes infinite recursion",
          "Virtual calls are not allowed there"],
      c: 1,
      why: "A NullReferenceException inside a method that looks obviously correct, and only for the derived type." },

    { q: "What does `: this(...)` on a constructor do?",
      a: ["Calls the base class constructor",
          "Chains to another constructor in the same class, so validation exists in one place and cannot drift between overloads",
          "Declares a copy constructor",
          "Makes the constructor private"],
      c: 1,
      why: "Every overload should funnel into one real constructor. Duplicated validation across two constructors always drifts apart eventually." },

    { q: "What does the `required` keyword on a property achieve?",
      a: ["It makes the property non-nullable",
          "It moves a run-time null check to compile time: an object initialiser that omits it does not build",
          "It adds a default value",
          "It prevents the property from changing"],
      c: 1,
      why: "Pairs naturally with init, giving immutability without a constructor that takes nine parameters." },

    { q: "What does `init` mean on a property setter?",
      a: ["It runs an initialiser method",
          "The property can be set in an object initialiser and never again",
          "It sets a default value",
          "It makes the property static"],
      c: 1,
      why: "It is also what makes `with` expressions on records work." },

    { q: "Why expose a collection as `IReadOnlyList<T>` rather than `List<T>`?",
      a: ["It is faster",
          "A get-only List property stops the list being replaced but not modified, so callers can Add straight past the method that validates additions",
          "List cannot be a property",
          "It prevents null"],
      c: 1,
      why: "The commonest way encapsulation leaks in real code. It protects against accident, not malice — a determined caller can still cast it back." },

    { q: "What is the difference between `const` and `static readonly`?",
      a: ["None",
          "A const is substituted into the caller's compiled code, so changing it in a library does not reach anything already built",
          "static readonly cannot be public",
          "const works only for strings"],
      c: 1,
      why: "The same versioning trap optional parameters have. Use const only for things true forever, like the value of pi." },

    { q: "`private readonly List<T> _lines = new();` — what does readonly prevent?",
      a: ["Adding to the list",
          "Repointing the field at a different list; the list itself is still mutable",
          "Reading the list",
          "Passing the list to a method"],
      c: 1,
      why: "A distinction that catches everybody once, and the reason a readonly field is not the same as an immutable object." },

    { q: "Why should a property be cheap and repeatable?",
      a: ["Because the compiler inlines it",
          "Because it looks like data — reading it twice should give the same answer and should not hit a database; if it is expensive or can fail, make it a method",
          "Because properties cannot throw",
          "Because of data binding only"],
      c: 1,
      why: "Parentheses are a promise to the reader that something is happening." },

    { q: "What is the default accessibility of a class member with no modifier, and of a top-level class?",
      a: ["public and public",
          "private for the member and internal for the class",
          "internal for both",
          "protected and public"],
      c: 1,
      why: "Both defaults are the safe ones, deliberately. Start at private and justify each step up." },

    { q: "When is `internal` the right modifier?",
      a: ["Almost never",
          "For most classes in an application, which are implementation detail of that application — and InternalsVisibleTo still gives the test project access",
          "Only for generated code",
          "Only inside a library"],
      c: 1,
      why: "The modifier people underuse. public is a promise you have to keep." },

    { q: "What does `public OrderStatus Status { get; private set; }` buy you?",
      a: ["Nothing over a public setter",
          "Everyone can read it and only the class can write it, so state changes must go through methods that check the rules",
          "Thread safety",
          "Automatic change notification"],
      c: 1,
      why: "The most useful encapsulation tool in the language, and one line." },

    { q: "Why is a static mutable collection a problem in a web application?",
      a: ["It is slower than an instance field",
          "It is global state shared across every request and thread, so it needs locking, and it cannot be substituted in a test",
          "Static fields are not allowed",
          "It is garbage collected too early"],
      c: 1,
      why: "The cure is dependency injection with a singleton registration: exactly one instance, and still replaceable in a test." },

    { q: "What happens if a static constructor throws?",
      a: ["The exception is swallowed",
          "The type becomes permanently unusable for the life of the process, which is a spectacularly confusing failure",
          "It runs again on the next use",
          "It is reported at compile time"],
      c: 1,
      why: "Which is a good reason to avoid putting work in one. You also cannot control when it runs." },

    { q: "When would you choose a `record` over a `class`?",
      a: ["Whenever the type has more than three properties",
          "When the thing is defined by its values rather than having an identity — a DTO, a message, a query result",
          "Whenever the type is immutable",
          "For entities, because it is shorter"],
      c: 1,
      why: "An Order has an id and a life of its own, so it is a class. And a record makes a poor EF Core entity, because value equality fights a change tracker that identifies by key." },

    { q: "What does `x with { Qty = 2 }` produce?",
      a: ["A mutation of x",
          "A shallow copy of x with that one property changed",
          "A deep clone",
          "A new reference to the same object"],
      c: 1,
      why: "Shallow: if the record holds a list, both copies share the same list." },

    { q: "When does a `struct` pay off?",
      a: ["Whenever you want value semantics",
          "When the type is small, short-lived and genuinely value-like — a large struct copies every byte on every assignment, and boxing undoes the benefit",
          "For any type with no methods",
          "Whenever allocation matters"],
      c: 1,
      why: "A readonly record struct is usually the shape you want when it does apply." },

    { q: "What is a static factory method good for that a constructor is not?",
      a: ["Performance",
          "A name — two constructors with the same parameter types and different meanings are impossible, and a cached instance cannot be returned from a constructor",
          "Validation",
          "Inheritance"],
      c: 1,
      why: "TimeSpan.FromMinutes is the framework's own example of the naming argument." },

    { q: "In what order do a field initialiser, the base constructor and the constructor body run?",
      a: ["Base, body, initialisers",
          "Field initialisers, then the base constructor, then the body",
          "Body, base, initialisers",
          "Initialisers and body in source order, base last"],
      c: 1,
      why: "Which is why a field initialiser cannot use a constructor parameter — it has not arrived yet." },
  ],

  /* ------------------------------------------------------------------ 03c */
  "03c-inheritance-and-interfaces": [
    { q: "What is polymorphism, in one sentence?",
      a: ["Several methods with the same name and different parameters",
          "One call resolved at run time from the object's actual type, so different kinds behave differently without the caller knowing which it has",
          "Converting one type into another",
          "Using generics to accept any type"],
      c: 1,
      why: "The first option describes overloading, which is a compile-time decision. That distinction is the most common confusion in this topic." },

    { q: "What does `abstract` on a member mean?",
      a: ["It has a default implementation that can be replaced",
          "It has no body and every concrete subclass must supply one, so a missing implementation is a compile error",
          "It cannot be called",
          "It is private to the hierarchy"],
      c: 1,
      why: "Compare the if-chain version, which reports a missing case at run time in production via NotSupportedException." },

    { q: "What is the difference between `override` and `new` on a member?",
      a: ["They are synonyms",
          "override replaces the member and is chosen by the object's real type; new hides it with a separate member chosen by the variable's declared type",
          "new is for static members",
          "override works only on properties"],
      c: 1,
      why: "So with `new`, the same object gives different answers depending on the variable holding it — essentially never intended." },

    { q: "What is the one legitimate use of the `new` modifier on a member?",
      a: ["Improving performance",
          "A base class you do not own adds a member whose name collides with yours, and new lets you keep compiling",
          "Hiding a member from a derived class",
          "Overriding a sealed member"],
      c: 1,
      why: "Without the keyword the compiler warns, and that is a warning never to suppress." },

    { q: "Overloads and overrides are resolved when, respectively?",
      a: ["Both at run time",
          "Overloads at compile time from the declared type; overrides at run time from the actual type",
          "Both at compile time",
          "Overloads at run time; overrides at compile time"],
      c: 1,
      why: "One sentence that explains new versus virtual, why an object variable holding a string picks the object overload, and boxed-int equality." },

    { q: "A class can derive from how many base classes and implement how many interfaces?",
      a: ["One and one",
          "One base class, any number of interfaces",
          "Any number of both",
          "One base class, at most four interfaces"],
      c: 1,
      why: "Usually the deciding constraint: if a type needs to be two things, at most one of them can be a base class." },

    { q: "When does an abstract class beat an interface?",
      a: ["Always; interfaces are ceremony",
          "When there is genuine shared behaviour and state to hold — a fixed algorithm with one varying step, written once",
          "When there will be more than three implementations",
          "When the type must be serialised"],
      c: 1,
      why: "The template method pattern. An interface could not hold the shared parts without repeating them in every implementation." },

    { q: "Why does adding a member to a published interface matter?",
      a: ["It does not; interfaces are versionable",
          "It breaks every existing implementer, unless you give the member a default implementation",
          "It changes the interface's identity",
          "It forces a major runtime version"],
      c: 1,
      why: "Default interface methods are a versioning tool, not a place to put logic." },

    { q: "What makes an abstraction leaky?",
      a: ["Having too few members",
          "Exposing the implementation in its own signature — a DbContext or a SqlDataReader on a repository interface means it could never be implemented a second way",
          "Using generics",
          "Being an interface rather than a class"],
      c: 1,
      why: "The test is whether it could be implemented a second, completely different way. If not, it is paperwork over one implementation." },

    { q: "Why is `IQueryable<T>` on a repository interface a subtle leak?",
      a: ["It is not; it is good design",
          "It looks abstract while requiring every implementation to have a LINQ provider, and it lets callers build queries the repository never anticipated",
          "It cannot be mocked",
          "It is slower than IEnumerable"],
      c: 1,
      why: "An abstraction should be defined by what the caller needs, not by what the implementation happens to have." },

    { q: "An override that throws `NotSupportedException` tells you what?",
      a: ["That the base class is badly named",
          "That the subclass cannot keep the base's promise, so it is not really a subtype — a substitution-principle violation",
          "That the method should be abstract",
          "Nothing; it is a normal pattern"],
      c: 1,
      why: "A phase is not a kind: DispatchedOrder should be a status on Order with the rules inside the methods." },

    { q: "`if (this is SomeSubclass)` inside a base class means what?",
      a: ["Good defensive coding",
          "The hierarchy is upside down — the base now knows its children, so adding one means editing and re-testing the base",
          "The subclass should be sealed",
          "The method should be static"],
      c: 1,
      why: "Make the member abstract and let each subclass answer. Then the compiler reports an omission rather than silently giving the base's answer." },

    { q: "Why is inheriting from a helper class to reuse its methods a mistake?",
      a: ["It is slower",
          "The relationship is `uses`, not `is a` — it welds the two together, exposes the helper's members on your public surface, and burns the one base class you get",
          "Helpers cannot be inherited",
          "It breaks serialisation"],
      c: 1,
      why: "Inject it as a constructor dependency and all four problems disappear at once." },

    { q: "What does `sealed` buy you, beyond design intent?",
      a: ["Smaller assemblies",
          "The JIT can prove no override exists and turn a virtual call into a direct one",
          "Automatic thread safety",
          "Faster construction"],
      c: 1,
      why: "Real but small — seal for design reasons first. You can unseal later; you cannot take back a hierarchy people have built on." },

    { q: "Why is `base.Method()` inside an override worth noticing?",
      a: ["It is required",
          "It means the override is extending rather than replacing — and an override that ignores base entirely is a hint that inheritance was the wrong tool",
          "It causes infinite recursion",
          "It skips the virtual dispatch"],
      c: 1,
      why: "Extending is what inheritance is for. Wholesale replacement usually wants composition." },

    { q: "How deep should an inheritance hierarchy be?",
      a: ["As deep as the domain requires",
          "Two levels is plenty — past that a behaviour's real definition is spread across four files and debugging becomes archaeology",
          "Three to five levels",
          "Depth does not matter"],
      c: 1,
      why: "If you need more, the varying part probably wants to be a separate injected object rather than a subclass." },

    { q: "What can composition do that inheritance cannot?",
      a: ["Nothing; they are equivalent",
          "Be swapped at run time, chosen by configuration, decorated, combined with others, and replaced in a test",
          "Share state",
          "Express an is-a relationship"],
      c: 1,
      why: "A field can be all of those; a base class is fixed at compile time and forever. That is the whole of `favour composition over inheritance`." },

    { q: "How does the runtime find the right method for a virtual call?",
      a: ["By scanning the class hierarchy each time",
          "Through a method table on the object's actual type, which is why the override wins wherever the object goes",
          "By the variable's declared type",
          "By reflection"],
      c: 1,
      why: "A non-virtual call is resolved at compile time and written directly into the IL, which is exactly why `new` follows the variable." },

    { q: "What is the practical value of an interface in application code?",
      a: ["Documentation",
          "It is the seam: the service depends on the interface, so a test supplies an in-memory implementation and never touches a database",
          "It makes the code faster",
          "It allows multiple inheritance of state"],
      c: 1,
      why: "The dependency-inversion half of SOLID, and the most practical reason interfaces exist." },

    { q: "An interface with sixteen members suggests what?",
      a: ["A well-specified contract",
          "That it is probably two interfaces — implementers are being forced to depend on members they do not use",
          "That it should be an abstract class",
          "Nothing; size does not matter"],
      c: 1,
      why: "The interface-segregation principle. Fewer, more meaningful members beat many small technical ones." },
  ],

  /* ------------------------------------------------------------------ 04 */
  "04-generics-delegates-events": [
    { q: "What problem do generics solve that `object` did not?",
      a: ["They make code shorter",
          "They give type safety and reuse at once — no casts, no boxing of value types, and errors at compile time instead of runtime",
          "They allow multiple inheritance",
          "They remove the need for interfaces"],
      c: 1,
      why: "ArrayList stored object: every read needed a cast, every int was boxed, and putting the wrong type in failed at runtime. List<T> makes all three impossible." },

    { q: "What is a delegate, in one sentence?",
      a: ["A class that cannot be inherited",
          "A type-safe reference to a method, so a method can be passed around like a value",
          "An interface with one method",
          "A lambda expression"],
      c: 1,
      why: "“A type-safe function pointer” is the shorter answer and is fine. The key words are type-safe: the signature is part of the delegate type." },

    { q: "What does this print?",
      code: "var actions = new List<Action>();\nfor (int i = 0; i < 3; i++)\n    actions.Add(() => Console.Write(i));\n\nforeach (var a in actions) a();",
      a: ["012", "333", "000", "It does not compile"],
      c: 1,
      why: "A `for` loop has ONE variable `i` for the whole loop, so all three lambdas close over the same variable — which is 3 by the time any of them runs. The fix is a copy inside the body: `int local = i;`. Note that `foreach` is different: since C# 5 its variable is per-iteration, so the same code with foreach prints each value." },

    { q: "What does the `event` keyword add over a plain public delegate field?",
      a: ["Nothing, it is syntax sugar",
          "It restricts outside code to += and -= only — outsiders cannot raise it or wipe the whole invocation list with =",
          "It makes the delegate thread-safe",
          "It makes subscribers run in parallel"],
      c: 1,
      why: "That is exactly why a graded lab in this repository ships a public delegate *field* as its deliberately-wrong stub: the missing keyword is the entire lesson." },

    { q: "Why is `Func<int, string>` the return type `string`?",
      a: ["Func always returns string",
          "In Func<...>, the LAST type parameter is the return type and the rest are the parameters",
          "It is the first parameter",
          "Func has no return type; Action does"],
      c: 1,
      why: "Func<A, B, C> means (A, B) => C. Action<...> has no return type at all, and Predicate<T> is a Func<T, bool> with a name." },

    { q: "What is a closure?",
      a: ["A sealed class",
          "A lambda that captures a variable from the enclosing scope, keeping it alive beyond the scope's normal lifetime",
          "The end of a using block",
          "A finally block"],
      c: 1,
      why: "The compiler lifts the captured variable into a generated class on the heap. That is why a captured loop variable is a bug, and why a captured `this` can keep a whole object alive." },

    { q: "You subscribe to a long-lived object's event from a short-lived one and never unsubscribe. What happens?",
      a: ["Nothing",
          "The publisher's invocation list holds a reference to the subscriber, so the subscriber cannot be collected — a managed memory leak",
          "The event stops firing",
          "The garbage collector removes the subscription automatically"],
      c: 1,
      why: "This is the single most common memory leak in WinForms and WPF applications, and it is why `-=` in Dispose is a habit worth having." },

    { q: "Select everything that is built on delegates under the surface.",
      a: ["LINQ operators like Where and Select", "async continuations", "ASP.NET Core middleware (RequestDelegate)", "The `foreach` statement"],
      c: [0, 1, 2],
      why: "foreach is built on the enumerator pattern, not delegates. The other three are all delegates wearing different clothes — which is why this chapter sits before all of them." },
  ],

  /* ------------------------------------------------------------------ 05 */
  "05-solid-and-patterns": [
    { q: "The Single Responsibility Principle is usually taught as “a class should do one thing”. What is the more useful formulation?",
      a: ["A class should have fewer than 100 lines",
          "A class should have one reason to change — one stakeholder or policy that can force it to be edited",
          "A class should have one public method",
          "A class should have one dependency"],
      c: 1,
      why: "“One thing” is unfalsifiable — any class does one thing at the right zoom level. “One reason to change” is testable: if the finance rules and the export format both force edits here, that is two." },

    { q: "The Open/Closed Principle in a modern codebase most often shows up as what?",
      a: ["Sealing every class",
          "Adding behaviour by registering a new implementation of an existing abstraction, rather than editing a switch statement",
          "Never deleting code",
          "Using inheritance everywhere"],
      c: 1,
      why: "The pipeline-behaviour and strategy shapes are OCP in practice. If adding a new payment method means editing a switch, the switch is the closed thing that should have been open." },

    { q: "What is the Interface Segregation Principle protecting the CALLER from?",
      a: ["Slow method calls",
          "Depending on methods it does not use, so it is forced to recompile or re-stub when those methods change",
          "Null references",
          "Multiple inheritance"],
      c: 1,
      why: "The test-double smell is the giveaway: if a mock needs six methods stubbed to test one behaviour, the interface is too wide." },

    { q: "What is the Repository pattern actually for, and what is the common objection to it over EF Core?",
      a: ["It makes queries faster; there is no objection",
          "It abstracts persistence behind a collection-like interface — but DbSet<T> is already that, so a thin repository over EF Core often only adds a layer that leaks IQueryable anyway",
          "It is required by EF Core",
          "It replaces the need for a database"],
      c: 1,
      why: "Being able to argue both sides is the point of the question. The defensible version is a repository that returns domain aggregates and hides IQueryable entirely, not one with GetAll()." },

    { q: "You are asked to name a design pattern you have used. Which answer lands best?",
      a: ["Singleton, because it is the easiest",
          "One you can point at in your own code, explain what it replaced, and say what it cost",
          "Abstract Factory, because it sounds advanced",
          "All twenty-three Gang of Four patterns"],
      c: 1,
      why: "The follow-up is always “why?”. A pattern named without a problem attached reads as memorised; a pattern with a before-and-after reads as experience." },

    { q: "Why is Singleton the pattern most often described as an anti-pattern today?",
      a: ["It is slow",
          "It hides a global dependency that no constructor declares, which makes code untestable and hides shared mutable state",
          "It does not work with generics",
          "It was removed from C#"],
      c: 1,
      why: "The DI container gives you singleton *lifetime* without the static access point. The lifetime was never the problem; the hidden global was." },

    { q: "Select the patterns you are genuinely likely to meet inside an ASP.NET Core codebase.",
      a: ["Strategy (interchangeable implementations behind one interface)",
          "Decorator (a wrapper adding caching or logging around an existing service)",
          "Mediator / dispatcher (a request object routed to its handler)",
          "Visitor"],
      c: [0, 1, 2],
      why: "Visitor turns up in compilers and expression-tree work, rarely in line-of-business code. The other three are in nearly every DI-based application, often without anyone naming them." },

    { q: "What is the strongest argument AGAINST applying all five SOLID principles to a small project?",
      a: ["They are wrong",
          "Every abstraction has a cost in indirection, and paying it before there is a second implementation buys nothing while making the code harder to read",
          "They only apply to Java",
          "They conflict with each other"],
      c: 1,
      why: "The mature answer is that these are pressure-relief valves, applied where change actually hurts. An interviewer who hears that trusts your judgement more than a recitation." },
  ],

  /* ----------------------------------------------------------------- 05b */
  "05b-readable-code": [
    { q: "A method is declared Get(int d, bool f) and called as Get(30, true). What is the strongest argument against it?",
      a: ["It is too short to be descriptive",
          "Nothing at the call site says what 30 or true mean, so every reader has to open the method to read one line",
          "Boolean parameters are not allowed in C#",
          "int should be long"],
      c: 1,
      why: "Names are the only documentation that is read every time and the only kind the compiler keeps in one place. GetRecentOrders(30, unshippedOnly: true) costs nothing extra to type." },

    { q: "Why is a bool parameter considered a design smell even when it is well named?",
      a: ["It allocates",
          "At the call site it is a bare true or false, which usually means the method does two different things",
          "Booleans cannot be null",
          "It prevents inlining"],
      c: 1,
      why: "A flag that switches behaviour is two methods wearing one name. Named arguments are the cheap fix; two methods are often the right one." },

    { q: "What is the actual rule behind 'methods should be short'?",
      a: ["No method may exceed twenty lines",
          "One level of abstraction per method: it either orchestrates named steps or performs one step, never both",
          "Every method must have a single return",
          "Methods should fit on one screen at 1080p"],
      c: 1,
      why: "Shortness is a consequence, not the goal. The number worth watching is cyclomatic complexity, because that is what decides how many tests it needs." },

    { q: "Why do guard clauses make a method easier to read than nested if/else?",
      a: ["They run faster",
          "Every refusal is stated once at the same indentation, and the happy path ends up last and unindented",
          "They avoid boxing",
          "The compiler optimises them"],
      c: 1,
      why: "The single-exit rule comes from C, where you freed memory by hand. In C# it produces arrow code, where the common case is the most deeply buried thing in the method." },

    { q: "Which of these comments is worth writing?",
      a: ["// increment the counter, above counter++",
          "// Ordered by PlacedAtUtc, not Id: the ids come from a Hi/Lo generator and are not monotonic across instances",
          "// TODO: refactor this",
          "// method to get orders, above GetOrders"],
      c: 1,
      why: "Code says what. A comment earns its space by saying why — the decision, the rejected alternative, the bug that caused it. A comment that describes the code becomes a lie after the first edit." },

    { q: "Why is commented-out code worse than deleted code?",
      a: ["It slows the build",
          "It is read as if it mattered, nobody knows whether it is safe to remove, and Git already remembers the deleted version with its author and date",
          "It breaks IntelliSense",
          "It is included in the compiled assembly"],
      c: 1,
      why: "Every reader spends attention deciding whether the block is relevant. Deleting it loses nothing that source control does not already hold." },

    { q: "What does enforcing invariants in the constructor buy you that validation methods do not?",
      a: ["Better performance",
          "An invalid instance cannot exist at all, so no later method has to re-check it and none can forget",
          "Automatic serialisation",
          "Thread safety"],
      c: 1,
      why: "A class whose properties all have public setters has no invariants, only hopes. Constructor plus get-only properties means valid on creation and valid for life." },

    { q: "Select every statement about DRY that a senior developer would agree with.",
      a: ["It is about one authoritative place for a piece of knowledge, not about text that looks alike",
          "Two blocks that are identical today but change for different reasons should stay separate",
          "Duplication is cheaper than the wrong abstraction, because it is easier to undo",
          "Any repeated line must be extracted into a shared method immediately"],
      c: [0, 1, 2],
      why: "The test is 'if this rule changes, must both places change?'. Merging things that merely look alike is how a helper grows five flags nobody can change safely." },

    { q: "What is the difference between throw; and throw ex; inside a catch block?",
      a: ["None",
          "throw; keeps the original stack trace; throw ex; resets it to that line, so the log points at the catch instead of the bug",
          "throw ex; is faster",
          "throw; only works for custom exceptions"],
      c: 1,
      why: "A favourite interview question because it separates people who have debugged production from people who have not. To add information, wrap in a new exception with the original as the inner one." },

    { q: "Which of these is the honest reason for preferring the simple version over the clever one?",
      a: ["Clever code is always slower",
          "Debugging is harder than writing, so code written at the limit of your cleverness cannot be debugged by you at 2 a.m.",
          "Compilers cannot optimise clever code",
          "Team leads dislike it"],
      c: 1,
      why: "KISS is not a style preference; it is a statement about the relative difficulty of two activities, and the second one always happens under worse conditions." },

    { q: "A colleague's pull request fixes one bug and reformats four hundred lines. What is the problem?",
      a: ["Reformatting is never acceptable",
          "The real change cannot be reviewed, because it is invisible among the noise",
          "It will merge slowly",
          "Formatting changes break Git blame permanently"],
      c: 1,
      why: "Leave the code better than you found it — in its own commit. The boy scout rule and a readable diff are not in conflict as long as they are separated." },

    { q: "When is speculative flexibility — an interface with one implementation, a config setting nobody changes — actually justified?",
      a: ["Always; it is good design",
          "When there is a second implementation, a test double you genuinely need, or a layer boundary you want the compiler to police",
          "Never, under any circumstances",
          "When the team has spare time"],
      c: 1,
      why: "YAGNI is the principle juniors under-apply most, because skipping the abstraction feels like not trying. The reviewer's question is 'what change is this protecting us from?'" },
  ],

  /* ------------------------------------------------------------------ 06 */
  "06-collections-and-linq": [
    { q: "You need to check membership of a 100 000-item set inside a loop. Which collection?",
      a: ["List<T> with Contains", "HashSet<T>", "T[]", "Queue<T>"],
      c: 1,
      why: "List.Contains is O(n) — a linear scan per check, so the loop is O(n·m). HashSet.Contains is O(1) amortised. This exact swap is the most common single-line performance fix in interviews." },

    { q: "What is the real difference between IEnumerable<T> and IQueryable<T>?",
      a: ["IQueryable is faster",
          "IEnumerable executes the lambda in memory as compiled code; IQueryable holds it as an expression tree that a provider can translate — into SQL, for example",
          "IEnumerable cannot be used with LINQ",
          "IQueryable is the async version"],
      c: 1,
      why: "This is the question that separates people who have used EF Core from people who have used it and understood it. Cast a DbSet to IEnumerable and you have just told it to download the table." },

    { q: "What does this actually do against a 5-million-row table?",
      code: "var recent = db.Orders\n    .AsEnumerable()\n    .Where(o => o.CreatedAt > cutoff)\n    .ToList();",
      a: ["Translates the filter to a WHERE clause",
          "Pulls all five million rows into memory and filters them there",
          "Throws at runtime",
          "Returns an empty list"],
      c: 1,
      why: "AsEnumerable is the boundary: everything after it is LINQ to Objects. Moving it after the Where fixes it. The same trap hides behind ToList(), AsParallel() and a foreach." },

    { q: "What is deferred execution?",
      a: ["The query runs on a background thread",
          "Building the query does nothing; it runs when it is enumerated — by foreach, ToList, First, Count and friends",
          "The query result is cached",
          "The query runs at compile time"],
      c: 1,
      why: "It is also why enumerating the same IQueryable twice hits the database twice, and why a query capturing a variable sees the variable's value at *enumeration* time, not at definition time." },

    { q: "Which pair does the same job, and which one throws when there is no match?",
      a: ["First throws, FirstOrDefault returns default",
          "FirstOrDefault throws, First returns null",
          "Both throw",
          "Neither throws"],
      c: 0,
      why: "And Single/SingleOrDefault throw when there is more than one — which is the point of using them: they assert uniqueness rather than silently taking the first of several." },

    { q: "When is Dictionary<TKey,TValue> the wrong choice?",
      a: ["When you need key lookup",
          "When you need the items kept in insertion or sorted order, or when you need to look items up by value",
          "When there are more than 1000 items",
          "When the key is a string"],
      c: 1,
      why: "Dictionary makes no ordering guarantee at all — code that relies on enumeration order is a bug waiting for a rehash. SortedDictionary or a List of pairs is the fix." },

    { q: "Select every LINQ operator that forces immediate execution.",
      a: ["ToList()", "Count()", "Where()", "Any()"],
      c: [0, 1, 3],
      why: "Where, Select, OrderBy, Skip and Take are all lazy — they build the pipeline. Anything that must produce a single value or a materialised collection has to run it." },

    { q: "What does GroupBy return in LINQ?",
      a: ["A Dictionary<TKey, List<T>>",
          "An IEnumerable<IGrouping<TKey, T>> — a sequence of groups, each of which is itself a sequence with a Key",
          "A List<T>",
          "A single object"],
      c: 1,
      why: "IGrouping<TKey,TElement> implements IEnumerable<TElement> and adds Key. Knowing that is what lets you write .Select(g => new { g.Key, Total = g.Sum(x => x.Amount) }) without guessing." },
  ],

  /* ------------------------------------------------------------------ 07 */
  "07-async-and-errors": [
    { q: "What is the primary reason async/await exists in a web API?",
      a: ["To make individual requests faster",
          "To release the thread while waiting on I/O, so a fixed thread pool can serve far more concurrent requests",
          "To run code in parallel on multiple cores",
          "To avoid exceptions"],
      c: 1,
      why: "A single async request is usually very slightly slower. Throughput under concurrency is what improves — and answering “it makes it faster” is the fastest way to reveal you have not thought about it." },

    { q: "What is the difference between asynchrony and parallelism?",
      a: ["They are the same thing",
          "Asynchrony is about not blocking while waiting; parallelism is about doing several things at once on several cores",
          "Parallelism only works with async",
          "Asynchrony requires multiple threads"],
      c: 1,
      why: "Async I/O uses no thread at all while it waits — there is no “async thread”. Parallel.For is the CPU-bound tool; await is the I/O-bound one." },

    { q: "Why does this deadlock in a classic ASP.NET or WinForms application?",
      code: "public string Get()\n{\n    return GetAsync().Result;\n}",
      a: ["Result is not thread-safe",
          "The continuation needs the captured synchronisation context to resume, and the calling thread is blocked inside .Result holding it",
          "GetAsync is never called",
          "It only deadlocks if the network is slow"],
      c: 1,
      why: "ASP.NET Core has no synchronisation context, so this specific deadlock is rarer there — but .Result still blocks a thread-pool thread, which is a throughput bug even when it does not hang." },

    { q: "What does `async void` break?",
      a: ["Nothing",
          "The caller cannot await it and cannot catch its exceptions — an unhandled one crashes the process",
          "It cannot use await inside",
          "It cannot be public"],
      c: 1,
      why: "The one legitimate use is an event handler, because the event signature demands void. Everywhere else, async Task." },

    { q: "What is ConfigureAwait(false) for?",
      a: ["Cancelling the operation",
          "Telling the continuation it does not need the original synchronisation context, so it can resume on any thread-pool thread",
          "Making the call synchronous",
          "Suppressing exceptions"],
      c: 1,
      why: "It matters in library code and in UI/legacy-ASP.NET applications. In an ASP.NET Core application there is no context to capture, so it is largely noise there." },

    { q: "What should a CancellationToken be doing in a Web API endpoint?",
      a: ["Nothing; it is for background services",
          "Flowing from the request all the way to the database call, so an abandoned request stops doing work",
          "Cancelling the whole application",
          "Retrying the operation"],
      c: 1,
      why: "A token that is accepted and then not passed on is worse than none: it advertises cancellation the caller does not get. Passing it to EF Core and HttpClient is the whole job." },

    { q: "Select every statement that is true about exceptions in C#.",
      a: ["`throw;` preserves the original stack trace; `throw ex;` resets it",
          "A `finally` block runs whether or not an exception was thrown",
          "Catching Exception and logging it is always correct",
          "An exception filter (`catch (X e) when (...)`) is evaluated before the stack unwinds"],
      c: [0, 1, 3],
      why: "Catch-all-and-log is how a corrupted state gets treated as a warning. Catch what you can actually handle; let the rest reach a global handler that turns it into one consistent error response." },

    { q: "An awaited Task that faulted throws what, and a `.Wait()`ed one throws what?",
      a: ["Both throw the original exception",
          "await rethrows the original exception; Wait/Result wrap it in an AggregateException",
          "Both throw AggregateException",
          "Neither throws"],
      c: 1,
      why: "That unwrapping is another reason to prefer await: your catch blocks match the exception you actually expect, instead of an AggregateException you have to unwrap." },
  ],

  /* ------------------------------------------------------------------ 08 */
  "08-disposal-and-thread-safety": [
    { q: "What does the garbage collector NOT clean up?",
      a: ["Managed objects with no references",
          "Unmanaged resources: file handles, sockets, database connections — anything the runtime did not allocate",
          "Large objects",
          "Static fields"],
      c: 1,
      why: "That gap is the entire reason IDisposable exists. The GC may eventually run a finalizer, but “eventually” is not a connection-pool strategy." },

    { q: "What does a `using` statement compile to?",
      a: ["A finally block that calls Dispose(), so it runs even when the body throws",
          "A call to the garbage collector",
          "A lock",
          "Nothing at runtime"],
      c: 0,
      why: "try/finally is the whole mechanism. `await using` does the same with DisposeAsync, and the finally still runs when the body throws — which is what makes a buffered writer flush." },

    { q: "Which of these generational facts about the .NET GC is true?",
      a: ["Gen 0 collections are cheap and frequent; surviving objects are promoted to gen 1 then gen 2",
          "Gen 2 is collected most often",
          "The GC compacts the large object heap on every collection",
          "Objects are collected in the order they were allocated"],
      c: 0,
      why: "The generational hypothesis is that most objects die young. Objects over 85 000 bytes go straight to the Large Object Heap, which is not compacted by default — the source of fragmentation in long-running services." },

    { q: "What is boxing, and when does it cost you?",
      a: ["Wrapping a class in a struct",
          "Copying a value type onto the heap so it can be treated as object — allocating each time, typically inside a hot loop or a non-generic collection",
          "Serialising to JSON",
          "Storing a value in a variable"],
      c: 1,
      why: "The sneaky cases: calling a non-overridden ToString on a struct, storing an int in an ArrayList, and using a struct as a Dictionary key when it does not override Equals/GetHashCode." },

    { q: "How do you leak memory in a garbage-collected language?",
      a: ["You cannot",
          "By keeping a reference alive: a static collection that grows, an unsubscribed event handler, or a captured `this` in a long-lived closure or timer",
          "By forgetting to call free()",
          "By allocating too many small objects"],
      c: 1,
      why: "The GC frees what is unreachable. A leak is therefore always somebody still holding a reference — which is why the fix is found by looking at roots, not at allocations." },

    { q: "Why is `count++` not thread-safe?",
      a: ["It is thread-safe",
          "It is three operations — read, add, write — and two threads can interleave between them, losing an increment",
          "The compiler removes it",
          "Only ++ on longs is unsafe"],
      c: 1,
      why: "Interlocked.Increment makes it a single atomic instruction. A `lock` also works and is what you need the moment more than one field must change together." },

    { q: "Select every collection or approach that is safe to use from several threads at once WITHOUT your own lock.",
      a: ["ConcurrentDictionary<K,V>", "Interlocked.Increment on an int field", "Dictionary<K,V>", "An immutable object you only read"],
      c: [0, 1, 3],
      why: "Dictionary is not merely “might return stale data” — a concurrent write during a resize can corrupt it into an infinite loop on read. Immutability is the cheapest thread safety there is." },

    { q: "Your Dispose method might be called twice. What must be true?",
      a: ["It must throw ObjectDisposedException the second time",
          "It must be idempotent — the second call does nothing and does not throw",
          "It must call GC.Collect()",
          "It cannot be called twice"],
      c: 1,
      why: "A `_disposed` flag guarded at the top is the standard shape. Throwing on a second Dispose turns a harmless double-dispose in a using-inside-using into a crash." },
  ],

  /* ----------------------------------------------------------------- 08b */
  "08b-looks-simple-isnt": [
    { q: "What is printed?",
      code: "decimal price = 200m;\ndecimal discount = price * (5 / 100);\n\nConsole.WriteLine(discount);",
      a: ["10", "0", "0.05", "It does not compile"],
      c: 1,
      why: "5 / 100 is integer division — decided entirely by the operands and not at all by the type it is assigned to. It evaluates to 0 before decimal is ever involved. 5m / 100m, or better 0.05m, fixes it." },

    { q: "Adding 0.1 to a double ten times and comparing the result with 1.0 gives False. Why?",
      a: ["Rounding in Console.WriteLine",
          "double is a binary fraction and one tenth has no exact binary representation, so each addition adds a slightly wrong value",
          "The loop runs eleven times",
          "double comparison uses a tolerance"],
      c: 1,
      why: "Never compare floating-point values with ==. decimal is a decimal fraction, so tenths are exact — which is precisely why money goes in it." },

    { q: "Two string variables hold the same text. Which comparison can return False?",
      a: ["a == b where both are declared as string",
          "(object)a == (object)b where one string was built at run time rather than written as a literal",
          "string.Equals(a, b, StringComparison.Ordinal)",
          "a.Equals(b)"],
      c: 1,
      why: "Casting to object changes which == the compiler picks, at compile time, to the reference comparison. Identical literals are interned and still match; a string built at run time is a different object." },

    { q: "Why does points[0].X = 99 fail to compile when points is a List of a mutable struct, while array[0].X = 99 compiles?",
      a: ["Lists are read-only",
          "The list indexer is a property and returns a copy, so writing to the copy would be pure waste; an array indexer returns a reference to the element",
          "Structs cannot be stored in a List",
          "The array version is also an error"],
      c: 1,
      why: "The compiler error is the language stopping the silent version of the same bug — foreach over a list of structs modifies copies and the list never hears about it. Prefer readonly record struct." },

    { q: "You override Equals on a class and use it as a dictionary key. Lookups with an equal-but-different instance return false. Why?",
      a: ["Dictionary uses reference equality only",
          "GetHashCode was not overridden, so equal objects land in different buckets and Equals is never even called",
          "The key must implement IComparable",
          "Dictionary requires a record type"],
      c: 1,
      why: "Hash first to pick a bucket, then Equals within it. The lookup is not slow, it is wrong. The compiler warns — CS0659 — which is an argument for keeping the warning count at zero." },

    { q: "A LINQ query over a Random is summed twice and gives two different totals. What is the mechanism?",
      code: "var noisy = numbers.Select(n => n + rng.Next(100));\n\nConsole.WriteLine(noisy.Sum());\nConsole.WriteLine(noisy.Sum());",
      a: ["Random is not thread-safe",
          "Deferred execution: Select built a recipe, and each Sum() runs the whole recipe again from the start",
          "Sum caches its result incorrectly",
          "The array is modified between the calls"],
      c: 1,
      why: "Multiple enumeration is the general bug, and it is far worse over IQueryable — three enumerations are three round trips to the database. Materialise once with ToList when you stop composing." },

    { q: "A method opens a StreamReader in a using and returns lines with yield return. When is the file actually opened?",
      a: ["When the method is called",
          "When something first enumerates the returned sequence",
          "When the using block is written",
          "Immediately, on a background thread"],
      c: 1,
      why: "yield return rewrites the method into a state machine: calling it constructs the machine and runs none of the body. Deleting the file between the call and the enumeration succeeds, and the enumeration then throws." },

    { q: "Three lambdas added inside a for loop all print 3. The same shape inside a foreach prints 0, 1, 2. Why the difference?",
      a: ["foreach is evaluated eagerly",
          "A for loop has one variable for the whole loop and the lambdas capture that variable; C# 5 made the foreach variable fresh per iteration",
          "Lambdas capture values, not variables",
          "The for loop runs one extra time"],
      c: 1,
      why: "Closures capture the variable, not its value. The for loop was deliberately left alone because i is genuinely one variable the body may change — the inconsistency is the cost of not breaking existing code." },

    { q: "Calling an async method with .Result inside an ASP.NET Framework controller hangs. What exactly is deadlocked?",
      a: ["The database connection pool",
          "The continuation after the await needs the synchronisation context, and .Result is blocking the thread that owns it",
          "The garbage collector",
          "The HTTP client's socket"],
      c: 1,
      why: "async all the way up is the fix. ConfigureAwait(false) breaks the cycle but treats the symptom, and on ASP.NET Core there is no context, so the same code merely wastes a thread instead of hanging." },

    { q: "Select every true statement about DateTime.",
      a: ["Comparison ignores Kind, so a Utc value and an Unspecified value are compared as raw ticks",
          "AddHours(24) and AddDays(1) can give different answers on a daylight-saving boundary for a local value",
          "DateTime.Now records which time zone it came from",
          "Storing local times in a database column is safe as long as the server never moves"],
      c: [0, 1],
      why: "Store and compute in UtcNow, render local, and use DateTimeOffset when the offset itself matters. 'What is the difference between Now and UtcNow' is a screening question whose real answer is the second half." },

    { q: "Removing items with RemoveAt inside a forward for loop leaves some of them behind. Why, and what is worse about it than the foreach version?",
      a: ["It throws, which is worse than a silent skip",
          "Each removal shifts the rest down while i++ moves on, so every removal skips its neighbour — and unlike foreach it does not throw, so it reaches production",
          "RemoveAt is O(n)",
          "The list is copied on each removal"],
      c: 1,
      why: "foreach checks a version number and throws loudly. The three fixes, in order: RemoveAll with a predicate, iterate a copy, or walk the for loop backwards." },

    { q: "Which question is most likely to explain output that surprises you in C#?",
      a: ["Which framework version is this?",
          "When does this actually run, am I holding the thing or a copy of it, and which overload did the compiler pick?",
          "Is the garbage collector running?",
          "Is this code thread-safe?"],
      c: 1,
      why: "Those three models cause almost every puzzle in this chapter, and unlike a memorised list of gotchas they generalise to the one you have not met yet." },
  ],

  /* ----------------------------------------------------------------- 08c */
  "08c-beginner-mistakes": [
    { q: "Wrapping a crashing line in try { } catch { } stops the crash. Why is that usually the most expensive thing a beginner does?",
      a: ["Exceptions are slow",
          "The failure did not go away, it became a wrong answer that nobody reports — with no log, no stack trace and no idea which run was bad",
          "catch blocks prevent inlining",
          "It hides compiler warnings"],
      c: 1,
      why: "A crash is information. Catch the narrowest type you can act on, at the layer that can act on it, and let the rest reach one handler at the top." },

    { q: "When should you prefer int.TryParse over try { int.Parse } catch?",
      a: ["Never; they are identical",
          "Whenever failure is expected rather than exceptional — an exception captures a stack trace, which is not free",
          "Only in loops",
          "Only when parsing user input from a database"],
      c: 1,
      why: "Exceptions as control flow is the mistake that follows the empty catch. Try-methods exist for the case where bad input is normal." },

    { q: "A repository method returns null when no rows match. What does that cost?",
      a: ["Nothing; it is the standard pattern",
          "Every call site needs a null check forever, one will be forgotten, and 'nothing found' becomes indistinguishable from 'you asked wrongly'",
          "An extra allocation",
          "It breaks LINQ"],
      c: 1,
      why: "An empty collection is already the right answer for 'no rows': you can iterate it and count it. Return IReadOnlyList, and throw for invalid arguments so the stack trace names the caller." },

    { q: "What is wrong with this code?",
      code: "var name = \"  Mario Rossi  \";\nname.Trim();\nConsole.WriteLine($\"[{name}]\");",
      a: ["Trim only removes leading spaces",
          "Strings are immutable, so Trim returns a new string and the result was discarded",
          "The interpolation is wrong",
          "Trim needs an argument"],
      c: 1,
      why: "A string method is a question, not a command. Once that model is in place, the whole family of these disappears at once — assign the result." },

    { q: "Why is building a CSV with csv += line inside a loop over 50 000 rows a production problem?",
      a: ["It uses too much stack",
          "Each += allocates a whole new string and copies everything so far, so the work is quadratic and the garbage is enormous",
          "String concatenation is not thread-safe",
          "The result exceeds the maximum string length"],
      c: 1,
      why: "StringBuilder is one growable buffer appended in place. Do not over-correct, though: a + b + c on a single line compiles to one efficient call and needs no builder." },

    { q: "What actually prevents SQL injection?",
      a: ["Escaping quotes in the input",
          "Sending the command text and the values separately, as parameters, so a value can never be parsed as SQL",
          "Validating that the input contains no SQL keywords",
          "Using a stored procedure, always"],
      c: 1,
      why: "'You escape the input' is the wrong answer that sounds right. Note the ORM caveat: FromSqlInterpolated parameterises the holes, FromSqlRaw with an interpolated string does not." },

    { q: "Why does a leaked database connection show up as a timeout in an unrelated part of the app half an hour later?",
      a: ["The garbage collector runs late",
          "The connection was never returned to the pool, so eventually some other request cannot get one — and that request is the one that reports the error",
          "SQL Server caches the failure",
          "The connection string is wrong"],
      c: 1,
      why: "A garbage collector handles memory; a file handle, a socket and a connection are not memory. Anything IDisposable goes in a using — with HttpClient as the deliberate exception that is reused." },

    { q: "Select every true statement about async in an ASP.NET Core application.",
      a: ["It exists to stop occupying a thread while waiting on something outside the process",
          "async void makes exceptions uncatchable by the caller and should be limited to event handlers",
          "Wrapping a database call in Task.Run makes it asynchronous",
          "A CancellationToken should be passed down the chain"],
      c: [0, 1, 3],
      why: "Task.Run moves the waiting to another thread rather than removing it, so the thread pool is busier for no benefit. Async is for I/O, all the way up, never mixed with a blocking wait." },

    { q: "Why is a DbContext registered as a singleton a serious bug rather than an optimisation?",
      a: ["It is slower",
          "A web server handles requests in parallel, and DbContext is neither thread-safe nor intended to accumulate every entity it has ever loaded",
          "Singletons cannot be disposed",
          "Entity Framework forbids it at startup"],
      c: 1,
      why: "Shared means immutable. Anything changeable and shared is written by several requests at once, which is untestable locally, intermittent in production, and shows one user another user's data." },

    { q: "Your project has sixty compiler warnings and builds fine. What is the argument for fixing them?",
      a: ["Warnings slow the build",
          "Sixty warnings is the same as none, because nobody can see the one that matters — and CS0659 and CS4014 are real bugs that have not happened yet",
          "They appear in the published output",
          "They block deployment"],
      c: 1,
      why: "Keep the count at zero from the first day, which is the only time it is cheap. TreatWarningsAsErrors makes it stick; suppress a specific one with a comment saying why." },

    { q: "What is 'testing the mock', and why is such a test worth deleting?",
      a: ["Using a mock instead of a real database",
          "Asserting that the fake was called in a particular way rather than asserting what the code produced — so it passes when the code is wrong and fails when it is merely rearranged",
          "Writing tests before the code",
          "Mocking a type you own"],
      c: 1,
      why: "If the assertion is about how the code worked rather than what it produced, it is usually coupling the test to the implementation and nothing else." },

    { q: "You have inherited a codebase with no tests. What is the best first move?",
      a: ["Retrofit coverage across the whole project before changing anything",
          "Put a test around the next thing you have to change, starting with the pure logic that needs no setup",
          "Rewrite the parts you do not understand",
          "Set a coverage target and work towards it"],
      c: 1,
      why: "Over a few months the tested area becomes exactly the area that changes, which is where tests are worth most. Chasing a coverage number produces tests that assert nothing." },

    { q: "Why is working for two days without committing a real cost rather than a matter of tidiness?",
      a: ["The repository gets out of date",
          "There is no point to go back to when it worked an hour ago and you cannot remember what changed",
          "Git compresses small commits better",
          "The build server cannot see your work"],
      c: 1,
      why: "Commit whenever it builds and the tests pass, with a message that says why — the diff already says what — and keep refactoring in its own commit so the real change stays reviewable." },

    { q: "A team-mate is stuck. What does a team usually expect?",
      a: ["Never ask; work it out yourself",
          "Genuine attempts for roughly half an hour, then a question saying what you tried, what happened and what you think is going on",
          "Ask immediately to avoid wasting time",
          "Only ask the team lead, privately"],
      c: 1,
      why: "Not asking after that is the thing that gets noticed, not asking. Half the time you solve it while writing the message, which is why rubber ducking has a name." },
  ],

  /* ------------------------------------------------------------------ 09 */
  "09-databases-and-sql-server": [
    { q: "What is the difference between a primary key and a unique constraint?",
      a: ["None",
          "A table has at most one primary key and it cannot be NULL; you can have many unique constraints and (in SQL Server) one NULL each",
          "Unique constraints cannot be indexed",
          "Primary keys must be integers"],
      c: 1,
      why: "The NULL behaviour is the part people miss, and it is where the interview follow-up goes: a UNIQUE column in SQL Server allows exactly one NULL, unlike some other engines." },

    { q: "Which type should hold a money amount in SQL Server?",
      a: ["float", "decimal(18,2) (or numeric)", "real", "money, always"],
      c: 1,
      why: "float is binary floating point: 0.1 has no exact representation, and totals drift. `money` exists but has only four decimal places and awkward rounding. decimal is the defensible answer." },

    { q: "You store a UTC instant. Which SQL Server type?",
      a: ["datetime", "datetime2 (or datetimeoffset when the offset matters)", "smalldatetime", "varchar(30)"],
      c: 1,
      why: "datetime has ~3.33 ms precision and a 1753 lower bound; datetime2 is more precise, wider and smaller. datetimeoffset is the one that actually carries a zone offset." },

    { q: "What does `WHERE Discount = NULL` return?",
      a: ["Every row where Discount is null",
          "No rows — comparison with NULL is unknown, and you need IS NULL",
          "Every row",
          "A syntax error"],
      c: 1,
      why: "Three-valued logic. The same trap catches `NOT IN (subquery)` when the subquery can produce a NULL: the whole predicate becomes unknown and you get zero rows." },

    { q: "Third normal form, informally?",
      a: ["Every table has an integer id",
          "Every non-key column depends on the key, the whole key, and nothing but the key",
          "No table has more than ten columns",
          "Every column is indexed"],
      c: 1,
      why: "The follow-up is always when you would denormalise — a reporting table, or a stored total that must reflect the price *at the time of sale* rather than today's price." },

    { q: "A clustered index on a SQL Server table is what, physically?",
      a: ["A copy of the data sorted by the key",
          "The table itself — the rows are stored in the clustered key's order, so there is at most one per table",
          "A pointer list",
          "An in-memory cache"],
      c: 1,
      why: "That is why a wide or randomly-ordered clustered key hurts everything: every non-clustered index carries the clustered key as its row locator, and random inserts cause page splits." },

    { q: "Select every reason a foreign key constraint is worth having even though the application “already checks”.",
      a: ["The application is not the only writer — scripts, imports and other services exist",
          "It documents the relationship for anyone reading the schema",
          "It lets the query optimiser make better decisions",
          "It makes inserts faster"],
      c: [0, 1, 2],
      why: "Constraints cost a little on write and save you from the one bad import that nobody remembers running. Data outlives every application that touches it." },

    { q: "What is the difference between DELETE, TRUNCATE and DROP?",
      a: ["They are the same",
          "DELETE removes rows and is logged per row and filterable; TRUNCATE empties the whole table quickly with minimal logging; DROP removes the table itself",
          "TRUNCATE can have a WHERE clause",
          "DROP only removes data"],
      c: 1,
      why: "TRUNCATE also resets IDENTITY and cannot run when a foreign key references the table — two facts that turn up in real migration scripts." },

    { q: "An Orders table has an IDENTITY primary key and the values run 1, 2, 3, 7, 1004. Is the table broken?",
      a: ["Yes — the identity seed has been corrupted",
          "No — IDENTITY guarantees uniqueness, not continuity: a rolled-back transaction consumes its number, and a restart can jump the counter because values are cached in blocks",
          "Yes — someone has deleted rows",
          "No, but the gaps mean the clustered index needs rebuilding"],
      c: 1,
      why: "Gaps are normal behaviour. The consequence that matters: never use IDENTITY for a document number the business or the tax authority reads." },

    { q: "Italian invoice numbering (numerazione progressiva) must be sequential and without holes. How should the number be generated?",
      a: ["An IDENTITY column on the Invoices table",
          "Separately from the primary key, inside the transaction, from a counter row updated and read per year and per series",
          "A GUID, formatted as a number",
          "A SEQUENCE, which is gapless by design"],
      c: 1,
      why: "A SEQUENCE gaps for the same reasons IDENTITY does — it is a better tool, not a gapless one. Keep IDENTITY as the meaningless internal key and generate the legal number deliberately." },

    { q: "What does a SEQUENCE give you that an IDENTITY column cannot?",
      a: ["Guaranteed continuity with no gaps",
          "The number before the insert, and one series that several tables can share",
          "Automatic thread safety",
          "A smaller storage footprint"],
      c: 1,
      why: "NEXT VALUE FOR hands you the value up front, so you can build the row or tell another system first — and Orders and Quotes can draw from the same run of document numbers." },
  ],

  /* ------------------------------------------------------------------ 10 */
  "10-tsql-querying": [
    { q: "In what order does SQL Server logically process a SELECT statement?",
      a: ["SELECT → FROM → WHERE → GROUP BY → ORDER BY",
          "FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY",
          "WHERE → FROM → SELECT → GROUP BY",
          "The order it is written in"],
      c: 1,
      why: "That order explains two everyday errors: you cannot use a SELECT alias in WHERE (SELECT has not happened yet), and you can use it in ORDER BY (it has)." },

    { q: "What is the difference between WHERE and HAVING?",
      a: ["None",
          "WHERE filters rows before grouping; HAVING filters groups after aggregation",
          "HAVING is faster",
          "WHERE cannot be used with GROUP BY"],
      c: 1,
      why: "Which is why `HAVING COUNT(*) > 1` is how you find duplicates, and putting a non-aggregate filter in HAVING is a performance bug — it filters later than it needed to." },

    { q: "A LEFT JOIN plus a WHERE clause on the right-hand table's column. What happens?",
      a: ["Nothing changes",
          "The WHERE filters out the unmatched rows, silently turning it into an INNER JOIN — the condition belongs in the ON clause instead",
          "It throws an error",
          "It returns duplicates"],
      c: 1,
      why: "This is the most common join bug there is. `WHERE b.Status = 'X'` discards rows where b is NULL; `AND b.Status = 'X'` in the ON clause keeps them." },

    { q: "What does a CTE (WITH ... AS) give you that a subquery does not?",
      a: ["Better performance, always",
          "Readability, reuse of the same block more than once in the query, and recursion",
          "A permanent table",
          "Automatic indexing"],
      c: 1,
      why: "A non-recursive CTE is usually inlined by the optimiser, so treating it as a performance tool is wrong. Recursion — walking a hierarchy — is the capability a subquery does not have." },

    { q: "You need each row plus a running total of the rows before it. Which tool?",
      a: ["A correlated subquery",
          "A window function: SUM(x) OVER (ORDER BY d ROWS UNBOUNDED PRECEDING)",
          "A cursor",
          "GROUP BY"],
      c: 1,
      why: "Window functions keep the individual rows and add an aggregate alongside — that is exactly what GROUP BY cannot do. ROW_NUMBER, RANK, DENSE_RANK and LAG/LEAD are the others worth knowing." },

    { q: "What is the safe way to page a large sorted result?",
      a: ["OFFSET n ROWS FETCH NEXT m ROWS ONLY, on any page",
          "Keyset pagination — WHERE (SortKey, Id) > (lastKey, lastId) — because OFFSET has to count and discard every skipped row",
          "SELECT TOP with a random seed",
          "Fetching everything and paging in C#"],
      c: 1,
      why: "OFFSET is fine for page 2 and terrible for page 2000. Keyset paging is also stable: inserting a row mid-scroll cannot make OFFSET show you the same row twice." },

    { q: "Why should a query use parameters rather than string concatenation?",
      a: ["It is shorter to write",
          "It prevents SQL injection AND lets the server reuse the cached execution plan",
          "It is required by EF Core",
          "It makes the query case-insensitive"],
      c: 1,
      why: "Both halves matter. The plan-cache half is the one that impresses: concatenated literals produce a new plan per distinct value and blow out the cache." },

    { q: "Select every statement that is true of a stored procedure compared with a parameterised query sent from C#.",
      a: ["It can be granted permissions independently of the underlying tables",
          "It keeps SQL in the database, versioned separately from the application",
          "It is automatically immune to SQL injection",
          "It can contain procedural logic and multiple statements in one round trip"],
      c: [0, 1, 3],
      why: "A stored procedure that builds dynamic SQL by concatenation is just as injectable. The security benefit is the permission boundary, not the procedure itself." },

    { q: "Two sessions run the same MERGE upsert at the same instant and one fails with a primary key violation. Why?",
      a: ["MERGE is not supported under concurrency",
          "MERGE does not take a range lock by default, so both sessions can find no match and both insert — it needs WITH (HOLDLOCK) on the target to be safe",
          "The transaction isolation level was set to READ UNCOMMITTED",
          "The primary key is missing an index"],
      c: 1,
      why: "Nothing in the syntax suggests HOLDLOCK is needed, which is why it is forgotten. Many teams settle on an UPDATE with UPDLOCK/HOLDLOCK followed by an INSERT when @@ROWCOUNT is 0." },

    { q: "Asked about MERGE in an interview, which answer is strongest?",
      a: ["I have never used it",
          "I know it, and I avoid it for upserts because it needs HOLDLOCK to be concurrency-safe and that is easy to forget",
          "I use it everywhere — it is more concise than UPDATE plus INSERT",
          "It is deprecated in modern SQL Server"],
      c: 1,
      why: "Knowing a feature and choosing not to use it, with a reason, is a stronger answer than not knowing it — and stronger than using it without the caveat." },
  ],

  /* ----------------------------------------------------------------- 10b */
  "10b-views-functions-procedures": [
    { q: "Does adding a view make a query faster?",
      a: ["Yes — the result set is cached by the engine",
          "No — a normal view stores no data; its definition is expanded into the calling query and optimised as one statement",
          "Yes — views are precompiled like stored procedures",
          "Only if the view has an ORDER BY"],
      c: 1,
      why: "A view saves typing, not work. The exception is an indexed view, which is genuinely materialised — and pays for it on every write to the base tables." },

    { q: "What is the strongest reason to create a view?",
      a: ["To speed up a slow join",
          "To give callers a stable contract over a schema you may change, and to grant access to some columns without granting it on the base table",
          "To store the result so it does not have to be recalculated",
          "Because stored procedures cannot contain joins"],
      c: 1,
      why: "Both real reasons are about contracts. The permission boundary in particular is enforced by the engine, and it is a good interview answer." },

    { q: "What does WITH SCHEMABINDING plus a unique clustered index turn a view into?",
      a: ["A read-only view that runs faster",
          "An indexed (materialised) view — real stored data, maintained automatically on every write to the base tables",
          "A stored procedure",
          "A temporary table rebuilt nightly"],
      c: 1,
      why: "You have moved cost from reads onto writes. It also locks the base tables against column changes until the view is dropped — correctness at the price of deployment friction." },

    { q: "Which of these does an indexed view require? (Select every one that applies.)",
      a: ["WITH SCHEMABINDING",
          "A unique clustered index",
          "COUNT_BIG(*) rather than COUNT(*) when aggregating",
          "An OUTPUT parameter"],
      c: [0, 1, 2],
      why: "OUTPUT parameters belong to stored procedures. The other three are hard requirements, and COUNT_BIG is the one people forget." },

    { q: "Why is this query slow even though the execution plan looks cheap?",
      code: "SELECT Id, dbo.fn_OrderTotal(Id) FROM dbo.Orders;   -- fn_OrderTotal is a scalar UDF",
      a: ["The plan is accurate; the slowness is elsewhere",
          "The scalar function runs once per row, and the optimiser cannot see inside it so it costs the call at nearly nothing — the plan is lying",
          "SELECT * is implied and reads every column",
          "Scalar functions always cause a table scan of OrderLines"],
      c: 1,
      why: "This is what makes it hard to find: the plan looks fine while the query takes minutes. Historically it also removed parallelism from the whole plan." },

    { q: "What makes a table-valued function INLINE rather than multi-statement?",
      a: ["It is marked with the INLINE keyword",
          "It is a single RETURN (SELECT …) with no BEGIN/END — so the optimiser expands it into the calling query like a view",
          "It returns fewer than 1000 rows",
          "It has no parameters"],
      c: 1,
      why: "An inline TVF is effectively a view that takes parameters, and it should be your default. A multi-statement TVF materialises into a table variable the optimiser cannot see through." },

    { q: "A colleague needs a parameterised query that other queries will filter and join further. Which object should they write?",
      a: ["A scalar function",
          "An inline table-valued function",
          "A stored procedure",
          "A multi-statement table-valued function"],
      c: 1,
      why: "It composes — you can add WHERE, ORDER BY and CROSS APPLY on top and still get one set-based plan. A stored procedure cannot be used inside a query at all." },

    { q: "What is the practical difference between a function and a stored procedure?",
      a: ["Functions are faster",
          "A function answers a question and can be used inside a SELECT or WHERE but cannot modify data; a procedure performs an action, can manage transactions, and cannot be called from inside a query",
          "Procedures cannot take parameters",
          "Functions cannot take parameters"],
      c: 1,
      why: "The usable rule: if it needs a transaction it is a procedure; if it belongs in a WHERE clause it is a function." },

    { q: "Why does SET XACT_ABORT ON belong at the top of a stored procedure?",
      a: ["It makes the procedure run faster",
          "It guarantees that an error aborts the whole transaction, instead of letting a statement fail and leave the transaction open holding locks",
          "It suppresses the “rows affected” messages",
          "It enables TRY/CATCH"],
      c: 1,
      why: "SET NOCOUNT ON is the one that suppresses the chatter — also not cosmetic, since those are network round-trips. XACT_ABORT is the one that prevents an abandoned open transaction." },

    { q: "Inside a CATCH block, why prefer a bare THROW over RAISERROR?",
      a: ["RAISERROR is deprecated and no longer runs",
          "Bare THROW re-raises the original error with its number, message and line number; RAISERROR always creates a new error and loses them",
          "THROW is faster",
          "RAISERROR cannot be used inside a CATCH block"],
      c: 1,
      why: "The exact parallel of throw; versus throw ex; in C#. Interviewers who know T-SQL like that connection." },

    { q: "A stored procedure was fast for months and is now slow. The code and the data volumes did not change materially. What do you suspect?",
      a: ["A missing index that was dropped",
          "Parameter sniffing — the cached plan was compiled for whatever parameter values were passed after the last recompile",
          "The statistics have been disabled",
          "The procedure was recompiled into a worse language"],
      c: 1,
      why: "A plan that suits a customer with three orders is disastrous for one with 400,000. Confirm with OPTION (RECOMPILE); fix indexes first, then RECOMPILE or OPTIMIZE FOR UNKNOWN." },

    { q: "Why does this EF Core code throw?",
      code: "db.Orders.FromSqlInterpolated($\"EXEC dbo.usp_GetOrders {id}\")\n         .Where(o => o.Total > 100)\n         .ToListAsync();",
      a: ["FromSqlInterpolated is vulnerable to injection and is blocked",
          "You cannot compose LINQ on top of a stored procedure call — EF cannot wrap EXEC in a subquery",
          "The entity needs HasNoKey()",
          "EXEC must be uppercase"],
      c: 1,
      why: "Filter inside the procedure, or materialise first and accept that the filtering moved into your process. Views and inline TVFs compose; procedures do not — a concrete reason to prefer them for queries." },

    { q: "Which EF Core configuration lets a database function be called INSIDE a LINQ query and still translate to SQL?",
      a: ["ToView()",
          "HasDbFunction()",
          "HasNoKey()",
          "FromSqlRaw()"],
      c: 1,
      why: "HasDbFunction maps a CLR method to a SQL function, so an inline TVF becomes composable from C#. ToView maps a read-only view; HasNoKey declares a keyless result shape." },

    { q: "Asked “should business logic live in the database?”, which answer shows the most judgement?",
      a: ["Always in the database — it is closer to the data",
          "Never in the database — logic belongs in C#",
          "Business rules in the domain where they are tested, bulk set-based work in the database — and consistency with what the team already does beats either preference",
          "It does not matter as long as it is documented"],
      c: 2,
      why: "The worst outcome is logic in both places and a rule in neither. Arguing both sides and then committing is what the question is actually testing." },

    { q: "A trigger's audit logic works for single-row updates and silently misses rows on a multi-row UPDATE. What was misunderstood?",
      a: ["Triggers cannot see the old values",
          "A trigger fires once per STATEMENT, not once per row — `inserted` and `deleted` are tables, so the logic must be set-based",
          "AFTER UPDATE triggers only see the first row by design",
          "The trigger needed SET NOCOUNT ON"],
      c: 1,
      why: "Code written as though the trigger were per-row is the classic trigger bug. It passes every test done by hand and fails the first time somebody updates a batch." },

    { q: "A DBA adds a trigger to a table and an EF Core application that nobody touched starts throwing on save. Why?",
      a: ["EF Core does not support triggers at all",
          "EF uses an OUTPUT clause to read back generated values, and SQL Server forbids that on a table with triggers — the table must be declared with HasTrigger, which switches EF to a slower strategy",
          "The trigger holds a lock EF cannot acquire",
          "Migrations must be regenerated after any schema change"],
      c: 1,
      why: "It is the sharpest argument against triggers in an EF codebase: a schema change made outside the application can break the application, and the error gives no hint that a trigger is involved." },

    { q: "When is a trigger genuinely the right tool?",
      a: ["Whenever you need auditing",
          "When you do not control every writer — rows also arrive from an integration job, a nightly import and someone in SSMS, so application-level auditing only records the polite callers",
          "When the logic is too slow for the application",
          "Never — they should always be replaced by application code"],
      c: 1,
      why: "The database is the one place every writer passes through — the same argument chapter 20 makes about invalidating a cache for a write path you do not own. Then offer temporal tables as the modern alternative." },
  ],

  /* ------------------------------------------------------------------ 11 */
  "11-tsql-performance": [
    { q: "What does an index actually cost?",
      a: ["Nothing",
          "Storage, plus time on every INSERT, UPDATE and DELETE that has to maintain it",
          "Only storage",
          "Only query time"],
      c: 1,
      why: "Which is why “add an index” is not a free answer. The interview follow-up is always about the write path, and about how many indexes is too many." },

    { q: "A query filters on `WHERE YEAR(OrderDate) = 2026` and the index on OrderDate is not used. Why?",
      a: ["The index is corrupt",
          "Wrapping the column in a function makes the predicate non-SARGable — the server cannot seek, so it scans",
          "YEAR is not supported",
          "The statistics are out of date"],
      c: 1,
      why: "Rewrite as a range: `OrderDate >= '2026-01-01' AND OrderDate < '2027-01-01'`. The same rule kills `WHERE LEFT(Code,3) = 'ABC'` and any implicit conversion on the column side." },

    { q: "What is a covering index?",
      a: ["An index on every column",
          "One that contains every column the query needs — key columns plus INCLUDE — so the server never has to look up the base table",
          "A clustered index",
          "An index with a filter"],
      c: 1,
      why: "The key lookup it eliminates is often the single most expensive operator in a plan. INCLUDE columns are stored only at the leaf level, so they widen the index far less than key columns." },

    { q: "In an execution plan, a “Key Lookup” next to an index seek means what?",
      a: ["The index is unused",
          "The index found the rows but did not contain every column needed, so the server is fetching the rest from the table — row by row",
          "The query is optimal",
          "A deadlock occurred"],
      c: 1,
      why: "It is the plan telling you exactly which columns to INCLUDE. Alongside it, watch for a thick arrow (many rows) and a big gap between estimated and actual row counts (stale statistics)." },

    { q: "What do the four letters of ACID stand for, and which one do isolation levels relax?",
      a: ["Atomicity, Consistency, Isolation, Durability — and Isolation is the one you trade away for concurrency",
          "Atomicity, Concurrency, Integrity, Durability — Durability is traded",
          "Access, Control, Identity, Data",
          "All four are fixed and cannot be changed"],
      c: 0,
      why: "READ COMMITTED, REPEATABLE READ, SERIALIZABLE and SNAPSHOT are all points on the isolation dial. Nothing else in ACID is configurable." },

    { q: "Which anomaly does READ COMMITTED still allow?",
      a: ["Dirty reads",
          "Non-repeatable reads and phantoms — the same query inside one transaction can return different data",
          "Nothing",
          "Lost updates only"],
      c: 1,
      why: "It blocks dirty reads only. SNAPSHOT isolation gives you a consistent view without blocking writers, at the cost of tempdb version-store pressure — the usual SQL Server compromise." },

    { q: "Two transactions deadlock. What actually happens?",
      a: ["Both wait forever",
          "SQL Server detects the cycle, kills one as the deadlock victim, and it must be retried",
          "The server restarts",
          "The rows are corrupted"],
      c: 1,
      why: "So the application MUST have a retry. The prevention is to touch objects in a consistent order and keep transactions short — never around a user prompt or an HTTP call." },

    { q: "Select every practice that reduces lock contention on a busy table.",
      a: ["Keeping transactions as short as possible",
          "Accessing tables in the same order everywhere",
          "Holding a transaction open across an external API call",
          "Using SNAPSHOT isolation for read-heavy reporting queries"],
      c: [0, 1, 3],
      why: "An HTTP call inside a transaction is the classic way to turn a 200 ms lock into a 30 second one. Whatever the timeout of that call is, it is now your lock duration." },

    { q: "A query loads 200,000 rows into a table variable and joins it to a large table. It never finishes, yet the plan looks tidy. What is happening?",
      a: ["The table variable is too large for tempdb",
          "The optimiser has no statistics for a table variable and historically estimates one row, so it picks a nested loop and runs it 200,000 times",
          "Table variables cannot be joined",
          "The join column needs an index on the large table"],
      c: 1,
      why: "Swapping @t for #t is often a one-character fix with a thousand-fold effect. SQL Server 2019 improved this with deferred compilation under compatibility level 150, but plenty of databases run lower." },

    { q: "Which is true of a table variable but NOT of a temp table?",
      a: ["It lives in tempdb",
          "Its contents survive a ROLLBACK",
          "It can be indexed after creation",
          "It is visible to a stored procedure it is passed to"],
      c: 1,
      why: "Both live in tempdb. Surviving a rollback is occasionally exactly what you want — it is how you keep a record of why something failed when the transaction took everything else with it." },

    { q: "You need an intermediate result that will be joined, filtered and indexed. Which do you choose?",
      a: ["A table variable, because it is lighter",
          "A temp table, because it has statistics and can be indexed after creation",
          "A common table expression",
          "A permanent staging table"],
      c: 1,
      why: "The rule of thumb: a few rows you are passing along, a table variable; anything you will join, filter or index, a temp table." },
  ],

  /* ----------------------------------------------------------------- 11b */
  "11b-oracle": [
    { q: "An advert says 'conoscenza dei database relazionali, preferibilmente Oracle'. What does 'preferibilmente' tell you?",
      a: ["Oracle is mandatory and you should not apply without it",
          "Oracle is preferred, not required — SQL Server and PostgreSQL fundamentals transfer, and Oracle itself is a plus",
          "They only accept Oracle certifications",
          "The database does not matter at all"],
      c: 1,
      why: "Preferibilmente means preferred. The relational fundamentals carry across engines, so the honest, strong answer is that your daily engines are SQL Server and PostgreSQL and you know where Oracle differs." },

    { q: "Which of these does NOT change when you move from SQL Server to Oracle?",
      a: ["The keyword for null fallback",
          "The relational model itself — keys, joins, indexes, transactions and isolation",
          "The name of the variable-length string type",
          "How you write the first-N-rows clause in legacy code"],
      c: 1,
      why: "The relational model is identical across engines. What differs is dialect, types and — genuinely — the default concurrency behaviour." },

    { q: "Why should you write VARCHAR2 rather than VARCHAR on Oracle?",
      a: ["VARCHAR is faster",
          "Oracle reserves VARCHAR and directs you to VARCHAR2 for variable-length strings, reserving the right to change what a bare VARCHAR means",
          "VARCHAR does not exist on Oracle",
          "They are stored differently on disk"],
      c: 1,
      why: "Using VARCHAR2 is also the tell that you have actually read Oracle rather than assumed it matches SQL Server." },

    { q: "On Oracle, what is stored when you insert an empty string '' into a VARCHAR2 column?",
      a: ["An empty string of length zero, distinct from NULL",
          "NULL — Oracle treats an empty string as NULL",
          "A single space",
          "It raises an error"],
      c: 1,
      why: "This is the most common cross-engine surprise. On SQL Server '' and NULL are distinct; on Oracle '' becomes NULL, so WHERE name = '' matches nothing." },

    { q: "A validation rejects blank names using WHERE name = ''. It works on SQL Server. What happens on Oracle?",
      a: ["It behaves identically",
          "It can invert — '' is NULL on Oracle, so the predicate matches nothing and the check passes everything",
          "It throws a syntax error",
          "It becomes case-sensitive"],
      c: 1,
      why: "Nothing equals NULL, so = '' never matches on Oracle. Test IS NULL instead, and never rely on blank being distinct from absent." },

    { q: "What is the modern (12c+) way to return the first ten rows by name on Oracle?",
      a: ["SELECT TOP 10 ...",
          "ORDER BY name FETCH FIRST 10 ROWS ONLY — the same standard clause SQL Server uses",
          "LIMIT 10",
          "SELECT FIRST 10 ..."],
      c: 1,
      why: "OFFSET/FETCH is SQL standard, so the paging you write for SQL Server 12c+ runs on Oracle unchanged. TOP and LIMIT are SQL Server and MySQL respectively." },

    { q: "In legacy Oracle, why does 'SELECT * FROM customers WHERE ROWNUM <= 10 ORDER BY name' return the wrong rows?",
      a: ["ROWNUM is not a real column",
          "ROWNUM is assigned before the ORDER BY, so it numbers ten arbitrary rows and then sorts those ten",
          "The limit should be < 10",
          "ORDER BY is not allowed with ROWNUM"],
      c: 1,
      why: "The fix is a sub-query: sort in the inner query, then apply ROWNUM in the outer one so it counts already-sorted rows." },

    { q: "How do you draw the next value from an Oracle sequence called customer_seq?",
      a: ["customer_seq.NEXT",
          "customer_seq.NEXTVAL",
          "NEXT VALUE FOR customer_seq",
          "customer_seq++"],
      c: 1,
      why: "NEXTVAL draws the next value; CURRVAL returns the last value THIS session drew, which is not a global latest. NEXT VALUE FOR is the SQL Server sequence syntax." },

    { q: "Are Oracle sequences gap-free?",
      a: ["Yes, they never skip numbers",
          "No — a rolled-back transaction still consumes its number, exactly like SQL Server IDENTITY",
          "Only if you set NOCACHE",
          "Only on Oracle 19c and later"],
      c: 1,
      why: "Neither engine promises a contiguous run. A business need for gap-free numbering (invoice numbers) is an application problem on both." },

    { q: "What is the closest modern Oracle equivalent of SQL Server's IDENTITY(1,1)?",
      a: ["AUTO_INCREMENT",
          "GENERATED AS IDENTITY, which wraps a sequence for you",
          "SERIAL",
          "ROWNUM"],
      c: 1,
      why: "AUTO_INCREMENT is MySQL and SERIAL is PostgreSQL. Oracle 12c+ offers GENERATED AS IDENTITY inline on the column." },

    { q: "What are the four sections of a PL/SQL block, in order?",
      a: ["BEGIN, TRY, CATCH, END",
          "DECLARE, BEGIN, EXCEPTION, END",
          "USE, DO, HANDLE, STOP",
          "OPEN, FETCH, CLOSE, END"],
      c: 1,
      why: "Variables first (DECLARE), logic in the middle (BEGIN), error handling last (EXCEPTION), then END. T-SQL instead wraps errors in TRY/CATCH." },

    { q: "In PL/SQL, what happens when 'SELECT ... INTO' matches no rows?",
      a: ["The variables are set to NULL silently",
          "It raises NO_DATA_FOUND",
          "It returns an empty result set",
          "It returns zero"],
      c: 1,
      why: "SELECT INTO expects exactly one row: no rows raises NO_DATA_FOUND, more than one raises TOO_MANY_ROWS. That is why the EXCEPTION block exists." },

    { q: "What is the single most important BEHAVIOURAL (not syntax) difference between Oracle's default and classic SQL Server's default?",
      a: ["Oracle is faster",
          "Oracle's default MVCC means readers never block writers and writers never block readers",
          "Oracle uses a different network port",
          "Oracle stores data in columns not rows"],
      c: 1,
      why: "A query reads a consistent snapshot as of when it started, reconstructed from undo. Classic SQL Server READ COMMITTED uses read locks, so readers and writers can block each other." },

    { q: "Why did the WITH (NOLOCK) hint become a cultural reflex on SQL Server but has no equivalent habit on Oracle?",
      a: ["NOLOCK is banned on Oracle",
          "Classic SQL Server readers and writers block each other, which NOLOCK avoids; Oracle's default MVCC does not have that problem",
          "Oracle has no locking at all",
          "Oracle queries are always read-only"],
      c: 1,
      why: "NOLOCK trades correctness for not waiting. On Oracle the reason it exists is not the default, and SQL Server matches Oracle only with Read Committed Snapshot Isolation (RCSI) on." },

    { q: "What does ORA-01555 'snapshot too old' indicate?",
      a: ["The database version is out of date",
          "A long-running query outlived the undo kept to reconstruct its consistent snapshot",
          "A table has not been backed up",
          "The statistics are stale"],
      c: 1,
      why: "It is the cost side of MVCC: the undo needed to serve the query's snapshot was overwritten before the query finished." },

    { q: "On Oracle, what happens to an open transaction when you run a CREATE TABLE?",
      a: ["It rolls back with the DDL",
          "DDL auto-commits — the CREATE commits the transaction around it",
          "Nothing; DDL is transactional like SQL Server",
          "The CREATE is queued until you COMMIT"],
      c: 1,
      why: "Oracle DDL auto-commits, which catches SQL Server developers who assume schema changes inside a transaction can be rolled back with everything else." },

    { q: "Coming from SQL Server, why might another session not see your INSERT on Oracle?",
      a: ["Oracle caches inserts for an hour",
          "You have not issued an explicit COMMIT — Oracle does not autocommit per statement",
          "The table needs a manual refresh",
          "Inserts are session-private forever"],
      c: 1,
      why: "SQL Server autocommits per statement by default; on Oracle nothing is durable or visible to other sessions until you COMMIT." },

    { q: "Which data-dictionary view lists the tables you own on Oracle?",
      a: ["sys.tables",
          "USER_TABLES (or ALL_TAB_COLUMNS for columns)",
          "INFORMATION_SCHEMA.TABLES",
          "pg_tables"],
      c: 1,
      why: "sys.tables and INFORMATION_SCHEMA are SQL Server; pg_tables is PostgreSQL. Oracle uses USER_*, ALL_* and DBA_* views." },

    { q: "In EF Core, what is the main change to run against Oracle instead of SQL Server?",
      a: ["Rewrite every LINQ query",
          "Swap the provider — UseOracle with Oracle.EntityFrameworkCore instead of UseSqlServer — and the connection string",
          "Convert the DbContext to raw SQL",
          "Drop EF Core entirely"],
      c: 1,
      why: "The provider model is exactly this seam. Entities, DbSets, LINQ and migrations stay; names, type mappings and the null rule are what leak through." },

    { q: "How does Oracle treat an unquoted identifier like the table name Customers?",
      a: ["It keeps the exact case you typed",
          "It folds it to uppercase CUSTOMERS; only a double-quoted \"Customers\" stays mixed-case",
          "It lowercases it",
          "It rejects mixed case"],
      c: 1,
      why: "Because a quoted identifier then forces every reference to quote it too, this casing rule is why EF Core's default names sometimes need explicit configuration on Oracle." },
  ],

  /* ------------------------------------------------------------------ 12 */
  "12-ef-core": [
    { q: "What is the N+1 problem?",
      a: ["A query that returns one too many rows",
          "One query for a list, then one more query per item to load a related entity — 1 + N round trips instead of one join",
          "A deadlock",
          "An off-by-one error in paging"],
      c: 1,
      why: "In EF Core it usually appears as a loop touching a navigation property with lazy loading on, or a Select over an unloaded collection. Include or a projection fixes it." },

    { q: "When should you use AsNoTracking()?",
      a: ["Always",
          "On read-only queries — it skips building change-tracking snapshots, which is real memory and CPU on large result sets",
          "Only on writes",
          "Never; it disables lazy loading"],
      c: 1,
      why: "It also stops the identity map deduplicating rows, so the same entity can appear as two objects. Fine for a DTO projection, wrong when you intend to update." },

    { q: "What does DbContext.SaveChanges() do with change tracking?",
      a: ["Saves every entity it has ever seen",
          "Compares each tracked entity against its original snapshot and issues INSERT/UPDATE/DELETE only for what actually changed — in one transaction",
          "Runs the migrations",
          "Clears the cache"],
      c: 1,
      why: "The implicit transaction is the part worth saying out loud: everything in one SaveChanges succeeds or fails together." },

    { q: "What lifetime should DbContext have in an ASP.NET Core application?",
      a: ["Singleton", "Scoped — one per request", "Transient per injection", "Static"],
      c: 1,
      why: "DbContext is not thread-safe and accumulates tracked entities. A singleton produces both concurrency exceptions and a slow memory leak; AddDbContext registers it scoped for exactly this reason." },

    { q: "Your LINQ query throws “could not be translated”. What is the fix, in general?",
      a: ["Add AsEnumerable() before it and move on",
          "Rewrite the expression using something the provider understands, or deliberately move the untranslatable part client-side after narrowing the rows",
          "Disable query translation",
          "Use a stored procedure always"],
      c: 1,
      why: "EF Core 3.0 deliberately made this an exception rather than a silent client-side evaluation, because the silent version shipped table scans to production." },

    { q: "What is the difference between Include and a projection with Select?",
      a: ["None",
          "Include loads whole entities and all their columns; a Select projection asks for only the columns the DTO needs — usually less data and no tracking",
          "Select cannot cross a relationship",
          "Include is always faster"],
      c: 1,
      why: "Projection is generally the better default for read endpoints. Include earns its place when you are about to modify the graph." },

    { q: "Select every statement that is true about EF Core migrations.",
      a: ["They are generated by comparing the model to a snapshot of the last migration",
          "Running Database.Migrate() at application startup is fine in production",
          "They should be committed to source control and reviewed like code",
          "A generated migration can be edited before it is applied"],
      c: [0, 2, 3],
      why: "Startup migration races when several instances start at once, and forces the runtime account to hold schema-altering permissions. Migrations belong in a single-instance pipeline step." },

    { q: "When is it right to drop out of EF Core into raw SQL?",
      a: ["Never",
          "For bulk operations, complex reporting queries, window functions and anything where the generated SQL is measurably wrong for the job",
          "For every query — EF Core is too slow",
          "Only for stored procedures"],
      c: 1,
      why: "FromSqlInterpolated keeps parameterisation while giving you the SQL. Knowing where the ORM stops paying is a senior signal, not a defeat." },
  ],
});
